"use strict";

var inherits = require('util').inherits
  , EventEmitter = require('events').EventEmitter
  , net = require('net')
  , tls = require('tls')
  , crypto = require('crypto')
  , f = require('util').format
  , debugOptions = require('./utils').debugOptions
  , Response = require('./commands').Response
  , MongoError = require('../error')
  , Logger = require('./logger');

var _id = 0;
var debugFields = ['host', 'port', 'size', 'keepAlive', 'keepAliveInitialDelay', 'noDelay'
  , 'connectionTimeout', 'socketTimeout', 'singleBufferSerializtion', 'ssl', 'ca', 'cert'
  , 'rejectUnauthorized', 'promoteLongs', 'promoteValues', 'promoteBuffers', 'checkServerIdentity'];
var connectionAccounting = false;
var connections = ***REMOVED******REMOVED***;

/**
 * Creates a new Connection instance
 * @class
 * @param ***REMOVED***string***REMOVED*** options.host The server host
 * @param ***REMOVED***number***REMOVED*** options.port The server port
 * @param ***REMOVED***boolean***REMOVED*** [options.keepAlive=true] TCP Connection keep alive enabled
 * @param ***REMOVED***number***REMOVED*** [options.keepAliveInitialDelay=0] Initial delay before TCP keep alive enabled
 * @param ***REMOVED***boolean***REMOVED*** [options.noDelay=true] TCP Connection no delay
 * @param ***REMOVED***number***REMOVED*** [options.connectionTimeout=0] TCP Connection timeout setting
 * @param ***REMOVED***number***REMOVED*** [options.socketTimeout=0] TCP Socket timeout setting
 * @param ***REMOVED***boolean***REMOVED*** [options.singleBufferSerializtion=true] Serialize into single buffer, trade of peak memory for serialization speed
 * @param ***REMOVED***boolean***REMOVED*** [options.ssl=false] Use SSL for connection
 * @param ***REMOVED***boolean|function***REMOVED*** [options.checkServerIdentity=true] Ensure we check server identify during SSL, set to false to disable checking. Only works for Node 0.12.x or higher. You can pass in a boolean or your own checkServerIdentity override function.
 * @param ***REMOVED***Buffer***REMOVED*** [options.ca] SSL Certificate store binary buffer
 * @param ***REMOVED***Buffer***REMOVED*** [options.cert] SSL Certificate binary buffer
 * @param ***REMOVED***Buffer***REMOVED*** [options.key] SSL Key file binary buffer
 * @param ***REMOVED***string***REMOVED*** [options.passphrase] SSL Certificate pass phrase
 * @param ***REMOVED***boolean***REMOVED*** [options.rejectUnauthorized=true] Reject unauthorized server certificates
 * @param ***REMOVED***boolean***REMOVED*** [options.promoteLongs=true] Convert Long values from the db into Numbers if they fit into 53 bits
 * @param ***REMOVED***boolean***REMOVED*** [options.promoteValues=true] Promotes BSON values to native types where possible, set to false to only receive wrapper types.
 * @param ***REMOVED***boolean***REMOVED*** [options.promoteBuffers=false] Promotes Binary BSON values to native Node Buffers.
 * @fires Connection#connect
 * @fires Connection#close
 * @fires Connection#error
 * @fires Connection#timeout
 * @fires Connection#parseError
 * @return ***REMOVED***Connection***REMOVED*** A cursor instance
 */
var Connection = function(messageHandler, options) ***REMOVED***
  // Add event listener
  EventEmitter.call(this);
  // Set empty if no options passed
  this.options = options || ***REMOVED******REMOVED***;
  // Identification information
  this.id = _id++;
  // Logger instance
  this.logger = Logger('Connection', options);
  // No bson parser passed in
  if(!options.bson) throw new Error("must pass in valid bson parser");
  // Get bson parser
  this.bson = options.bson;
  // Grouping tag used for debugging purposes
  this.tag = options.tag;
  // Message handler
  this.messageHandler = messageHandler;

  // Max BSON message size
  this.maxBsonMessageSize = options.maxBsonMessageSize || (1024 * 1024 * 16 * 4);
  // Debug information
  if(this.logger.isDebug()) this.logger.debug(f('creating connection %s with options [%s]', this.id, JSON.stringify(debugOptions(debugFields, options))));

  // Default options
  this.port = options.port || 27017;
  this.host = options.host || 'localhost';
  this.keepAlive = typeof options.keepAlive == 'boolean' ? options.keepAlive : true;
  this.keepAliveInitialDelay = options.keepAliveInitialDelay || 0;
  this.noDelay = typeof options.noDelay == 'boolean' ? options.noDelay : true;
  this.connectionTimeout = options.connectionTimeout || 0;
  this.socketTimeout = options.socketTimeout || 0;

  // If connection was destroyed
  this.destroyed = false;

  // Check if we have a domain socket
  this.domainSocket = this.host.indexOf('\/') != -1;

  // Serialize commands using function
  this.singleBufferSerializtion = typeof options.singleBufferSerializtion == 'boolean' ? options.singleBufferSerializtion : true;
  this.serializationFunction = this.singleBufferSerializtion ? 'toBinUnified' : 'toBin';

  // SSL options
  this.ca = options.ca || null;
  this.cert = options.cert || null;
  this.key = options.key || null;
  this.passphrase = options.passphrase || null;
  this.ssl = typeof options.ssl == 'boolean' ? options.ssl : false;
  this.rejectUnauthorized = typeof options.rejectUnauthorized == 'boolean' ? options.rejectUnauthorized : true;
  this.checkServerIdentity = typeof options.checkServerIdentity == 'boolean'
    || typeof options.checkServerIdentity == 'function' ? options.checkServerIdentity : true;

  // If ssl not enabled
  if(!this.ssl) this.rejectUnauthorized = false;

  // Response options
  this.responseOptions = ***REMOVED***
    promoteLongs: typeof options.promoteLongs == 'boolean' ?  options.promoteLongs : true,
    promoteValues: typeof options.promoteValues == 'boolean' ? options.promoteValues : true,
    promoteBuffers: typeof options.promoteBuffers == 'boolean' ? options.promoteBuffers: false
  ***REMOVED***

  // Flushing
  this.flushing = false;
  this.queue = [];

  // Internal state
  this.connection = null;
  this.writeStream = null;

  // Create hash method
  var hash = crypto.createHash('sha1');
  hash.update(f('%s:%s', this.host, this.port));

  // Create a hash name
  this.hashedName = hash.digest('hex');

  // All operations in flight on the connection
  this.workItems = [];
***REMOVED***

inherits(Connection, EventEmitter);

Connection.prototype.setSocketTimeout = function(value) ***REMOVED***
  if(this.connection) ***REMOVED***
    this.connection.setTimeout(value);
  ***REMOVED***
***REMOVED***

Connection.prototype.resetSocketTimeout = function() ***REMOVED***
  if(this.connection) ***REMOVED***
    this.connection.setTimeout(this.socketTimeout);
  ***REMOVED***
***REMOVED***

Connection.enableConnectionAccounting = function() ***REMOVED***
  connectionAccounting = true;
  connections = ***REMOVED******REMOVED***;
***REMOVED***

Connection.disableConnectionAccounting = function() ***REMOVED***
  connectionAccounting = false;
***REMOVED***

Connection.connections = function() ***REMOVED***
  return connections;
***REMOVED***

function deleteConnection(id) ***REMOVED***
  delete connections[id];
***REMOVED***

function addConnection(id, connection) ***REMOVED***
  connections[id] = connection;
***REMOVED***

//
// Connection handlers
var errorHandler = function(self) ***REMOVED***
  return function(err) ***REMOVED***
    if(connectionAccounting) deleteConnection(self.id);
    // Debug information
    if(self.logger.isDebug()) self.logger.debug(f('connection %s for [%s:%s] errored out with [%s]', self.id, self.host, self.port, JSON.stringify(err)));
    // Emit the error
    if(self.listeners('error').length > 0) self.emit("error", MongoError.create(err), self);
  ***REMOVED***
***REMOVED***

var timeoutHandler = function(self) ***REMOVED***
  return function() ***REMOVED***
    if(connectionAccounting) deleteConnection(self.id);
    // Debug information
    if(self.logger.isDebug()) self.logger.debug(f('connection %s for [%s:%s] timed out', self.id, self.host, self.port));
    // Emit timeout error
    self.emit("timeout"
      , MongoError.create(f("connection %s to %s:%s timed out", self.id, self.host, self.port))
      , self);
  ***REMOVED***
***REMOVED***

var closeHandler = function(self) ***REMOVED***
  return function(hadError) ***REMOVED***
    if(connectionAccounting) deleteConnection(self.id);
    // Debug information
    if(self.logger.isDebug()) self.logger.debug(f('connection %s with for [%s:%s] closed', self.id, self.host, self.port));

    // Emit close event
    if(!hadError) ***REMOVED***
      self.emit("close"
        , MongoError.create(f("connection %s to %s:%s closed", self.id, self.host, self.port))
        , self);
    ***REMOVED***
  ***REMOVED***
***REMOVED***

var dataHandler = function(self) ***REMOVED***
  return function(data) ***REMOVED***
    // Parse until we are done with the data
    while(data.length > 0) ***REMOVED***
      // If we still have bytes to read on the current message
      if(self.bytesRead > 0 && self.sizeOfMessage > 0) ***REMOVED***
        // Calculate the amount of remaining bytes
        var remainingBytesToRead = self.sizeOfMessage - self.bytesRead;
        // Check if the current chunk contains the rest of the message
        if(remainingBytesToRead > data.length) ***REMOVED***
          // Copy the new data into the exiting buffer (should have been allocated when we know the message size)
          data.copy(self.buffer, self.bytesRead);
          // Adjust the number of bytes read so it point to the correct index in the buffer
          self.bytesRead = self.bytesRead + data.length;

          // Reset state of buffer
          data = new Buffer(0);
        ***REMOVED*** else ***REMOVED***
          // Copy the missing part of the data into our current buffer
          data.copy(self.buffer, self.bytesRead, 0, remainingBytesToRead);
          // Slice the overflow into a new buffer that we will then re-parse
          data = data.slice(remainingBytesToRead);

          // Emit current complete message
          try ***REMOVED***
            var emitBuffer = self.buffer;
            // Reset state of buffer
            self.buffer = null;
            self.sizeOfMessage = 0;
            self.bytesRead = 0;
            self.stubBuffer = null;
            // Emit the buffer
            self.messageHandler(new Response(self.bson, emitBuffer, self.responseOptions), self);
          ***REMOVED*** catch(err) ***REMOVED***
            var errorObject = ***REMOVED***err:"socketHandler", trace:err, bin:self.buffer, parseState:***REMOVED***
              sizeOfMessage:self.sizeOfMessage,
              bytesRead:self.bytesRead,
              stubBuffer:self.stubBuffer***REMOVED******REMOVED***;
            // We got a parse Error fire it off then keep going
            self.emit("parseError", errorObject, self);
          ***REMOVED***
        ***REMOVED***
      ***REMOVED*** else ***REMOVED***
        // Stub buffer is kept in case we don't get enough bytes to determine the
        // size of the message (< 4 bytes)
        if(self.stubBuffer != null && self.stubBuffer.length > 0) ***REMOVED***
          // If we have enough bytes to determine the message size let's do it
          if(self.stubBuffer.length + data.length > 4) ***REMOVED***
            // Prepad the data
            var newData = new Buffer(self.stubBuffer.length + data.length);
            self.stubBuffer.copy(newData, 0);
            data.copy(newData, self.stubBuffer.length);
            // Reassign for parsing
            data = newData;

            // Reset state of buffer
            self.buffer = null;
            self.sizeOfMessage = 0;
            self.bytesRead = 0;
            self.stubBuffer = null;

          ***REMOVED*** else ***REMOVED***

            // Add the the bytes to the stub buffer
            var newStubBuffer = new Buffer(self.stubBuffer.length + data.length);
            // Copy existing stub buffer
            self.stubBuffer.copy(newStubBuffer, 0);
            // Copy missing part of the data
            data.copy(newStubBuffer, self.stubBuffer.length);
            // Exit parsing loop
            data = new Buffer(0);
          ***REMOVED***
        ***REMOVED*** else ***REMOVED***
          if(data.length > 4) ***REMOVED***
            // Retrieve the message size
            // var sizeOfMessage = data.readUInt32LE(0);
            var sizeOfMessage = data[0] | data[1] << 8 | data[2] << 16 | data[3] << 24;
            // If we have a negative sizeOfMessage emit error and return
            if(sizeOfMessage < 0 || sizeOfMessage > self.maxBsonMessageSize) ***REMOVED***
              errorObject = ***REMOVED***err:"socketHandler", trace:'', bin:self.buffer, parseState:***REMOVED***
                sizeOfMessage: sizeOfMessage,
                bytesRead: self.bytesRead,
                stubBuffer: self.stubBuffer***REMOVED******REMOVED***;
              // We got a parse Error fire it off then keep going
              self.emit("parseError", errorObject, self);
              return;
            ***REMOVED***

            // Ensure that the size of message is larger than 0 and less than the max allowed
            if(sizeOfMessage > 4 && sizeOfMessage < self.maxBsonMessageSize && sizeOfMessage > data.length) ***REMOVED***
              self.buffer = new Buffer(sizeOfMessage);
              // Copy all the data into the buffer
              data.copy(self.buffer, 0);
              // Update bytes read
              self.bytesRead = data.length;
              // Update sizeOfMessage
              self.sizeOfMessage = sizeOfMessage;
              // Ensure stub buffer is null
              self.stubBuffer = null;
              // Exit parsing loop
              data = new Buffer(0);

            ***REMOVED*** else if(sizeOfMessage > 4 && sizeOfMessage < self.maxBsonMessageSize && sizeOfMessage == data.length) ***REMOVED***
              try ***REMOVED***
                emitBuffer = data;
                // Reset state of buffer
                self.buffer = null;
                self.sizeOfMessage = 0;
                self.bytesRead = 0;
                self.stubBuffer = null;
                // Exit parsing loop
                data = new Buffer(0);
                // Emit the message
                self.messageHandler(new Response(self.bson, emitBuffer, self.responseOptions), self);
              ***REMOVED*** catch (err) ***REMOVED***
                self.emit("parseError", err, self);
              ***REMOVED***
            ***REMOVED*** else if(sizeOfMessage <= 4 || sizeOfMessage > self.maxBsonMessageSize) ***REMOVED***
              errorObject = ***REMOVED***err:"socketHandler", trace:null, bin:data, parseState:***REMOVED***
                sizeOfMessage:sizeOfMessage,
                bytesRead:0,
                buffer:null,
                stubBuffer:null***REMOVED******REMOVED***;
              // We got a parse Error fire it off then keep going
              self.emit("parseError", errorObject, self);

              // Clear out the state of the parser
              self.buffer = null;
              self.sizeOfMessage = 0;
              self.bytesRead = 0;
              self.stubBuffer = null;
              // Exit parsing loop
              data = new Buffer(0);
            ***REMOVED*** else ***REMOVED***
              emitBuffer = data.slice(0, sizeOfMessage);
              // Reset state of buffer
              self.buffer = null;
              self.sizeOfMessage = 0;
              self.bytesRead = 0;
              self.stubBuffer = null;
              // Copy rest of message
              data = data.slice(sizeOfMessage);
              // Emit the message
              self.messageHandler(new Response(self.bson, emitBuffer, self.responseOptions), self);
            ***REMOVED***
          ***REMOVED*** else ***REMOVED***
            // Create a buffer that contains the space for the non-complete message
            self.stubBuffer = new Buffer(data.length)
            // Copy the data to the stub buffer
            data.copy(self.stubBuffer, 0);
            // Exit parsing loop
            data = new Buffer(0);
          ***REMOVED***
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
***REMOVED***

// List of socket level valid ssl options
var legalSslSocketOptions = ['pfx', 'key', 'passphrase', 'cert', 'ca', 'ciphers'
  , 'NPNProtocols', 'ALPNProtocols', 'servername'
  , 'secureProtocol', 'secureContext', 'session'
  , 'minDHSize'];

function merge(options1, options2) ***REMOVED***
  // Merge in any allowed ssl options
  for(var name in options2) ***REMOVED***
    if(options2[name] != null && legalSslSocketOptions.indexOf(name) != -1) ***REMOVED***
      options1[name] = options2[name];
    ***REMOVED***
  ***REMOVED***
***REMOVED***

/**
 * Connect
 * @method
 */
Connection.prototype.connect = function(_options) ***REMOVED***
  var self = this;
  _options = _options || ***REMOVED******REMOVED***;
  // Set the connections
  if(connectionAccounting) addConnection(this.id, this);
  // Check if we are overriding the promoteLongs
  if(typeof _options.promoteLongs == 'boolean') ***REMOVED***
    self.responseOptions.promoteLongs = _options.promoteLongs;
    self.responseOptions.promoteValues = _options.promoteValues;
    self.responseOptions.promoteBuffers = _options.promoteBuffers;
  ***REMOVED***

  // Create new connection instance
  self.connection = self.domainSocket
    ? net.createConnection(self.host)
    : net.createConnection(self.port, self.host);

  // Set the options for the connection
  self.connection.setKeepAlive(self.keepAlive, self.keepAliveInitialDelay);
  self.connection.setTimeout(self.connectionTimeout);
  self.connection.setNoDelay(self.noDelay);

  // If we have ssl enabled
  if(self.ssl) ***REMOVED***
    var sslOptions = ***REMOVED***
        socket: self.connection
      , rejectUnauthorized: self.rejectUnauthorized
    ***REMOVED***

    // Merge in options
    merge(sslOptions, this.options);
    merge(sslOptions, _options);

    // Set options for ssl
    if(self.ca) sslOptions.ca = self.ca;
    if(self.cert) sslOptions.cert = self.cert;
    if(self.key) sslOptions.key = self.key;
    if(self.passphrase) sslOptions.passphrase = self.passphrase;

    // Override checkServerIdentity behavior
    if(self.checkServerIdentity == false) ***REMOVED***
      // Skip the identiy check by retuning undefined as per node documents
      // https://nodejs.org/api/tls.html#tls_tls_connect_options_callback
      sslOptions.checkServerIdentity = function() ***REMOVED***
        return undefined;
      ***REMOVED***
    ***REMOVED*** else if(typeof self.checkServerIdentity == 'function') ***REMOVED***
      sslOptions.checkServerIdentity = self.checkServerIdentity;
    ***REMOVED***

    // Set default sni servername to be the same as host
    if(sslOptions.servername == null) ***REMOVED***
      sslOptions.servername = self.host;
    ***REMOVED***

    // Attempt SSL connection
    self.connection = tls.connect(self.port, self.host, sslOptions, function() ***REMOVED***
      // Error on auth or skip
      if(self.connection.authorizationError && self.rejectUnauthorized) ***REMOVED***
        return self.emit("error", self.connection.authorizationError, self, ***REMOVED***ssl:true***REMOVED***);
      ***REMOVED***

      // Set socket timeout instead of connection timeout
      self.connection.setTimeout(self.socketTimeout);
      // We are done emit connect
      self.emit('connect', self);
    ***REMOVED***);
    self.connection.setTimeout(self.connectionTimeout);
  ***REMOVED*** else ***REMOVED***
    self.connection.on('connect', function() ***REMOVED***
      // Set socket timeout instead of connection timeout
      self.connection.setTimeout(self.socketTimeout);
      // Emit connect event
      self.emit('connect', self);
    ***REMOVED***);
  ***REMOVED***

  // Add handlers for events
  self.connection.once('error', errorHandler(self));
  self.connection.once('timeout', timeoutHandler(self));
  self.connection.once('close', closeHandler(self));
  self.connection.on('data', dataHandler(self));
***REMOVED***

/**
 * Unref this connection
 * @method
 * @return ***REMOVED***boolean***REMOVED***
 */
Connection.prototype.unref = function() ***REMOVED***
  if (this.connection) this.connection.unref();
  else ***REMOVED***
    var self = this;
    this.once('connect', function() ***REMOVED***
      self.connection.unref();
    ***REMOVED***);
  ***REMOVED***
***REMOVED***

/**
 * Destroy connection
 * @method
 */
Connection.prototype.destroy = function() ***REMOVED***
  // Set the connections
  if(connectionAccounting) deleteConnection(this.id);
  if(this.connection) ***REMOVED***
    this.connection.end();
    this.connection.destroy();
  ***REMOVED***

  this.destroyed = true;
***REMOVED***

/**
 * Write to connection
 * @method
 * @param ***REMOVED***Command***REMOVED*** command Command to write out need to implement toBin and toBinUnified
 */
Connection.prototype.write = function(buffer) ***REMOVED***
  var i;
  // Debug Log
  if(this.logger.isDebug()) ***REMOVED***
    if(!Array.isArray(buffer)) ***REMOVED***
      this.logger.debug(f('writing buffer [%s] to %s:%s', buffer.toString('hex'), this.host, this.port));
    ***REMOVED*** else ***REMOVED***
      for(i = 0; i < buffer.length; i++)
        this.logger.debug(f('writing buffer [%s] to %s:%s', buffer[i].toString('hex'), this.host, this.port));
    ***REMOVED***
  ***REMOVED***

  // Write out the command
  if(!Array.isArray(buffer)) return this.connection.write(buffer, 'binary');
  // Iterate over all buffers and write them in order to the socket
  for(i = 0; i < buffer.length; i++) this.connection.write(buffer[i], 'binary');
***REMOVED***

/**
 * Return id of connection as a string
 * @method
 * @return ***REMOVED***string***REMOVED***
 */
Connection.prototype.toString = function() ***REMOVED***
  return "" + this.id;
***REMOVED***

/**
 * Return json object of connection
 * @method
 * @return ***REMOVED***object***REMOVED***
 */
Connection.prototype.toJSON = function() ***REMOVED***
  return ***REMOVED***id: this.id, host: this.host, port: this.port***REMOVED***;
***REMOVED***

/**
 * Is the connection connected
 * @method
 * @return ***REMOVED***boolean***REMOVED***
 */
Connection.prototype.isConnected = function() ***REMOVED***
  if(this.destroyed) return false;
  return !this.connection.destroyed && this.connection.writable;
***REMOVED***

/**
 * A server connect event, used to verify that the connection is up and running
 *
 * @event Connection#connect
 * @type ***REMOVED***Connection***REMOVED***
 */

/**
 * The server connection closed, all pool connections closed
 *
 * @event Connection#close
 * @type ***REMOVED***Connection***REMOVED***
 */

/**
 * The server connection caused an error, all pool connections closed
 *
 * @event Connection#error
 * @type ***REMOVED***Connection***REMOVED***
 */

/**
 * The server connection timed out, all pool connections closed
 *
 * @event Connection#timeout
 * @type ***REMOVED***Connection***REMOVED***
 */

/**
 * The driver experienced an invalid message, all pool connections closed
 *
 * @event Connection#parseError
 * @type ***REMOVED***Connection***REMOVED***
 */

module.exports = Connection;

"use strict";

/**
 * Creates a new MongoError
 * @class
 * @augments Error
 * @param ***REMOVED***string***REMOVED*** message The error message
 * @return ***REMOVED***MongoError***REMOVED*** A MongoError instance
 */
function MongoError(message) ***REMOVED***
  this.name = 'MongoError';
  this.message = message;
  Error.captureStackTrace(this, MongoError);
***REMOVED***

/**
 * Creates a new MongoError object
 * @method
 * @param ***REMOVED***object***REMOVED*** options The error options
 * @return ***REMOVED***MongoError***REMOVED*** A MongoError instance
 */
MongoError.create = function(options) ***REMOVED***
  var err = null;

  if(options instanceof Error) ***REMOVED***
    err = new MongoError(options.message);
    err.stack = options.stack;
  ***REMOVED*** else if(typeof options == 'string') ***REMOVED***
    err = new MongoError(options);
  ***REMOVED*** else ***REMOVED***
    err = new MongoError(options.message || options.errmsg || options.$err || "n/a");
    // Other options
    for(var name in options) ***REMOVED***
      err[name] = options[name];
    ***REMOVED***
  ***REMOVED***

  return err;
***REMOVED***

// Extend JavaScript error
MongoError.prototype = new Error;

module.exports = MongoError;

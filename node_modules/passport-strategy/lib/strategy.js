/**
 * Creates an instance of `Strategy`.
 *
 * @constructor
 * @api public
 */
function Strategy() ***REMOVED***
***REMOVED***

/**
 * Authenticate request.
 *
 * This function must be overridden by subclasses.  In abstract form, it always
 * throws an exception.
 *
 * @param ***REMOVED***Object***REMOVED*** req The request to authenticate.
 * @param ***REMOVED***Object***REMOVED*** [options] Strategy-specific options.
 * @api public
 */
Strategy.prototype.authenticate = function(req, options) ***REMOVED***
  throw new Error('Strategy#authenticate must be overridden by subclass');
***REMOVED***;


/**
 * Expose `Strategy`.
 */
module.exports = Strategy;

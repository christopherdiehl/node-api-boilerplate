/*!
 * depd
 * Copyright(c) 2014 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module exports.
 */

module.exports = bufferConcat

/**
 * Concatenate an array of Buffers.
 */

function bufferConcat(bufs) ***REMOVED***
  var length = 0

  for (var i = 0, len = bufs.length; i < len; i++) ***REMOVED***
    length += bufs[i].length
  ***REMOVED***

  var buf = new Buffer(length)
  var pos = 0

  for (var i = 0, len = bufs.length; i < len; i++) ***REMOVED***
    bufs[i].copy(buf, pos)
    pos += bufs[i].length
  ***REMOVED***

  return buf
***REMOVED***

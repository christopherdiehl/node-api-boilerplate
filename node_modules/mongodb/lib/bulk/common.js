"use strict";

var Long = require('mongodb-core').BSON.Long,
  Timestamp = require('mongodb-core').BSON.Timestamp;

// Error codes
var UNKNOWN_ERROR = 8;
var INVALID_BSON_ERROR = 22;
var WRITE_CONCERN_ERROR = 64;
var MULTIPLE_ERROR = 65;

// Insert types
var INSERT = 1;
var UPDATE = 2;
var REMOVE = 3


// Get write concern
var writeConcern = function(target, col, options) ***REMOVED***
  var writeConcern = ***REMOVED******REMOVED***;

  // Collection level write concern
  if(col.writeConcern && col.writeConcern.w != null) writeConcern.w = col.writeConcern.w;
  if(col.writeConcern && col.writeConcern.j != null) writeConcern.j = col.writeConcern.j;
  if(col.writeConcern && col.writeConcern.fsync != null) writeConcern.fsync = col.writeConcern.fsync;
  if(col.writeConcern && col.writeConcern.wtimeout != null) writeConcern.wtimeout = col.writeConcern.wtimeout;

  // Options level write concern
  if(options && options.w != null) writeConcern.w = options.w;
  if(options && options.wtimeout != null) writeConcern.wtimeout = options.wtimeout;
  if(options && options.j != null) writeConcern.j = options.j;
  if(options && options.fsync != null) writeConcern.fsync = options.fsync;

  // Return write concern
  return writeConcern;
***REMOVED***

/**
 * Helper function to define properties
 * @ignore
 */
var defineReadOnlyProperty = function(self, name, value) ***REMOVED***
  Object.defineProperty(self, name, ***REMOVED***
      enumerable: true
    , get: function() ***REMOVED***
      return value;
    ***REMOVED***
  ***REMOVED***);
***REMOVED***

/**
 * Keeps the state of a unordered batch so we can rewrite the results
 * correctly after command execution
 * @ignore
 */
var Batch = function(batchType, originalZeroIndex) ***REMOVED***
  this.originalZeroIndex = originalZeroIndex;
  this.currentIndex = 0;
  this.originalIndexes = [];
  this.batchType = batchType;
  this.operations = [];
  this.size = 0;
  this.sizeBytes = 0;
***REMOVED***

/**
 * Wraps a legacy operation so we can correctly rewrite it's error
 * @ignore
 */
var LegacyOp = function(batchType, operation, index) ***REMOVED***
  this.batchType = batchType;
  this.index = index;
  this.operation = operation;
***REMOVED***

/**
 * Create a new BulkWriteResult instance (INTERNAL TYPE, do not instantiate directly)
 *
 * @class
 * @property ***REMOVED***boolean***REMOVED*** ok Did bulk operation correctly execute
 * @property ***REMOVED***number***REMOVED*** nInserted number of inserted documents
 * @property ***REMOVED***number***REMOVED*** nUpdated number of documents updated logically
 * @property ***REMOVED***number***REMOVED*** nUpserted Number of upserted documents
 * @property ***REMOVED***number***REMOVED*** nModified Number of documents updated physically on disk
 * @property ***REMOVED***number***REMOVED*** nRemoved Number of removed documents
 * @return ***REMOVED***BulkWriteResult***REMOVED*** a BulkWriteResult instance
 */
var BulkWriteResult = function(bulkResult) ***REMOVED***
  defineReadOnlyProperty(this, "ok", bulkResult.ok);
  defineReadOnlyProperty(this, "nInserted", bulkResult.nInserted);
  defineReadOnlyProperty(this, "nUpserted", bulkResult.nUpserted);
  defineReadOnlyProperty(this, "nMatched", bulkResult.nMatched);
  defineReadOnlyProperty(this, "nModified", bulkResult.nModified);
  defineReadOnlyProperty(this, "nRemoved", bulkResult.nRemoved);

  /**
   * Return an array of inserted ids
   *
   * @return ***REMOVED***object[]***REMOVED***
   */
  this.getInsertedIds = function() ***REMOVED***
    return bulkResult.insertedIds;
  ***REMOVED***

  /**
   * Return an array of upserted ids
   *
   * @return ***REMOVED***object[]***REMOVED***
   */
  this.getUpsertedIds = function() ***REMOVED***
    return bulkResult.upserted;
  ***REMOVED***

  /**
   * Return the upserted id at position x
   *
   * @param ***REMOVED***number***REMOVED*** index the number of the upserted id to return, returns undefined if no result for passed in index
   * @return ***REMOVED***object***REMOVED***
   */
  this.getUpsertedIdAt = function(index) ***REMOVED***
    return bulkResult.upserted[index];
  ***REMOVED***

  /**
   * Return raw internal result
   *
   * @return ***REMOVED***object***REMOVED***
   */
  this.getRawResponse = function() ***REMOVED***
    return bulkResult;
  ***REMOVED***

  /**
   * Returns true if the bulk operation contains a write error
   *
   * @return ***REMOVED***boolean***REMOVED***
   */
  this.hasWriteErrors = function() ***REMOVED***
    return bulkResult.writeErrors.length > 0;
  ***REMOVED***

  /**
   * Returns the number of write errors off the bulk operation
   *
   * @return ***REMOVED***number***REMOVED***
   */
  this.getWriteErrorCount = function() ***REMOVED***
    return bulkResult.writeErrors.length;
  ***REMOVED***

  /**
   * Returns a specific write error object
   *
   * @return ***REMOVED***WriteError***REMOVED***
   */
  this.getWriteErrorAt = function(index) ***REMOVED***
    if(index < bulkResult.writeErrors.length) ***REMOVED***
      return bulkResult.writeErrors[index];
    ***REMOVED***
    return null;
  ***REMOVED***

  /**
   * Retrieve all write errors
   *
   * @return ***REMOVED***object[]***REMOVED***
   */
  this.getWriteErrors = function() ***REMOVED***
    return bulkResult.writeErrors;
  ***REMOVED***

  /**
   * Retrieve lastOp if available
   *
   * @return ***REMOVED***object***REMOVED***
   */
  this.getLastOp = function() ***REMOVED***
    return bulkResult.lastOp;
  ***REMOVED***

  /**
   * Retrieve the write concern error if any
   *
   * @return ***REMOVED***WriteConcernError***REMOVED***
   */
  this.getWriteConcernError = function() ***REMOVED***
    if(bulkResult.writeConcernErrors.length == 0) ***REMOVED***
      return null;
    ***REMOVED*** else if(bulkResult.writeConcernErrors.length == 1) ***REMOVED***
      // Return the error
      return bulkResult.writeConcernErrors[0];
    ***REMOVED*** else ***REMOVED***

      // Combine the errors
      var errmsg = "";
      for(var i = 0; i < bulkResult.writeConcernErrors.length; i++) ***REMOVED***
        var err = bulkResult.writeConcernErrors[i];
        errmsg = errmsg + err.errmsg;

        // TODO: Something better
        if(i == 0) errmsg = errmsg + " and ";
      ***REMOVED***

      return new WriteConcernError(***REMOVED*** errmsg : errmsg, code : WRITE_CONCERN_ERROR ***REMOVED***);
    ***REMOVED***
  ***REMOVED***

  this.toJSON = function() ***REMOVED***
    return bulkResult;
  ***REMOVED***

  this.toString = function() ***REMOVED***
    return "BulkWriteResult(" + this.toJSON(bulkResult) + ")";
  ***REMOVED***

  this.isOk = function() ***REMOVED***
    return bulkResult.ok == 1;
  ***REMOVED***
***REMOVED***

/**
 * Create a new WriteConcernError instance (INTERNAL TYPE, do not instantiate directly)
 *
 * @class
 * @property ***REMOVED***number***REMOVED*** code Write concern error code.
 * @property ***REMOVED***string***REMOVED*** errmsg Write concern error message.
 * @return ***REMOVED***WriteConcernError***REMOVED*** a WriteConcernError instance
 */
var WriteConcernError = function(err) ***REMOVED***
  if(!(this instanceof WriteConcernError)) return new WriteConcernError(err);

  // Define properties
  defineReadOnlyProperty(this, "code", err.code);
  defineReadOnlyProperty(this, "errmsg", err.errmsg);

  this.toJSON = function() ***REMOVED***
    return ***REMOVED***code: err.code, errmsg: err.errmsg***REMOVED***;
  ***REMOVED***

  this.toString = function() ***REMOVED***
    return "WriteConcernError(" + err.errmsg + ")";
  ***REMOVED***
***REMOVED***

/**
 * Create a new WriteError instance (INTERNAL TYPE, do not instantiate directly)
 *
 * @class
 * @property ***REMOVED***number***REMOVED*** code Write concern error code.
 * @property ***REMOVED***number***REMOVED*** index Write concern error original bulk operation index.
 * @property ***REMOVED***string***REMOVED*** errmsg Write concern error message.
 * @return ***REMOVED***WriteConcernError***REMOVED*** a WriteConcernError instance
 */
var WriteError = function(err) ***REMOVED***
  if(!(this instanceof WriteError)) return new WriteError(err);

  // Define properties
  defineReadOnlyProperty(this, "code", err.code);
  defineReadOnlyProperty(this, "index", err.index);
  defineReadOnlyProperty(this, "errmsg", err.errmsg);

  //
  // Define access methods
  this.getOperation = function() ***REMOVED***
    return err.op;
  ***REMOVED***

  this.toJSON = function() ***REMOVED***
    return ***REMOVED***code: err.code, index: err.index, errmsg: err.errmsg, op: err.op***REMOVED***;
  ***REMOVED***

  this.toString = function() ***REMOVED***
    return "WriteError(" + JSON.stringify(this.toJSON()) + ")";
  ***REMOVED***
***REMOVED***

/**
 * Merges results into shared data structure
 * @ignore
 */
var mergeBatchResults = function(ordered, batch, bulkResult, err, result) ***REMOVED***
  // If we have an error set the result to be the err object
  if(err) ***REMOVED***
    result = err;
  ***REMOVED*** else if(result && result.result) ***REMOVED***
    result = result.result;
  ***REMOVED*** else if(result == null) ***REMOVED***
    return;
  ***REMOVED***

  // Do we have a top level error stop processing and return
  if(result.ok == 0 && bulkResult.ok == 1) ***REMOVED***
    bulkResult.ok = 0;

    var writeError = ***REMOVED***
        index: 0
      , code: result.code || 0
      , errmsg: result.message
      , op: batch.operations[0]
    ***REMOVED***;

    bulkResult.writeErrors.push(new WriteError(writeError));
    return;
  ***REMOVED*** else if(result.ok == 0 && bulkResult.ok == 0) ***REMOVED***
    return;
  ***REMOVED***

  // Deal with opTime if available
  if(result.opTime || result.lastOp) ***REMOVED***
    var opTime = result.lastOp || result.opTime;
    var lastOpTS = null;
    var lastOpT = null;

    // We have a time stamp
    if(opTime && opTime._bsontype == 'Timestamp') ***REMOVED***
      if(bulkResult.lastOp == null) ***REMOVED***
        bulkResult.lastOp = opTime;
      ***REMOVED*** else if(opTime.greaterThan(bulkResult.lastOp)) ***REMOVED***
        bulkResult.lastOp = opTime;
      ***REMOVED***
    ***REMOVED*** else ***REMOVED***
      // Existing TS
      if(bulkResult.lastOp) ***REMOVED***
        lastOpTS = typeof bulkResult.lastOp.ts == 'number'
          ? Long.fromNumber(bulkResult.lastOp.ts) : bulkResult.lastOp.ts;
        lastOpT = typeof bulkResult.lastOp.t == 'number'
          ? Long.fromNumber(bulkResult.lastOp.t) : bulkResult.lastOp.t;
      ***REMOVED***

      // Current OpTime TS
      var opTimeTS = typeof opTime.ts == 'number'
        ? Long.fromNumber(opTime.ts) : opTime.ts;
      var opTimeT = typeof opTime.t == 'number'
        ? Long.fromNumber(opTime.t) : opTime.t;

      // Compare the opTime's
      if(bulkResult.lastOp == null) ***REMOVED***
        bulkResult.lastOp = opTime;
      ***REMOVED*** else if(opTimeTS.greaterThan(lastOpTS)) ***REMOVED***
        bulkResult.lastOp = opTime;
      ***REMOVED*** else if(opTimeTS.equals(lastOpTS)) ***REMOVED***
        if(opTimeT.greaterThan(lastOpT)) ***REMOVED***
          bulkResult.lastOp = opTime;
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  // If we have an insert Batch type
  if(batch.batchType == INSERT && result.n) ***REMOVED***
    bulkResult.nInserted = bulkResult.nInserted + result.n;
  ***REMOVED***

  // If we have an insert Batch type
  if(batch.batchType == REMOVE && result.n) ***REMOVED***
    bulkResult.nRemoved = bulkResult.nRemoved + result.n;
  ***REMOVED***

  var nUpserted = 0;

  // We have an array of upserted values, we need to rewrite the indexes
  if(Array.isArray(result.upserted)) ***REMOVED***
    nUpserted = result.upserted.length;

    for(var i = 0; i < result.upserted.length; i++) ***REMOVED***
      bulkResult.upserted.push(***REMOVED***
          index: result.upserted[i].index + batch.originalZeroIndex
        , _id: result.upserted[i]._id
      ***REMOVED***);
    ***REMOVED***
  ***REMOVED*** else if(result.upserted) ***REMOVED***

    nUpserted = 1;

    bulkResult.upserted.push(***REMOVED***
        index: batch.originalZeroIndex
      , _id: result.upserted
    ***REMOVED***);
  ***REMOVED***

  // If we have an update Batch type
  if(batch.batchType == UPDATE && result.n) ***REMOVED***
    var nModified = result.nModified;
    bulkResult.nUpserted = bulkResult.nUpserted + nUpserted;
    bulkResult.nMatched = bulkResult.nMatched + (result.n - nUpserted);

    if(typeof nModified == 'number') ***REMOVED***
      bulkResult.nModified = bulkResult.nModified + nModified;
    ***REMOVED*** else ***REMOVED***
      bulkResult.nModified = null;
    ***REMOVED***
  ***REMOVED***

  if(Array.isArray(result.writeErrors)) ***REMOVED***
    for(i = 0; i < result.writeErrors.length; i++) ***REMOVED***

      writeError = ***REMOVED***
          index: batch.originalZeroIndex + result.writeErrors[i].index
        , code: result.writeErrors[i].code
        , errmsg: result.writeErrors[i].errmsg
        , op: batch.operations[result.writeErrors[i].index]
      ***REMOVED***;

      bulkResult.writeErrors.push(new WriteError(writeError));
    ***REMOVED***
  ***REMOVED***

  if(result.writeConcernError) ***REMOVED***
    bulkResult.writeConcernErrors.push(new WriteConcernError(result.writeConcernError));
  ***REMOVED***
***REMOVED***

//
// Clone the options
var cloneOptions = function(options) ***REMOVED***
  var clone = ***REMOVED******REMOVED***;
  var keys = Object.keys(options);
  for(var i = 0; i < keys.length; i++) ***REMOVED***
    clone[keys[i]] = options[keys[i]];
  ***REMOVED***

  return clone;
***REMOVED***

// Exports symbols
exports.BulkWriteResult = BulkWriteResult;
exports.WriteError = WriteError;
exports.Batch = Batch;
exports.LegacyOp = LegacyOp;
exports.mergeBatchResults = mergeBatchResults;
exports.cloneOptions = cloneOptions;
exports.writeConcern = writeConcern;
exports.INVALID_BSON_ERROR = INVALID_BSON_ERROR;
exports.WRITE_CONCERN_ERROR = WRITE_CONCERN_ERROR;
exports.MULTIPLE_ERROR = MULTIPLE_ERROR;
exports.UNKNOWN_ERROR = UNKNOWN_ERROR;
exports.INSERT = INSERT;
exports.UPDATE = UPDATE;
exports.REMOVE = REMOVE;

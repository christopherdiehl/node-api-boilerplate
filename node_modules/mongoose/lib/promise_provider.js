/*!
 * Module dependencies.
 */

var MPromise = require('./promise');

/**
 * Helper for multiplexing promise implementations
 *
 * @api private
 */

var Promise = ***REMOVED***
  _promise: MPromise
***REMOVED***;

/**
 * Get the current promise constructor
 *
 * @api private
 */
Promise.get = function() ***REMOVED***
  return Promise._promise;
***REMOVED***;

/**
 * Set the current promise constructor
 *
 * @api private
 */

Promise.set = function(lib) ***REMOVED***
  if (lib === MPromise) ***REMOVED***
    return Promise.reset();
  ***REMOVED***
  Promise._promise = require('./ES6Promise');
  Promise._promise.use(lib);
  require('mquery').Promise = Promise._promise.ES6;
***REMOVED***;

/**
 * Resets to using mpromise
 *
 * @api private
 */

Promise.reset = function() ***REMOVED***
  Promise._promise = MPromise;
***REMOVED***;

module.exports = Promise;

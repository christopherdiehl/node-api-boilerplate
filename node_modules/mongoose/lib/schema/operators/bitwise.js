/*!
 * Module requirements.
 */

var CastError = require('../../error/cast');

/*!
 * ignore
 */

function handleBitwiseOperator(val) ***REMOVED***
  var _this = this;
  if (Array.isArray(val)) ***REMOVED***
    return val.map(function(v) ***REMOVED***
      return _castNumber(_this.path, v);
    ***REMOVED***);
  ***REMOVED*** else if (Buffer.isBuffer(val)) ***REMOVED***
    return val;
  ***REMOVED***
  // Assume trying to cast to number
  return _castNumber(_this.path, val);
***REMOVED***

/*!
 * ignore
 */

function _castNumber(path, num) ***REMOVED***
  var v = Number(num);
  if (isNaN(v)) ***REMOVED***
    throw new CastError('number', num, path);
  ***REMOVED***
  return v;
***REMOVED***

module.exports = handleBitwiseOperator;

/*!
 * Module dependencies.
 */

var SchemaType = require('../schematype');
var utils = require('../utils');

/**
 * Mixed SchemaType constructor.
 *
 * @param ***REMOVED***String***REMOVED*** path
 * @param ***REMOVED***Object***REMOVED*** options
 * @inherits SchemaType
 * @api public
 */

function Mixed(path, options) ***REMOVED***
  if (options && options.default) ***REMOVED***
    var def = options.default;
    if (Array.isArray(def) && def.length === 0) ***REMOVED***
      // make sure empty array defaults are handled
      options.default = Array;
    ***REMOVED*** else if (!options.shared && utils.isObject(def) && Object.keys(def).length === 0) ***REMOVED***
      // prevent odd "shared" objects between documents
      options.default = function() ***REMOVED***
        return ***REMOVED******REMOVED***;
      ***REMOVED***;
    ***REMOVED***
  ***REMOVED***

  SchemaType.call(this, path, options, 'Mixed');
***REMOVED***

/**
 * This schema type's name, to defend against minifiers that mangle
 * function names.
 *
 * @api public
 */
Mixed.schemaName = 'Mixed';

/*!
 * Inherits from SchemaType.
 */
Mixed.prototype = Object.create(SchemaType.prototype);
Mixed.prototype.constructor = Mixed;

/**
 * Casts `val` for Mixed.
 *
 * _this is a no-op_
 *
 * @param ***REMOVED***Object***REMOVED*** value to cast
 * @api private
 */

Mixed.prototype.cast = function(val) ***REMOVED***
  return val;
***REMOVED***;

/**
 * Casts contents for queries.
 *
 * @param ***REMOVED***String***REMOVED*** $cond
 * @param ***REMOVED***any***REMOVED*** [val]
 * @api private
 */

Mixed.prototype.castForQuery = function($cond, val) ***REMOVED***
  if (arguments.length === 2) ***REMOVED***
    return val;
  ***REMOVED***
  return $cond;
***REMOVED***;

/*!
 * Module exports.
 */

module.exports = Mixed;

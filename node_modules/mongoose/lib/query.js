/*!
 * Module dependencies.
 */

var PromiseProvider = require('./promise_provider');
var QueryCursor = require('./querycursor');
var QueryStream = require('./querystream');
var StrictModeError = require('./error/strict');
var cast = require('./cast');
var helpers = require('./queryhelpers');
var mquery = require('mquery');
var readPref = require('./drivers').ReadPreference;
var setDefaultsOnInsert = require('./services/setDefaultsOnInsert');
var updateValidators = require('./services/updateValidators');
var util = require('util');
var utils = require('./utils');

/**
 * Query constructor used for building queries.
 *
 * ####Example:
 *
 *     var query = new Query();
 *     query.setOptions(***REMOVED*** lean : true ***REMOVED***);
 *     query.collection(model.collection);
 *     query.where('age').gte(21).exec(callback);
 *
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @param ***REMOVED***Object***REMOVED*** [model]
 * @param ***REMOVED***Object***REMOVED*** [conditions]
 * @param ***REMOVED***Object***REMOVED*** [collection] Mongoose collection
 * @api private
 */

function Query(conditions, options, model, collection) ***REMOVED***
  // this stuff is for dealing with custom queries created by #toConstructor
  if (!this._mongooseOptions) ***REMOVED***
    this._mongooseOptions = ***REMOVED******REMOVED***;
  ***REMOVED***

  // this is the case where we have a CustomQuery, we need to check if we got
  // options passed in, and if we did, merge them in
  if (options) ***REMOVED***
    var keys = Object.keys(options);
    for (var i = 0; i < keys.length; ++i) ***REMOVED***
      var k = keys[i];
      this._mongooseOptions[k] = options[k];
    ***REMOVED***
  ***REMOVED***

  if (collection) ***REMOVED***
    this.mongooseCollection = collection;
  ***REMOVED***

  if (model) ***REMOVED***
    this.model = model;
    this.schema = model.schema;
  ***REMOVED***

  // this is needed because map reduce returns a model that can be queried, but
  // all of the queries on said model should be lean
  if (this.model && this.model._mapreduce) ***REMOVED***
    this.lean();
  ***REMOVED***

  // inherit mquery
  mquery.call(this, this.mongooseCollection, options);

  if (conditions) ***REMOVED***
    this.find(conditions);
  ***REMOVED***

  if (this.schema) ***REMOVED***
    var kareemOptions = ***REMOVED*** useErrorHandlers: true ***REMOVED***;
    this._count = this.model.hooks.createWrapper('count',
        Query.prototype._count, this, kareemOptions);
    this._execUpdate = this.model.hooks.createWrapper('update',
        Query.prototype._execUpdate, this, kareemOptions);
    this._find = this.model.hooks.createWrapper('find',
        Query.prototype._find, this, kareemOptions);
    this._findOne = this.model.hooks.createWrapper('findOne',
        Query.prototype._findOne, this, kareemOptions);
    this._findOneAndRemove = this.model.hooks.createWrapper('findOneAndRemove',
        Query.prototype._findOneAndRemove, this, kareemOptions);
    this._findOneAndUpdate = this.model.hooks.createWrapper('findOneAndUpdate',
        Query.prototype._findOneAndUpdate, this, kareemOptions);
  ***REMOVED***
***REMOVED***

/*!
 * inherit mquery
 */

Query.prototype = new mquery;
Query.prototype.constructor = Query;
Query.base = mquery.prototype;

/**
 * Flag to opt out of using `$geoWithin`.
 *
 *     mongoose.Query.use$geoWithin = false;
 *
 * MongoDB 2.4 deprecated the use of `$within`, replacing it with `$geoWithin`. Mongoose uses `$geoWithin` by default (which is 100% backward compatible with $within). If you are running an older version of MongoDB, set this flag to `false` so your `within()` queries continue to work.
 *
 * @see http://docs.mongodb.org/manual/reference/operator/geoWithin/
 * @default true
 * @property use$geoWithin
 * @memberOf Query
 * @receiver Query
 * @api public
 */

Query.use$geoWithin = mquery.use$geoWithin;

/**
 * Converts this query to a customized, reusable query constructor with all arguments and options retained.
 *
 * ####Example
 *
 *     // Create a query for adventure movies and read from the primary
 *     // node in the replica-set unless it is down, in which case we'll
 *     // read from a secondary node.
 *     var query = Movie.find(***REMOVED*** tags: 'adventure' ***REMOVED***).read('primaryPreferred');
 *
 *     // create a custom Query constructor based off these settings
 *     var Adventure = query.toConstructor();
 *
 *     // Adventure is now a subclass of mongoose.Query and works the same way but with the
 *     // default query parameters and options set.
 *     Adventure().exec(callback)
 *
 *     // further narrow down our query results while still using the previous settings
 *     Adventure().where(***REMOVED*** name: /^Life/ ***REMOVED***).exec(callback);
 *
 *     // since Adventure is a stand-alone constructor we can also add our own
 *     // helper methods and getters without impacting global queries
 *     Adventure.prototype.startsWith = function (prefix) ***REMOVED***
 *       this.where(***REMOVED*** name: new RegExp('^' + prefix) ***REMOVED***)
 *       return this;
 *     ***REMOVED***
 *     Object.defineProperty(Adventure.prototype, 'highlyRated', ***REMOVED***
 *       get: function () ***REMOVED***
 *         this.where(***REMOVED*** rating: ***REMOVED*** $gt: 4.5 ***REMOVED******REMOVED***);
 *         return this;
 *       ***REMOVED***
 *     ***REMOVED***)
 *     Adventure().highlyRated.startsWith('Life').exec(callback)
 *
 * New in 3.7.3
 *
 * @return ***REMOVED***Query***REMOVED*** subclass-of-Query
 * @api public
 */

Query.prototype.toConstructor = function toConstructor() ***REMOVED***
  var model = this.model;
  var coll = this.mongooseCollection;

  var CustomQuery = function(criteria, options) ***REMOVED***
    if (!(this instanceof CustomQuery)) ***REMOVED***
      return new CustomQuery(criteria, options);
    ***REMOVED***
    this._mongooseOptions = utils.clone(p._mongooseOptions);
    Query.call(this, criteria, options || null, model, coll);
  ***REMOVED***;

  util.inherits(CustomQuery, Query);

  // set inherited defaults
  var p = CustomQuery.prototype;

  p.options = ***REMOVED******REMOVED***;

  p.setOptions(this.options);

  p.op = this.op;
  p._conditions = utils.clone(this._conditions, ***REMOVED*** retainKeyOrder: true ***REMOVED***);
  p._fields = utils.clone(this._fields);
  p._update = utils.clone(this._update);
  p._path = this._path;
  p._distinct = this._distinct;
  p._collection = this._collection;
  p._mongooseOptions = this._mongooseOptions;

  return CustomQuery;
***REMOVED***;

/**
 * Specifies a javascript function or expression to pass to MongoDBs query system.
 *
 * ####Example
 *
 *     query.$where('this.comments.length === 10 || this.name.length === 5')
 *
 *     // or
 *
 *     query.$where(function () ***REMOVED***
 *       return this.comments.length === 10 || this.name.length === 5;
 *     ***REMOVED***)
 *
 * ####NOTE:
 *
 * Only use `$where` when you have a condition that cannot be met using other MongoDB operators like `$lt`.
 * **Be sure to read about all of [its caveats](http://docs.mongodb.org/manual/reference/operator/where/) before using.**
 *
 * @see $where http://docs.mongodb.org/manual/reference/operator/where/
 * @method $where
 * @param ***REMOVED***String|Function***REMOVED*** js javascript string or function
 * @return ***REMOVED***Query***REMOVED*** this
 * @memberOf Query
 * @method $where
 * @api public
 */

/**
 * Specifies a `path` for use with chaining.
 *
 * ####Example
 *
 *     // instead of writing:
 *     User.find(***REMOVED***age: ***REMOVED***$gte: 21, $lte: 65***REMOVED******REMOVED***, callback);
 *
 *     // we can instead write:
 *     User.where('age').gte(21).lte(65);
 *
 *     // passing query conditions is permitted
 *     User.find().where(***REMOVED*** name: 'vonderful' ***REMOVED***)
 *
 *     // chaining
 *     User
 *     .where('age').gte(21).lte(65)
 *     .where('name', /^vonderful/i)
 *     .where('friends').slice(10)
 *     .exec(callback)
 *
 * @method where
 * @memberOf Query
 * @param ***REMOVED***String|Object***REMOVED*** [path]
 * @param ***REMOVED***any***REMOVED*** [val]
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Specifies the complementary comparison value for paths specified with `where()`
 *
 * ####Example
 *
 *     User.where('age').equals(49);
 *
 *     // is the same as
 *
 *     User.where('age', 49);
 *
 * @method equals
 * @memberOf Query
 * @param ***REMOVED***Object***REMOVED*** val
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Specifies arguments for an `$or` condition.
 *
 * ####Example
 *
 *     query.or([***REMOVED*** color: 'red' ***REMOVED***, ***REMOVED*** status: 'emergency' ***REMOVED***])
 *
 * @see $or http://docs.mongodb.org/manual/reference/operator/or/
 * @method or
 * @memberOf Query
 * @param ***REMOVED***Array***REMOVED*** array array of conditions
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Specifies arguments for a `$nor` condition.
 *
 * ####Example
 *
 *     query.nor([***REMOVED*** color: 'green' ***REMOVED***, ***REMOVED*** status: 'ok' ***REMOVED***])
 *
 * @see $nor http://docs.mongodb.org/manual/reference/operator/nor/
 * @method nor
 * @memberOf Query
 * @param ***REMOVED***Array***REMOVED*** array array of conditions
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Specifies arguments for a `$and` condition.
 *
 * ####Example
 *
 *     query.and([***REMOVED*** color: 'green' ***REMOVED***, ***REMOVED*** status: 'ok' ***REMOVED***])
 *
 * @method and
 * @memberOf Query
 * @see $and http://docs.mongodb.org/manual/reference/operator/and/
 * @param ***REMOVED***Array***REMOVED*** array array of conditions
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Specifies a $gt query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * ####Example
 *
 *     Thing.find().where('age').gt(21)
 *
 *     // or
 *     Thing.find().gt('age', 21)
 *
 * @method gt
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @see $gt http://docs.mongodb.org/manual/reference/operator/gt/
 * @api public
 */

/**
 * Specifies a $gte query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @method gte
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @see $gte http://docs.mongodb.org/manual/reference/operator/gte/
 * @api public
 */

/**
 * Specifies a $lt query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @method lt
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @see $lt http://docs.mongodb.org/manual/reference/operator/lt/
 * @api public
 */

/**
 * Specifies a $lte query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @method lte
 * @see $lte http://docs.mongodb.org/manual/reference/operator/lte/
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies a $ne query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @see $ne http://docs.mongodb.org/manual/reference/operator/ne/
 * @method ne
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies an $in query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @see $in http://docs.mongodb.org/manual/reference/operator/in/
 * @method in
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies an $nin query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @see $nin http://docs.mongodb.org/manual/reference/operator/nin/
 * @method nin
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies an $all query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @see $all http://docs.mongodb.org/manual/reference/operator/all/
 * @method all
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies a $size query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * ####Example
 *
 *     MyModel.where('tags').size(0).exec(function (err, docs) ***REMOVED***
 *       if (err) return handleError(err);
 *
 *       assert(Array.isArray(docs));
 *       console.log('documents with 0 tags', docs);
 *     ***REMOVED***)
 *
 * @see $size http://docs.mongodb.org/manual/reference/operator/size/
 * @method size
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies a $regex query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @see $regex http://docs.mongodb.org/manual/reference/operator/regex/
 * @method regex
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies a $maxDistance query condition.
 *
 * When called with one argument, the most recent path passed to `where()` is used.
 *
 * @see $maxDistance http://docs.mongodb.org/manual/reference/operator/maxDistance/
 * @method maxDistance
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies a `$mod` condition
 *
 * @method mod
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $mod http://docs.mongodb.org/manual/reference/operator/mod/
 * @api public
 */

/**
 * Specifies an `$exists` condition
 *
 * ####Example
 *
 *     // ***REMOVED*** name: ***REMOVED*** $exists: true ***REMOVED******REMOVED***
 *     Thing.where('name').exists()
 *     Thing.where('name').exists(true)
 *     Thing.find().exists('name')
 *
 *     // ***REMOVED*** name: ***REMOVED*** $exists: false ***REMOVED******REMOVED***
 *     Thing.where('name').exists(false);
 *     Thing.find().exists('name', false);
 *
 * @method exists
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $exists http://docs.mongodb.org/manual/reference/operator/exists/
 * @api public
 */

/**
 * Specifies an `$elemMatch` condition
 *
 * ####Example
 *
 *     query.elemMatch('comment', ***REMOVED*** author: 'autobot', votes: ***REMOVED***$gte: 5***REMOVED******REMOVED***)
 *
 *     query.where('comment').elemMatch(***REMOVED*** author: 'autobot', votes: ***REMOVED***$gte: 5***REMOVED******REMOVED***)
 *
 *     query.elemMatch('comment', function (elem) ***REMOVED***
 *       elem.where('author').equals('autobot');
 *       elem.where('votes').gte(5);
 *     ***REMOVED***)
 *
 *     query.where('comment').elemMatch(function (elem) ***REMOVED***
 *       elem.where(***REMOVED*** author: 'autobot' ***REMOVED***);
 *       elem.where('votes').gte(5);
 *     ***REMOVED***)
 *
 * @method elemMatch
 * @memberOf Query
 * @param ***REMOVED***String|Object|Function***REMOVED*** path
 * @param ***REMOVED***Object|Function***REMOVED*** criteria
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $elemMatch http://docs.mongodb.org/manual/reference/operator/elemMatch/
 * @api public
 */

/**
 * Defines a `$within` or `$geoWithin` argument for geo-spatial queries.
 *
 * ####Example
 *
 *     query.where(path).within().box()
 *     query.where(path).within().circle()
 *     query.where(path).within().geometry()
 *
 *     query.where('loc').within(***REMOVED*** center: [50,50], radius: 10, unique: true, spherical: true ***REMOVED***);
 *     query.where('loc').within(***REMOVED*** box: [[40.73, -73.9], [40.7, -73.988]] ***REMOVED***);
 *     query.where('loc').within(***REMOVED*** polygon: [[],[],[],[]] ***REMOVED***);
 *
 *     query.where('loc').within([], [], []) // polygon
 *     query.where('loc').within([], []) // box
 *     query.where('loc').within(***REMOVED*** type: 'LineString', coordinates: [...] ***REMOVED***); // geometry
 *
 * **MUST** be used after `where()`.
 *
 * ####NOTE:
 *
 * As of Mongoose 3.7, `$geoWithin` is always used for queries. To change this behavior, see [Query.use$geoWithin](#query_Query-use%2524geoWithin).
 *
 * ####NOTE:
 *
 * In Mongoose 3.7, `within` changed from a getter to a function. If you need the old syntax, use [this](https://github.com/ebensing/mongoose-within).
 *
 * @method within
 * @see $polygon http://docs.mongodb.org/manual/reference/operator/polygon/
 * @see $box http://docs.mongodb.org/manual/reference/operator/box/
 * @see $geometry http://docs.mongodb.org/manual/reference/operator/geometry/
 * @see $center http://docs.mongodb.org/manual/reference/operator/center/
 * @see $centerSphere http://docs.mongodb.org/manual/reference/operator/centerSphere/
 * @memberOf Query
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Specifies a $slice projection for an array.
 *
 * ####Example
 *
 *     query.slice('comments', 5)
 *     query.slice('comments', -5)
 *     query.slice('comments', [10, 5])
 *     query.where('comments').slice(5)
 *     query.where('comments').slice([-10, 5])
 *
 * @method slice
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Number***REMOVED*** val number/range of elements to slice
 * @return ***REMOVED***Query***REMOVED*** this
 * @see mongodb http://www.mongodb.org/display/DOCS/Retrieving+a+Subset+of+Fields#RetrievingaSubsetofFields-RetrievingaSubrangeofArrayElements
 * @see $slice http://docs.mongodb.org/manual/reference/projection/slice/#prj._S_slice
 * @api public
 */

/**
 * Specifies the maximum number of documents the query will return.
 *
 * ####Example
 *
 *     query.limit(20)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method limit
 * @memberOf Query
 * @param ***REMOVED***Number***REMOVED*** val
 * @api public
 */

/**
 * Specifies the number of documents to skip.
 *
 * ####Example
 *
 *     query.skip(100).limit(20)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method skip
 * @memberOf Query
 * @param ***REMOVED***Number***REMOVED*** val
 * @see cursor.skip http://docs.mongodb.org/manual/reference/method/cursor.skip/
 * @api public
 */

/**
 * Specifies the maxScan option.
 *
 * ####Example
 *
 *     query.maxScan(100)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method maxScan
 * @memberOf Query
 * @param ***REMOVED***Number***REMOVED*** val
 * @see maxScan http://docs.mongodb.org/manual/reference/operator/maxScan/
 * @api public
 */

/**
 * Specifies the batchSize option.
 *
 * ####Example
 *
 *     query.batchSize(100)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method batchSize
 * @memberOf Query
 * @param ***REMOVED***Number***REMOVED*** val
 * @see batchSize http://docs.mongodb.org/manual/reference/method/cursor.batchSize/
 * @api public
 */

/**
 * Specifies the `comment` option.
 *
 * ####Example
 *
 *     query.comment('login query')
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method comment
 * @memberOf Query
 * @param ***REMOVED***Number***REMOVED*** val
 * @see comment http://docs.mongodb.org/manual/reference/operator/comment/
 * @api public
 */

/**
 * Specifies this query as a `snapshot` query.
 *
 * ####Example
 *
 *     query.snapshot() // true
 *     query.snapshot(true)
 *     query.snapshot(false)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method snapshot
 * @memberOf Query
 * @see snapshot http://docs.mongodb.org/manual/reference/operator/snapshot/
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Sets query hints.
 *
 * ####Example
 *
 *     query.hint(***REMOVED*** indexA: 1, indexB: -1***REMOVED***)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @method hint
 * @memberOf Query
 * @param ***REMOVED***Object***REMOVED*** val a hint object
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $hint http://docs.mongodb.org/manual/reference/operator/hint/
 * @api public
 */

/**
 * Specifies which document fields to include or exclude (also known as the query "projection")
 *
 * When using string syntax, prefixing a path with `-` will flag that path as excluded. When a path does not have the `-` prefix, it is included. Lastly, if a path is prefixed with `+`, it forces inclusion of the path, which is useful for paths excluded at the [schema level](/docs/api.html#schematype_SchemaType-select).
 *
 * ####Example
 *
 *     // include a and b, exclude other fields
 *     query.select('a b');
 *
 *     // exclude c and d, include other fields
 *     query.select('-c -d');
 *
 *     // or you may use object notation, useful when
 *     // you have keys already prefixed with a "-"
 *     query.select(***REMOVED*** a: 1, b: 1 ***REMOVED***);
 *     query.select(***REMOVED*** c: 0, d: 0 ***REMOVED***);
 *
 *     // force inclusion of field excluded at schema level
 *     query.select('+path')
 *
 * ####NOTE:
 *
 * Cannot be used with `distinct()`.
 *
 * _v2 had slightly different syntax such as allowing arrays of field names. This support was removed in v3._
 *
 * @method select
 * @memberOf Query
 * @param ***REMOVED***Object|String***REMOVED*** arg
 * @return ***REMOVED***Query***REMOVED*** this
 * @see SchemaType
 * @api public
 */

/**
 * _DEPRECATED_ Sets the slaveOk option.
 *
 * **Deprecated** in MongoDB 2.2 in favor of [read preferences](#query_Query-read).
 *
 * ####Example:
 *
 *     query.slaveOk() // true
 *     query.slaveOk(true)
 *     query.slaveOk(false)
 *
 * @method slaveOk
 * @memberOf Query
 * @deprecated use read() preferences instead if on mongodb >= 2.2
 * @param ***REMOVED***Boolean***REMOVED*** v defaults to true
 * @see mongodb http://docs.mongodb.org/manual/applications/replication/#read-preference
 * @see slaveOk http://docs.mongodb.org/manual/reference/method/rs.slaveOk/
 * @see read() #query_Query-read
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/**
 * Determines the MongoDB nodes from which to read.
 *
 * ####Preferences:
 *
 *     primary - (default) Read from primary only. Operations will produce an error if primary is unavailable. Cannot be combined with tags.
 *     secondary            Read from secondary if available, otherwise error.
 *     primaryPreferred     Read from primary if available, otherwise a secondary.
 *     secondaryPreferred   Read from a secondary if available, otherwise read from the primary.
 *     nearest              All operations read from among the nearest candidates, but unlike other modes, this option will include both the primary and all secondaries in the random selection.
 *
 * Aliases
 *
 *     p   primary
 *     pp  primaryPreferred
 *     s   secondary
 *     sp  secondaryPreferred
 *     n   nearest
 *
 * ####Example:
 *
 *     new Query().read('primary')
 *     new Query().read('p')  // same as primary
 *
 *     new Query().read('primaryPreferred')
 *     new Query().read('pp') // same as primaryPreferred
 *
 *     new Query().read('secondary')
 *     new Query().read('s')  // same as secondary
 *
 *     new Query().read('secondaryPreferred')
 *     new Query().read('sp') // same as secondaryPreferred
 *
 *     new Query().read('nearest')
 *     new Query().read('n')  // same as nearest
 *
 *     // read from secondaries with matching tags
 *     new Query().read('s', [***REMOVED*** dc:'sf', s: 1 ***REMOVED***,***REMOVED*** dc:'ma', s: 2 ***REMOVED***])
 *
 * Read more about how to use read preferrences [here](http://docs.mongodb.org/manual/applications/replication/#read-preference) and [here](http://mongodb.github.com/node-mongodb-native/driver-articles/anintroductionto1_1and2_2.html#read-preferences).
 *
 * @method read
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** pref one of the listed preference options or aliases
 * @param ***REMOVED***Array***REMOVED*** [tags] optional tags for this query
 * @see mongodb http://docs.mongodb.org/manual/applications/replication/#read-preference
 * @see driver http://mongodb.github.com/node-mongodb-native/driver-articles/anintroductionto1_1and2_2.html#read-preferences
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

Query.prototype.read = function read(pref, tags) ***REMOVED***
  // first cast into a ReadPreference object to support tags
  var read = readPref.call(readPref, pref, tags);
  return Query.base.read.call(this, read);
***REMOVED***;

/**
 * Merges another Query or conditions object into this one.
 *
 * When a Query is passed, conditions, field selection and options are merged.
 *
 * New in 3.7.0
 *
 * @method merge
 * @memberOf Query
 * @param ***REMOVED***Query|Object***REMOVED*** source
 * @return ***REMOVED***Query***REMOVED*** this
 */

/**
 * Sets query options.
 *
 * ####Options:
 *
 * - [tailable](http://www.mongodb.org/display/DOCS/Tailable+Cursors) *
 * - [sort](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%7B%7Bsort(\)%7D%7D) *
 * - [limit](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%7B%7Blimit%28%29%7D%7D) *
 * - [skip](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%7B%7Bskip%28%29%7D%7D) *
 * - [maxscan](https://docs.mongodb.org/v3.2/reference/operator/meta/maxScan/#metaOp._S_maxScan) *
 * - [batchSize](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%7B%7BbatchSize%28%29%7D%7D) *
 * - [comment](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%24comment) *
 * - [snapshot](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%7B%7Bsnapshot%28%29%7D%7D) *
 * - [hint](http://www.mongodb.org/display/DOCS/Advanced+Queries#AdvancedQueries-%24hint) *
 * - [readPreference](http://docs.mongodb.org/manual/applications/replication/#read-preference) **
 * - [lean](./api.html#query_Query-lean) *
 * - [safe](http://www.mongodb.org/display/DOCS/getLastError+Command)
 *
 * _* denotes a query helper method is also available_
 * _** query helper method to set `readPreference` is `read()`_
 *
 * @param ***REMOVED***Object***REMOVED*** options
 * @api public
 */

Query.prototype.setOptions = function(options, overwrite) ***REMOVED***
  // overwrite is only for internal use
  if (overwrite) ***REMOVED***
    // ensure that _mongooseOptions & options are two different objects
    this._mongooseOptions = (options && utils.clone(options)) || ***REMOVED******REMOVED***;
    this.options = options || ***REMOVED******REMOVED***;

    if ('populate' in options) ***REMOVED***
      this.populate(this._mongooseOptions);
    ***REMOVED***
    return this;
  ***REMOVED***

  if (!(options && options.constructor.name === 'Object')) ***REMOVED***
    return this;
  ***REMOVED***

  if (options && Array.isArray(options.populate)) ***REMOVED***
    var populate = options.populate;
    delete options.populate;
    var _numPopulate = populate.length;
    for (var i = 0; i < _numPopulate; ++i) ***REMOVED***
      this.populate(populate[i]);
    ***REMOVED***
  ***REMOVED***

  return Query.base.setOptions.call(this, options);
***REMOVED***;

/**
 * Returns the current query conditions as a JSON object.
 *
 * ####Example:
 *
 *     var query = new Query();
 *     query.find(***REMOVED*** a: 1 ***REMOVED***).where('b').gt(2);
 *     query.getQuery(); // ***REMOVED*** a: 1, b: ***REMOVED*** $gt: 2 ***REMOVED*** ***REMOVED***
 *
 * @return ***REMOVED***Object***REMOVED*** current query conditions
 * @api public
 */

Query.prototype.getQuery = function() ***REMOVED***
  return this._conditions;
***REMOVED***;

/**
 * Returns the current update operations as a JSON object.
 *
 * ####Example:
 *
 *     var query = new Query();
 *     query.update(***REMOVED******REMOVED***, ***REMOVED*** $set: ***REMOVED*** a: 5 ***REMOVED*** ***REMOVED***);
 *     query.getUpdate(); // ***REMOVED*** $set: ***REMOVED*** a: 5 ***REMOVED*** ***REMOVED***
 *
 * @return ***REMOVED***Object***REMOVED*** current update operations
 * @api public
 */

Query.prototype.getUpdate = function() ***REMOVED***
  return this._update;
***REMOVED***;

/**
 * Returns fields selection for this query.
 *
 * @method _fieldsForExec
 * @return ***REMOVED***Object***REMOVED***
 * @api private
 * @receiver Query
 */

/**
 * Return an update document with corrected $set operations.
 *
 * @method _updateForExec
 * @api private
 * @receiver Query
 */

Query.prototype._updateForExec = function() ***REMOVED***
  var update = utils.clone(this._update, ***REMOVED***
    retainKeyOrder: true,
    transform: false,
    depopulate: true
  ***REMOVED***);
  var ops = Object.keys(update);
  var i = ops.length;
  var ret = ***REMOVED******REMOVED***;

  while (i--) ***REMOVED***
    var op = ops[i];

    if (this.options.overwrite) ***REMOVED***
      ret[op] = update[op];
      continue;
    ***REMOVED***

    if ('$' !== op[0]) ***REMOVED***
      // fix up $set sugar
      if (!ret.$set) ***REMOVED***
        if (update.$set) ***REMOVED***
          ret.$set = update.$set;
        ***REMOVED*** else ***REMOVED***
          ret.$set = ***REMOVED******REMOVED***;
        ***REMOVED***
      ***REMOVED***
      ret.$set[op] = update[op];
      ops.splice(i, 1);
      if (!~ops.indexOf('$set')) ops.push('$set');
    ***REMOVED*** else if ('$set' === op) ***REMOVED***
      if (!ret.$set) ***REMOVED***
        ret[op] = update[op];
      ***REMOVED***
    ***REMOVED*** else ***REMOVED***
      ret[op] = update[op];
    ***REMOVED***
  ***REMOVED***

  return ret;
***REMOVED***;

/**
 * Makes sure _path is set.
 *
 * @method _ensurePath
 * @param ***REMOVED***String***REMOVED*** method
 * @api private
 * @receiver Query
 */

/**
 * Determines if `conds` can be merged using `mquery().merge()`
 *
 * @method canMerge
 * @memberOf Query
 * @param ***REMOVED***Object***REMOVED*** conds
 * @return ***REMOVED***Boolean***REMOVED***
 * @api private
 */

/**
 * Returns default options for this query.
 *
 * @param ***REMOVED***Model***REMOVED*** model
 * @api private
 */

Query.prototype._optionsForExec = function(model) ***REMOVED***
  var options = Query.base._optionsForExec.call(this);

  delete options.populate;
  delete options.retainKeyOrder;
  model = model || this.model;

  if (!model) ***REMOVED***
    return options;
  ***REMOVED***

  if (!('safe' in options) && model.schema.options.safe) ***REMOVED***
    options.safe = model.schema.options.safe;
  ***REMOVED***

  if (!('readPreference' in options) && model.schema.options.read) ***REMOVED***
    options.readPreference = model.schema.options.read;
  ***REMOVED***

  return options;
***REMOVED***;

/**
 * Sets the lean option.
 *
 * Documents returned from queries with the `lean` option enabled are plain javascript objects, not [MongooseDocuments](#document-js). They have no `save` method, getters/setters or other Mongoose magic applied.
 *
 * ####Example:
 *
 *     new Query().lean() // true
 *     new Query().lean(true)
 *     new Query().lean(false)
 *
 *     Model.find().lean().exec(function (err, docs) ***REMOVED***
 *       docs[0] instanceof mongoose.Document // false
 *     ***REMOVED***);
 *
 * This is a [great](https://groups.google.com/forum/#!topic/mongoose-orm/u2_DzDydcnA/discussion) option in high-performance read-only scenarios, especially when combined with [stream](#query_Query-stream).
 *
 * @param ***REMOVED***Boolean***REMOVED*** bool defaults to true
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

Query.prototype.lean = function(v) ***REMOVED***
  this._mongooseOptions.lean = arguments.length ? !!v : true;
  return this;
***REMOVED***;

/**
 * Thunk around find()
 *
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @api private
 */
Query.prototype._find = function(callback) ***REMOVED***
  if (this._castError) ***REMOVED***
    callback(this._castError);
    return this;
  ***REMOVED***

  this._applyPaths();
  this._fields = this._castFields(this._fields);

  var fields = this._fieldsForExec();
  var options = this._mongooseOptions;
  var _this = this;

  var cb = function(err, docs) ***REMOVED***
    if (err) ***REMOVED***
      return callback(err);
    ***REMOVED***

    if (docs.length === 0) ***REMOVED***
      return callback(null, docs);
    ***REMOVED***

    if (!options.populate) ***REMOVED***
      return options.lean === true
          ? callback(null, docs)
          : completeMany(_this.model, docs, fields, null, callback);
    ***REMOVED***

    var pop = helpers.preparePopulationOptionsMQ(_this, options);
    pop.__noPromise = true;
    _this.model.populate(docs, pop, function(err, docs) ***REMOVED***
      if (err) return callback(err);
      return options.lean === true
          ? callback(null, docs)
          : completeMany(_this.model, docs, fields, pop, callback);
    ***REMOVED***);
  ***REMOVED***;

  return Query.base.find.call(this, ***REMOVED******REMOVED***, cb);
***REMOVED***;

/**
 * Finds documents.
 *
 * When no `callback` is passed, the query is not executed. When the query is executed, the result will be an array of documents.
 *
 * ####Example
 *
 *     query.find(***REMOVED*** name: 'Los Pollos Hermanos' ***REMOVED***).find(callback)
 *
 * @param ***REMOVED***Object***REMOVED*** [criteria] mongodb selector
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

Query.prototype.find = function(conditions, callback) ***REMOVED***
  if (typeof conditions === 'function') ***REMOVED***
    callback = conditions;
    conditions = ***REMOVED******REMOVED***;
  ***REMOVED***

  conditions = utils.toObject(conditions);

  if (mquery.canMerge(conditions)) ***REMOVED***
    this.merge(conditions);
  ***REMOVED***

  prepareDiscriminatorCriteria(this);

  try ***REMOVED***
    this.cast(this.model);
    this._castError = null;
  ***REMOVED*** catch (err) ***REMOVED***
    this._castError = err;
  ***REMOVED***

  // if we don't have a callback, then just return the query object
  if (!callback) ***REMOVED***
    return Query.base.find.call(this);
  ***REMOVED***

  this._find(callback);

  return this;
***REMOVED***;

/**
 * Merges another Query or conditions object into this one.
 *
 * When a Query is passed, conditions, field selection and options are merged.
 *
 * @param ***REMOVED***Query|Object***REMOVED*** source
 * @return ***REMOVED***Query***REMOVED*** this
 */

Query.prototype.merge = function(source) ***REMOVED***
  if (!source) ***REMOVED***
    return this;
  ***REMOVED***

  var opts = ***REMOVED*** retainKeyOrder: this.options.retainKeyOrder, overwrite: true ***REMOVED***;

  if (source instanceof Query) ***REMOVED***
    // if source has a feature, apply it to ourselves

    if (source._conditions) ***REMOVED***
      utils.merge(this._conditions, source._conditions, opts);
    ***REMOVED***

    if (source._fields) ***REMOVED***
      this._fields || (this._fields = ***REMOVED******REMOVED***);
      utils.merge(this._fields, source._fields, opts);
    ***REMOVED***

    if (source.options) ***REMOVED***
      this.options || (this.options = ***REMOVED******REMOVED***);
      utils.merge(this.options, source.options, opts);
    ***REMOVED***

    if (source._update) ***REMOVED***
      this._update || (this._update = ***REMOVED******REMOVED***);
      utils.mergeClone(this._update, source._update);
    ***REMOVED***

    if (source._distinct) ***REMOVED***
      this._distinct = source._distinct;
    ***REMOVED***

    return this;
  ***REMOVED***

  // plain object
  utils.merge(this._conditions, source, opts);

  return this;
***REMOVED***;

/*!
 * hydrates many documents
 *
 * @param ***REMOVED***Model***REMOVED*** model
 * @param ***REMOVED***Array***REMOVED*** docs
 * @param ***REMOVED***Object***REMOVED*** fields
 * @param ***REMOVED***Query***REMOVED*** self
 * @param ***REMOVED***Array***REMOVED*** [pop] array of paths used in population
 * @param ***REMOVED***Function***REMOVED*** callback
 */

function completeMany(model, docs, fields, pop, callback) ***REMOVED***
  var arr = [];
  var count = docs.length;
  var len = count;
  var opts = pop ?
  ***REMOVED***populated: pop***REMOVED***
      : undefined;
  function init(err) ***REMOVED***
    if (err) return callback(err);
    --count || callback(null, arr);
  ***REMOVED***
  for (var i = 0; i < len; ++i) ***REMOVED***
    arr[i] = helpers.createModel(model, docs[i], fields);
    arr[i].init(docs[i], opts, init);
  ***REMOVED***
***REMOVED***

/**
 * Thunk around findOne()
 *
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @see findOne http://docs.mongodb.org/manual/reference/method/db.collection.findOne/
 * @api private
 */

Query.prototype._findOne = function(callback) ***REMOVED***
  if (this._castError) ***REMOVED***
    return callback(this._castError);
  ***REMOVED***

  this._applyPaths();
  this._fields = this._castFields(this._fields);

  var options = this._mongooseOptions;
  var projection = this._fieldsForExec();
  var _this = this;

  // don't pass in the conditions because we already merged them in
  Query.base.findOne.call(_this, ***REMOVED******REMOVED***, function(err, doc) ***REMOVED***
    if (err) ***REMOVED***
      return callback(err);
    ***REMOVED***
    if (!doc) ***REMOVED***
      return callback(null, null);
    ***REMOVED***

    if (!options.populate) ***REMOVED***
      return options.lean === true
          ? callback(null, doc)
          : completeOne(_this.model, doc, null, projection, null, callback);
    ***REMOVED***

    var pop = helpers.preparePopulationOptionsMQ(_this, options);
    pop.__noPromise = true;
    _this.model.populate(doc, pop, function(err, doc) ***REMOVED***
      if (err) ***REMOVED***
        return callback(err);
      ***REMOVED***
      return options.lean === true
          ? callback(null, doc)
          : completeOne(_this.model, doc, null, projection, pop, callback);
    ***REMOVED***);
  ***REMOVED***);
***REMOVED***;

/**
 * Declares the query a findOne operation. When executed, the first found document is passed to the callback.
 *
 * Passing a `callback` executes the query. The result of the query is a single document.
 *
 * * *Note:* `conditions` is optional, and if `conditions` is null or undefined,
 * mongoose will send an empty `findOne` command to MongoDB, which will return
 * an arbitrary document. If you're querying by `_id`, use `Model.findById()`
 * instead.
 *
 * ####Example
 *
 *     var query  = Kitten.where(***REMOVED*** color: 'white' ***REMOVED***);
 *     query.findOne(function (err, kitten) ***REMOVED***
 *       if (err) return handleError(err);
 *       if (kitten) ***REMOVED***
 *         // doc may be null if no document matched
 *       ***REMOVED***
 *     ***REMOVED***);
 *
 * @param ***REMOVED***Object|Query***REMOVED*** [criteria] mongodb selector
 * @param ***REMOVED***Object***REMOVED*** [projection] optional fields to return
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see findOne http://docs.mongodb.org/manual/reference/method/db.collection.findOne/
 * @see Query.select #query_Query-select
 * @api public
 */

Query.prototype.findOne = function(conditions, projection, options, callback) ***REMOVED***
  if (typeof conditions === 'function') ***REMOVED***
    callback = conditions;
    conditions = null;
    projection = null;
    options = null;
  ***REMOVED*** else if (typeof projection === 'function') ***REMOVED***
    callback = projection;
    options = null;
    projection = null;
  ***REMOVED*** else if (typeof options === 'function') ***REMOVED***
    callback = options;
    options = null;
  ***REMOVED***

  // make sure we don't send in the whole Document to merge()
  conditions = utils.toObject(conditions);

  this.op = 'findOne';

  if (options) ***REMOVED***
    this.setOptions(options);
  ***REMOVED***

  if (projection) ***REMOVED***
    this.select(projection);
  ***REMOVED***

  if (mquery.canMerge(conditions)) ***REMOVED***
    this.merge(conditions);
  ***REMOVED*** else if (conditions != null) ***REMOVED***
    throw new Error('Invalid argument to findOne(): ' +
      util.inspect(conditions));
  ***REMOVED***

  prepareDiscriminatorCriteria(this);

  try ***REMOVED***
    this.cast(this.model);
    this._castError = null;
  ***REMOVED*** catch (err) ***REMOVED***
    this._castError = err;
  ***REMOVED***

  if (!callback) ***REMOVED***
    // already merged in the conditions, don't need to send them in.
    return Query.base.findOne.call(this);
  ***REMOVED***

  this._findOne(callback);

  return this;
***REMOVED***;

/**
 * Thunk around count()
 *
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @see count http://docs.mongodb.org/manual/reference/method/db.collection.count/
 * @api private
 */

Query.prototype._count = function(callback) ***REMOVED***
  try ***REMOVED***
    this.cast(this.model);
  ***REMOVED*** catch (err) ***REMOVED***
    process.nextTick(function() ***REMOVED***
      callback(err);
    ***REMOVED***);
    return this;
  ***REMOVED***

  var conds = this._conditions;
  var options = this._optionsForExec();

  this._collection.count(conds, options, utils.tick(callback));
***REMOVED***;

/**
 * Specifying this query as a `count` query.
 *
 * Passing a `callback` executes the query.
 *
 * ####Example:
 *
 *     var countQuery = model.where(***REMOVED*** 'color': 'black' ***REMOVED***).count();
 *
 *     query.count(***REMOVED*** color: 'black' ***REMOVED***).count(callback)
 *
 *     query.count(***REMOVED*** color: 'black' ***REMOVED***, callback)
 *
 *     query.where('color', 'black').count(function (err, count) ***REMOVED***
 *       if (err) return handleError(err);
 *       console.log('there are %d kittens', count);
 *     ***REMOVED***)
 *
 * @param ***REMOVED***Object***REMOVED*** [criteria] mongodb selector
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see count http://docs.mongodb.org/manual/reference/method/db.collection.count/
 * @api public
 */

Query.prototype.count = function(conditions, callback) ***REMOVED***
  if (typeof conditions === 'function') ***REMOVED***
    callback = conditions;
    conditions = undefined;
  ***REMOVED***

  if (mquery.canMerge(conditions)) ***REMOVED***
    this.merge(conditions);
  ***REMOVED***

  this.op = 'count';
  if (!callback) ***REMOVED***
    return this;
  ***REMOVED***

  this._count(callback);

  return this;
***REMOVED***;

/**
 * Declares or executes a distict() operation.
 *
 * Passing a `callback` executes the query.
 *
 * ####Example
 *
 *     distinct(field, conditions, callback)
 *     distinct(field, conditions)
 *     distinct(field, callback)
 *     distinct(field)
 *     distinct(callback)
 *     distinct()
 *
 * @param ***REMOVED***String***REMOVED*** [field]
 * @param ***REMOVED***Object|Query***REMOVED*** [criteria]
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see distinct http://docs.mongodb.org/manual/reference/method/db.collection.distinct/
 * @api public
 */

Query.prototype.distinct = function(field, conditions, callback) ***REMOVED***
  if (!callback) ***REMOVED***
    if (typeof conditions === 'function') ***REMOVED***
      callback = conditions;
      conditions = undefined;
    ***REMOVED*** else if (typeof field === 'function') ***REMOVED***
      callback = field;
      field = undefined;
      conditions = undefined;
    ***REMOVED***
  ***REMOVED***

  conditions = utils.toObject(conditions);

  if (mquery.canMerge(conditions)) ***REMOVED***
    this.merge(conditions);
  ***REMOVED***

  try ***REMOVED***
    this.cast(this.model);
  ***REMOVED*** catch (err) ***REMOVED***
    if (!callback) ***REMOVED***
      throw err;
    ***REMOVED***
    callback(err);
    return this;
  ***REMOVED***

  return Query.base.distinct.call(this, ***REMOVED******REMOVED***, field, callback);
***REMOVED***;

/**
 * Sets the sort order
 *
 * If an object is passed, values allowed are `asc`, `desc`, `ascending`, `descending`, `1`, and `-1`.
 *
 * If a string is passed, it must be a space delimited list of path names. The
 * sort order of each path is ascending unless the path name is prefixed with `-`
 * which will be treated as descending.
 *
 * ####Example
 *
 *     // sort by "field" ascending and "test" descending
 *     query.sort(***REMOVED*** field: 'asc', test: -1 ***REMOVED***);
 *
 *     // equivalent
 *     query.sort('field -test');
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @param ***REMOVED***Object|String***REMOVED*** arg
 * @return ***REMOVED***Query***REMOVED*** this
 * @see cursor.sort http://docs.mongodb.org/manual/reference/method/cursor.sort/
 * @api public
 */

Query.prototype.sort = function(arg) ***REMOVED***
  var nArg = ***REMOVED******REMOVED***;

  if (arguments.length > 1) ***REMOVED***
    throw new Error('sort() only takes 1 Argument');
  ***REMOVED***

  if (Array.isArray(arg)) ***REMOVED***
    // time to deal with the terrible syntax
    for (var i = 0; i < arg.length; i++) ***REMOVED***
      if (!Array.isArray(arg[i])) throw new Error('Invalid sort() argument.');
      nArg[arg[i][0]] = arg[i][1];
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    nArg = arg;
  ***REMOVED***

  return Query.base.sort.call(this, nArg);
***REMOVED***;

/**
 * Declare and/or execute this query as a remove() operation.
 *
 * ####Example
 *
 *     Model.remove(***REMOVED*** artist: 'Anne Murray' ***REMOVED***, callback)
 *
 * ####Note
 *
 * The operation is only executed when a callback is passed. To force execution without a callback, you must first call `remove()` and then execute it by using the `exec()` method.
 *
 *     // not executed
 *     var query = Model.find().remove(***REMOVED*** name: 'Anne Murray' ***REMOVED***)
 *
 *     // executed
 *     query.remove(***REMOVED*** name: 'Anne Murray' ***REMOVED***, callback)
 *     query.remove(***REMOVED*** name: 'Anne Murray' ***REMOVED***).remove(callback)
 *
 *     // executed without a callback
 *     query.exec()
 *
 *     // summary
 *     query.remove(conds, fn); // executes
 *     query.remove(conds)
 *     query.remove(fn) // executes
 *     query.remove()
 *
 * @param ***REMOVED***Object|Query***REMOVED*** [criteria] mongodb selector
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see remove http://docs.mongodb.org/manual/reference/method/db.collection.remove/
 * @api public
 */

Query.prototype.remove = function(cond, callback) ***REMOVED***
  if (typeof cond === 'function') ***REMOVED***
    callback = cond;
    cond = null;
  ***REMOVED***

  var cb = typeof callback === 'function';

  try ***REMOVED***
    this.cast(this.model);
  ***REMOVED*** catch (err) ***REMOVED***
    if (cb) return process.nextTick(callback.bind(null, err));
    return this;
  ***REMOVED***

  return Query.base.remove.call(this, cond, callback);
***REMOVED***;

/*!
 * hydrates a document
 *
 * @param ***REMOVED***Model***REMOVED*** model
 * @param ***REMOVED***Document***REMOVED*** doc
 * @param ***REMOVED***Object***REMOVED*** res 3rd parameter to callback
 * @param ***REMOVED***Object***REMOVED*** fields
 * @param ***REMOVED***Query***REMOVED*** self
 * @param ***REMOVED***Array***REMOVED*** [pop] array of paths used in population
 * @param ***REMOVED***Function***REMOVED*** callback
 */

function completeOne(model, doc, res, fields, pop, callback) ***REMOVED***
  var opts = pop ?
  ***REMOVED***populated: pop***REMOVED***
      : undefined;

  var casted = helpers.createModel(model, doc, fields);
  casted.init(doc, opts, function(err) ***REMOVED***
    if (err) ***REMOVED***
      return callback(err);
    ***REMOVED***
    if (res) ***REMOVED***
      return callback(null, casted, decorateResult(res));
    ***REMOVED***
    callback(null, casted);
  ***REMOVED***);
***REMOVED***

/*!
 * If the model is a discriminator type and not root, then add the key & value to the criteria.
 */

function prepareDiscriminatorCriteria(query) ***REMOVED***
  if (!query || !query.model || !query.model.schema) ***REMOVED***
    return;
  ***REMOVED***

  var schema = query.model.schema;

  if (schema && schema.discriminatorMapping && !schema.discriminatorMapping.isRoot) ***REMOVED***
    query._conditions[schema.discriminatorMapping.key] = schema.discriminatorMapping.value;
  ***REMOVED***
***REMOVED***

/**
 * Issues a mongodb [findAndModify](http://www.mongodb.org/display/DOCS/findAndModify+Command) update command.
 *
 * Finds a matching document, updates it according to the `update` arg, passing any `options`, and returns the found document (if any) to the callback. The query executes immediately if `callback` is passed.
 *
 * ####Available options
 *
 * - `new`: bool - if true, return the modified document rather than the original. defaults to false (changed in 4.0)
 * - `upsert`: bool - creates the object if it doesn't exist. defaults to false.
 * - `fields`: ***REMOVED***Object|String***REMOVED*** - Field selection. Equivalent to `.select(fields).findOneAndUpdate()`
 * - `sort`: if multiple docs are found by the conditions, sets the sort order to choose which doc to update
 * - `maxTimeMS`: puts a time limit on the query - requires mongodb >= 2.6.0
 * - `runValidators`: if true, runs [update validators](/docs/validation.html#update-validators) on this command. Update validators validate the update operation against the model's schema.
 * - `setDefaultsOnInsert`: if this and `upsert` are true, mongoose will apply the [defaults](http://mongoosejs.com/docs/defaults.html) specified in the model's schema if a new document is created. This option only works on MongoDB >= 2.4 because it relies on [MongoDB's `$setOnInsert` operator](https://docs.mongodb.org/v2.4/reference/operator/update/setOnInsert/).
 * - `passRawResult`: if true, passes the [raw result from the MongoDB driver as the third callback parameter](http://mongodb.github.io/node-mongodb-native/2.0/api/Collection.html#findAndModify)
 * - `context` (string) if set to 'query' and `runValidators` is on, `this` will refer to the query in custom validator functions that update validation runs. Does nothing if `runValidators` is false.
 *
 * ####Callback Signature
 *     function(error, doc) ***REMOVED***
 *       // error: any errors that occurred
 *       // doc: the document before updates are applied if `new: false`, or after updates if `new = true`
 *     ***REMOVED***
 *
 * ####Examples
 *
 *     query.findOneAndUpdate(conditions, update, options, callback) // executes
 *     query.findOneAndUpdate(conditions, update, options)  // returns Query
 *     query.findOneAndUpdate(conditions, update, callback) // executes
 *     query.findOneAndUpdate(conditions, update)           // returns Query
 *     query.findOneAndUpdate(update, callback)             // returns Query
 *     query.findOneAndUpdate(update)                       // returns Query
 *     query.findOneAndUpdate(callback)                     // executes
 *     query.findOneAndUpdate()                             // returns Query
 *
 * @method findOneAndUpdate
 * @memberOf Query
 * @param ***REMOVED***Object|Query***REMOVED*** [query]
 * @param ***REMOVED***Object***REMOVED*** [doc]
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @see mongodb http://www.mongodb.org/display/DOCS/findAndModify+Command
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

Query.prototype.findOneAndUpdate = function(criteria, doc, options, callback) ***REMOVED***
  this.op = 'findOneAndUpdate';
  this._validate();

  switch (arguments.length) ***REMOVED***
    case 3:
      if (typeof options === 'function') ***REMOVED***
        callback = options;
        options = ***REMOVED******REMOVED***;
      ***REMOVED***
      break;
    case 2:
      if (typeof doc === 'function') ***REMOVED***
        callback = doc;
        doc = criteria;
        criteria = undefined;
      ***REMOVED***
      options = undefined;
      break;
    case 1:
      if (typeof criteria === 'function') ***REMOVED***
        callback = criteria;
        criteria = options = doc = undefined;
      ***REMOVED*** else ***REMOVED***
        doc = criteria;
        criteria = options = undefined;
      ***REMOVED***
  ***REMOVED***

  if (mquery.canMerge(criteria)) ***REMOVED***
    this.merge(criteria);
  ***REMOVED***

  // apply doc
  if (doc) ***REMOVED***
    this._mergeUpdate(doc);
  ***REMOVED***

  if (options) ***REMOVED***
    options = utils.clone(options, ***REMOVED*** retainKeyOrder: true ***REMOVED***);
    if (options.projection) ***REMOVED***
      this.select(options.projection);
      delete options.projection;
    ***REMOVED***
    if (options.fields) ***REMOVED***
      this.select(options.fields);
      delete options.fields;
    ***REMOVED***

    this.setOptions(options);
  ***REMOVED***

  if (!callback) ***REMOVED***
    return this;
  ***REMOVED***

  return this._findOneAndUpdate(callback);
***REMOVED***;

/**
 * Thunk around findOneAndUpdate()
 *
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @api private
 */

Query.prototype._findOneAndUpdate = function(callback) ***REMOVED***
  this._findAndModify('update', callback);
  return this;
***REMOVED***;

/**
 * Issues a mongodb [findAndModify](http://www.mongodb.org/display/DOCS/findAndModify+Command) remove command.
 *
 * Finds a matching document, removes it, passing the found document (if any) to the callback. Executes immediately if `callback` is passed.
 *
 * ####Available options
 *
 * - `sort`: if multiple docs are found by the conditions, sets the sort order to choose which doc to update
 * - `maxTimeMS`: puts a time limit on the query - requires mongodb >= 2.6.0
 * - `passRawResult`: if true, passes the [raw result from the MongoDB driver as the third callback parameter](http://mongodb.github.io/node-mongodb-native/2.0/api/Collection.html#findAndModify)
 *
 * ####Callback Signature
 *     function(error, doc, result) ***REMOVED***
 *       // error: any errors that occurred
 *       // doc: the document before updates are applied if `new: false`, or after updates if `new = true`
 *       // result: [raw result from the MongoDB driver](http://mongodb.github.io/node-mongodb-native/2.0/api/Collection.html#findAndModify)
 *     ***REMOVED***
 *
 * ####Examples
 *
 *     A.where().findOneAndRemove(conditions, options, callback) // executes
 *     A.where().findOneAndRemove(conditions, options)  // return Query
 *     A.where().findOneAndRemove(conditions, callback) // executes
 *     A.where().findOneAndRemove(conditions) // returns Query
 *     A.where().findOneAndRemove(callback)   // executes
 *     A.where().findOneAndRemove()           // returns Query
 *
 * @method findOneAndRemove
 * @memberOf Query
 * @param ***REMOVED***Object***REMOVED*** [conditions]
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see mongodb http://www.mongodb.org/display/DOCS/findAndModify+Command
 * @api public
 */

Query.prototype.findOneAndRemove = function(conditions, options, callback) ***REMOVED***
  this.op = 'findOneAndRemove';
  this._validate();

  switch (arguments.length) ***REMOVED***
    case 2:
      if (typeof options === 'function') ***REMOVED***
        callback = options;
        options = ***REMOVED******REMOVED***;
      ***REMOVED***
      break;
    case 1:
      if (typeof conditions === 'function') ***REMOVED***
        callback = conditions;
        conditions = undefined;
        options = undefined;
      ***REMOVED***
      break;
  ***REMOVED***

  if (mquery.canMerge(conditions)) ***REMOVED***
    this.merge(conditions);
  ***REMOVED***

  options && this.setOptions(options);

  if (!callback) ***REMOVED***
    return this;
  ***REMOVED***

  this._findOneAndRemove(callback);

  return this;
***REMOVED***;

/**
 * Thunk around findOneAndRemove()
 *
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @api private
 */
Query.prototype._findOneAndRemove = function(callback) ***REMOVED***
  Query.base.findOneAndRemove.call(this, callback);
***REMOVED***;

/*!
 * ignore
 */

function decorateResult(res) ***REMOVED***
  if (res) ***REMOVED***
    res._kareemIgnore = true;
  ***REMOVED***
  return res;
***REMOVED***

/**
 * Override mquery.prototype._findAndModify to provide casting etc.
 *
 * @param ***REMOVED***String***REMOVED*** type - either "remove" or "update"
 * @param ***REMOVED***Function***REMOVED*** callback
 * @api private
 */

Query.prototype._findAndModify = function(type, callback) ***REMOVED***
  if (typeof callback !== 'function') ***REMOVED***
    throw new Error('Expected callback in _findAndModify');
  ***REMOVED***

  var model = this.model;
  var schema = model.schema;
  var _this = this;
  var castedQuery;
  var castedDoc;
  var fields;
  var opts;
  var doValidate;

  castedQuery = castQuery(this);
  if (castedQuery instanceof Error) ***REMOVED***
    return callback(castedQuery);
  ***REMOVED***

  opts = this._optionsForExec(model);

  if ('strict' in opts) ***REMOVED***
    this._mongooseOptions.strict = opts.strict;
  ***REMOVED***

  if (type === 'remove') ***REMOVED***
    opts.remove = true;
  ***REMOVED*** else ***REMOVED***
    if (!('new' in opts)) ***REMOVED***
      opts.new = false;
    ***REMOVED***
    if (!('upsert' in opts)) ***REMOVED***
      opts.upsert = false;
    ***REMOVED***
    if (opts.upsert || opts['new']) ***REMOVED***
      opts.remove = false;
    ***REMOVED***

    castedDoc = castDoc(this, opts.overwrite);
    castedDoc = setDefaultsOnInsert(this, schema, castedDoc, opts);
    if (!castedDoc) ***REMOVED***
      if (opts.upsert) ***REMOVED***
        // still need to do the upsert to empty doc
        var doc = utils.clone(castedQuery);
        delete doc._id;
        castedDoc = ***REMOVED***$set: doc***REMOVED***;
      ***REMOVED*** else ***REMOVED***
        return this.findOne(callback);
      ***REMOVED***
    ***REMOVED*** else if (castedDoc instanceof Error) ***REMOVED***
      return callback(castedDoc);
    ***REMOVED*** else ***REMOVED***
      // In order to make MongoDB 2.6 happy (see
      // https://jira.mongodb.org/browse/SERVER-12266 and related issues)
      // if we have an actual update document but $set is empty, junk the $set.
      if (castedDoc.$set && Object.keys(castedDoc.$set).length === 0) ***REMOVED***
        delete castedDoc.$set;
      ***REMOVED***
    ***REMOVED***

    doValidate = updateValidators(this, schema, castedDoc, opts);
  ***REMOVED***

  this._applyPaths();

  var options = this._mongooseOptions;

  if (this._fields) ***REMOVED***
    fields = utils.clone(this._fields);
    opts.fields = this._castFields(fields);
    if (opts.fields instanceof Error) ***REMOVED***
      return callback(opts.fields);
    ***REMOVED***
  ***REMOVED***

  if (opts.sort) convertSortToArray(opts);

  var cb = function(err, doc, res) ***REMOVED***
    if (err) ***REMOVED***
      return callback(err);
    ***REMOVED***

    if (!doc || (utils.isObject(doc) && Object.keys(doc).length === 0)) ***REMOVED***
      if (opts.passRawResult) ***REMOVED***
        return callback(null, null, decorateResult(res));
      ***REMOVED***
      return callback(null, null);
    ***REMOVED***

    if (!opts.passRawResult) ***REMOVED***
      res = null;
    ***REMOVED***

    if (!options.populate) ***REMOVED***
      return options.lean === true
          ? (opts.passRawResult ? callback(null, doc, decorateResult(res)) : callback(null, doc))
          : completeOne(_this.model, doc, res, fields, null, callback);
    ***REMOVED***

    var pop = helpers.preparePopulationOptionsMQ(_this, options);
    pop.__noPromise = true;
    _this.model.populate(doc, pop, function(err, doc) ***REMOVED***
      if (err) ***REMOVED***
        return callback(err);
      ***REMOVED***

      return options.lean === true
          ? (opts.passRawResult ? callback(null, doc, decorateResult(res)) : callback(null, doc))
          : completeOne(_this.model, doc, res, fields, pop, callback);
    ***REMOVED***);
  ***REMOVED***;

  if (opts.runValidators && doValidate) ***REMOVED***
    var _callback = function(error) ***REMOVED***
      if (error) ***REMOVED***
        return callback(error);
      ***REMOVED***
      _this._collection.findAndModify(castedQuery, castedDoc, opts, utils.tick(function(error, res) ***REMOVED***
        return cb(error, res ? res.value : res, res);
      ***REMOVED***));
    ***REMOVED***;

    try ***REMOVED***
      doValidate(_callback);
    ***REMOVED*** catch (error) ***REMOVED***
      callback(error);
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    this._collection.findAndModify(castedQuery, castedDoc, opts, utils.tick(function(error, res) ***REMOVED***
      return cb(error, res ? res.value : res, res);
    ***REMOVED***));
  ***REMOVED***

  return this;
***REMOVED***;

/**
 * Override mquery.prototype._mergeUpdate to handle mongoose objects in
 * updates.
 *
 * @param ***REMOVED***Object***REMOVED*** doc
 * @api private
 */

Query.prototype._mergeUpdate = function(doc) ***REMOVED***
  if (!this._update) this._update = ***REMOVED******REMOVED***;
  if (doc instanceof Query) ***REMOVED***
    if (doc._update) ***REMOVED***
      utils.mergeClone(this._update, doc._update);
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    utils.mergeClone(this._update, doc);
  ***REMOVED***
***REMOVED***;

/*!
 * The mongodb driver 1.3.23 only supports the nested array sort
 * syntax. We must convert it or sorting findAndModify will not work.
 */

function convertSortToArray(opts) ***REMOVED***
  if (Array.isArray(opts.sort)) ***REMOVED***
    return;
  ***REMOVED***
  if (!utils.isObject(opts.sort)) ***REMOVED***
    return;
  ***REMOVED***

  var sort = [];

  for (var key in opts.sort) ***REMOVED***
    if (utils.object.hasOwnProperty(opts.sort, key)) ***REMOVED***
      sort.push([key, opts.sort[key]]);
    ***REMOVED***
  ***REMOVED***

  opts.sort = sort;
***REMOVED***

/**
 * Internal thunk for .update()
 *
 * @param ***REMOVED***Function***REMOVED*** callback
 * @see Model.update #model_Model.update
 * @api private
 */
Query.prototype._execUpdate = function(callback) ***REMOVED***
  var schema = this.model.schema;
  var doValidate;
  var _this;

  var castedQuery = this._conditions;
  var castedDoc = this._update;
  var options = this.options;

  if (this._castError) ***REMOVED***
    callback(this._castError);
    return this;
  ***REMOVED***

  if (this.options.runValidators) ***REMOVED***
    _this = this;
    doValidate = updateValidators(this, schema, castedDoc, options);
    var _callback = function(err) ***REMOVED***
      if (err) ***REMOVED***
        return callback(err);
      ***REMOVED***

      Query.base.update.call(_this, castedQuery, castedDoc, options, callback);
    ***REMOVED***;
    try ***REMOVED***
      doValidate(_callback);
    ***REMOVED*** catch (err) ***REMOVED***
      process.nextTick(function() ***REMOVED***
        callback(err);
      ***REMOVED***);
    ***REMOVED***
    return this;
  ***REMOVED***

  Query.base.update.call(this, castedQuery, castedDoc, options, callback);
  return this;
***REMOVED***;

/**
 * Declare and/or execute this query as an update() operation.
 *
 * _All paths passed that are not $atomic operations will become $set ops._
 *
 * ####Example
 *
 *     Model.where(***REMOVED*** _id: id ***REMOVED***).update(***REMOVED*** title: 'words' ***REMOVED***)
 *
 *     // becomes
 *
 *     Model.where(***REMOVED*** _id: id ***REMOVED***).update(***REMOVED*** $set: ***REMOVED*** title: 'words' ***REMOVED******REMOVED***)
 *
 * ####Valid options:
 *
 *  - `safe` (boolean) safe mode (defaults to value set in schema (true))
 *  - `upsert` (boolean) whether to create the doc if it doesn't match (false)
 *  - `multi` (boolean) whether multiple documents should be updated (false)
 *  - `runValidators`: if true, runs [update validators](/docs/validation.html#update-validators) on this command. Update validators validate the update operation against the model's schema.
 *  - `setDefaultsOnInsert`: if this and `upsert` are true, mongoose will apply the [defaults](http://mongoosejs.com/docs/defaults.html) specified in the model's schema if a new document is created. This option only works on MongoDB >= 2.4 because it relies on [MongoDB's `$setOnInsert` operator](https://docs.mongodb.org/v2.4/reference/operator/update/setOnInsert/).
 *  - `strict` (boolean) overrides the `strict` option for this update
 *  - `overwrite` (boolean) disables update-only mode, allowing you to overwrite the doc (false)
 *  - `context` (string) if set to 'query' and `runValidators` is on, `this` will refer to the query in custom validator functions that update validation runs. Does nothing if `runValidators` is false.
 *
 * ####Note
 *
 * Passing an empty object `***REMOVED******REMOVED***` as the doc will result in a no-op unless the `overwrite` option is passed. Without the `overwrite` option set, the update operation will be ignored and the callback executed without sending the command to MongoDB so as to prevent accidently overwritting documents in the collection.
 *
 * ####Note
 *
 * The operation is only executed when a callback is passed. To force execution without a callback, we must first call update() and then execute it by using the `exec()` method.
 *
 *     var q = Model.where(***REMOVED*** _id: id ***REMOVED***);
 *     q.update(***REMOVED*** $set: ***REMOVED*** name: 'bob' ***REMOVED******REMOVED***).update(); // not executed
 *
 *     q.update(***REMOVED*** $set: ***REMOVED*** name: 'bob' ***REMOVED******REMOVED***).exec(); // executed
 *
 *     // keys that are not $atomic ops become $set.
 *     // this executes the same command as the previous example.
 *     q.update(***REMOVED*** name: 'bob' ***REMOVED***).exec();
 *
 *     // overwriting with empty docs
 *     var q = Model.where(***REMOVED*** _id: id ***REMOVED***).setOptions(***REMOVED*** overwrite: true ***REMOVED***)
 *     q.update(***REMOVED*** ***REMOVED***, callback); // executes
 *
 *     // multi update with overwrite to empty doc
 *     var q = Model.where(***REMOVED*** _id: id ***REMOVED***);
 *     q.setOptions(***REMOVED*** multi: true, overwrite: true ***REMOVED***)
 *     q.update(***REMOVED*** ***REMOVED***);
 *     q.update(callback); // executed
 *
 *     // multi updates
 *     Model.where()
 *          .update(***REMOVED*** name: /^match/ ***REMOVED***, ***REMOVED*** $set: ***REMOVED*** arr: [] ***REMOVED******REMOVED***, ***REMOVED*** multi: true ***REMOVED***, callback)
 *
 *     // more multi updates
 *     Model.where()
 *          .setOptions(***REMOVED*** multi: true ***REMOVED***)
 *          .update(***REMOVED*** $set: ***REMOVED*** arr: [] ***REMOVED******REMOVED***, callback)
 *
 *     // single update by default
 *     Model.where(***REMOVED*** email: 'address@example.com' ***REMOVED***)
 *          .update(***REMOVED*** $inc: ***REMOVED*** counter: 1 ***REMOVED******REMOVED***, callback)
 *
 * API summary
 *
 *     update(criteria, doc, options, cb) // executes
 *     update(criteria, doc, options)
 *     update(criteria, doc, cb) // executes
 *     update(criteria, doc)
 *     update(doc, cb) // executes
 *     update(doc)
 *     update(cb) // executes
 *     update(true) // executes
 *     update()
 *
 * @param ***REMOVED***Object***REMOVED*** [criteria]
 * @param ***REMOVED***Object***REMOVED*** [doc] the update command
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see Model.update #model_Model.update
 * @see update http://docs.mongodb.org/manual/reference/method/db.collection.update/
 * @api public
 */

Query.prototype.update = function(conditions, doc, options, callback) ***REMOVED***
  if (typeof options === 'function') ***REMOVED***
    // .update(conditions, doc, callback)
    callback = options;
    options = null;
  ***REMOVED*** else if (typeof doc === 'function') ***REMOVED***
    // .update(doc, callback);
    callback = doc;
    doc = conditions;
    conditions = ***REMOVED******REMOVED***;
    options = null;
  ***REMOVED*** else if (typeof conditions === 'function') ***REMOVED***
    // .update(callback)
    callback = conditions;
    conditions = undefined;
    doc = undefined;
    options = undefined;
  ***REMOVED*** else if (typeof conditions === 'object' && !doc && !options && !callback) ***REMOVED***
    // .update(doc)
    doc = conditions;
    conditions = undefined;
    options = undefined;
    callback = undefined;
  ***REMOVED***

  // make sure we don't send in the whole Document to merge()
  conditions = utils.toObject(conditions);

  var oldCb = callback;
  if (oldCb) ***REMOVED***
    if (typeof oldCb === 'function') ***REMOVED***
      callback = function(error, result) ***REMOVED***
        oldCb(error, result ? result.result : ***REMOVED***ok: 0, n: 0, nModified: 0***REMOVED***);
      ***REMOVED***;
    ***REMOVED*** else ***REMOVED***
      throw new Error('Invalid callback() argument.');
    ***REMOVED***
  ***REMOVED***

  // strict is an option used in the update checking, make sure it gets set
  if (options) ***REMOVED***
    if ('strict' in options) ***REMOVED***
      this._mongooseOptions.strict = options.strict;
    ***REMOVED***
  ***REMOVED***

  // if doc is undefined at this point, this means this function is being
  // executed by exec(not always see below). Grab the update doc from here in
  // order to validate
  // This could also be somebody calling update() or update(***REMOVED******REMOVED***). Probably not a
  // common use case, check for _update to make sure we don't do anything bad
  if (!doc && this._update) ***REMOVED***
    doc = this._updateForExec();
  ***REMOVED***

  if (mquery.canMerge(conditions)) ***REMOVED***
    this.merge(conditions);
  ***REMOVED***

  // validate the selector part of the query
  var castedQuery = castQuery(this);
  if (castedQuery instanceof Error) ***REMOVED***
    this._castError = castedQuery;
    if (callback) ***REMOVED***
      callback(castedQuery);
      return this;
    ***REMOVED*** else if (!options || !options.dontThrowCastError) ***REMOVED***
      throw castedQuery;
    ***REMOVED***
  ***REMOVED***

  // validate the update part of the query
  var castedDoc;
  try ***REMOVED***
    var $options = ***REMOVED***retainKeyOrder: true***REMOVED***;
    if (options && options.minimize) ***REMOVED***
      $options.minimize = true;
    ***REMOVED***
    castedDoc = this._castUpdate(utils.clone(doc, $options),
        options && options.overwrite);
  ***REMOVED*** catch (err) ***REMOVED***
    this._castError = castedQuery;
    if (callback) ***REMOVED***
      callback(err);
      return this;
    ***REMOVED*** else if (!options || !options.dontThrowCastError) ***REMOVED***
      throw err;
    ***REMOVED***
  ***REMOVED***

  castedDoc = setDefaultsOnInsert(this, this.schema, castedDoc, options);
  if (!castedDoc) ***REMOVED***
    // Make sure promises know that this is still an update, see gh-2796
    this.op = 'update';
    callback && callback(null);
    return this;
  ***REMOVED***

  if (utils.isObject(options)) ***REMOVED***
    this.setOptions(options);
  ***REMOVED***

  if (!this._update) this._update = castedDoc;

  // Hooks
  if (callback) ***REMOVED***
    return this._execUpdate(callback);
  ***REMOVED***

  return Query.base.update.call(this, castedQuery, castedDoc, options, callback);
***REMOVED***;

/**
 * Executes the query
 *
 * ####Examples:
 *
 *     var promise = query.exec();
 *     var promise = query.exec('update');
 *
 *     query.exec(callback);
 *     query.exec('find', callback);
 *
 * @param ***REMOVED***String|Function***REMOVED*** [operation]
 * @param ***REMOVED***Function***REMOVED*** [callback]
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 */

Query.prototype.exec = function exec(op, callback) ***REMOVED***
  var Promise = PromiseProvider.get();
  var _this = this;

  if (typeof op === 'function') ***REMOVED***
    callback = op;
    op = null;
  ***REMOVED*** else if (typeof op === 'string') ***REMOVED***
    this.op = op;
  ***REMOVED***

  var _results;
  var promise = new Promise.ES6(function(resolve, reject) ***REMOVED***
    if (!_this.op) ***REMOVED***
      resolve();
      return;
    ***REMOVED***

    _this[_this.op].call(_this, function(error, res) ***REMOVED***
      if (error) ***REMOVED***
        reject(error);
        return;
      ***REMOVED***
      _results = arguments;
      resolve(res);
    ***REMOVED***);
  ***REMOVED***);

  if (callback) ***REMOVED***
    promise.then(
      function() ***REMOVED***
        callback.apply(null, _results);
      ***REMOVED***,
      function(error) ***REMOVED***
        callback(error);
      ***REMOVED***).
      catch(function(error) ***REMOVED***
        // If we made it here, we must have an error in the callback re:
        // gh-4500, so we need to emit.
        setImmediate(function() ***REMOVED***
          _this.model.emit('error', error);
        ***REMOVED***);
      ***REMOVED***);
  ***REMOVED***

  return promise;
***REMOVED***;

/**
 * Executes the query returning a `Promise` which will be
 * resolved with either the doc(s) or rejected with the error.
 *
 * @param ***REMOVED***Function***REMOVED*** [resolve]
 * @param ***REMOVED***Function***REMOVED*** [reject]
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 */

Query.prototype.then = function(resolve, reject) ***REMOVED***
  return this.exec().then(resolve, reject);
***REMOVED***;

/**
 * Executes the query returning a `Promise` which will be
 * resolved with either the doc(s) or rejected with the error.
 * Like `.then()`, but only takes a rejection handler.
 *
 * @param ***REMOVED***Function***REMOVED*** [reject]
 * @return ***REMOVED***Promise***REMOVED***
 * @api public
 */

Query.prototype.catch = function(reject) ***REMOVED***
  return this.exec().then(null, reject);
***REMOVED***;

/**
 * Finds the schema for `path`. This is different than
 * calling `schema.path` as it also resolves paths with
 * positional selectors (something.$.another.$.path).
 *
 * @param ***REMOVED***String***REMOVED*** path
 * @api private
 */

Query.prototype._getSchema = function _getSchema(path) ***REMOVED***
  return this.model._getSchema(path);
***REMOVED***;

/*!
 * These operators require casting docs
 * to real Documents for Update operations.
 */

var castOps = ***REMOVED***
  $push: 1,
  $pushAll: 1,
  $addToSet: 1,
  $set: 1
***REMOVED***;

/*!
 * These operators should be cast to numbers instead
 * of their path schema type.
 */

var numberOps = ***REMOVED***
  $pop: 1,
  $unset: 1,
  $inc: 1
***REMOVED***;

/**
 * Casts obj for an update command.
 *
 * @param ***REMOVED***Object***REMOVED*** obj
 * @return ***REMOVED***Object***REMOVED*** obj after casting its values
 * @api private
 */

Query.prototype._castUpdate = function _castUpdate(obj, overwrite) ***REMOVED***
  if (!obj) ***REMOVED***
    return undefined;
  ***REMOVED***

  var ops = Object.keys(obj);
  var i = ops.length;
  var ret = ***REMOVED******REMOVED***;
  var hasKeys;
  var val;
  var hasDollarKey = false;

  while (i--) ***REMOVED***
    var op = ops[i];
    // if overwrite is set, don't do any of the special $set stuff
    if (op[0] !== '$' && !overwrite) ***REMOVED***
      // fix up $set sugar
      if (!ret.$set) ***REMOVED***
        if (obj.$set) ***REMOVED***
          ret.$set = obj.$set;
        ***REMOVED*** else ***REMOVED***
          ret.$set = ***REMOVED******REMOVED***;
        ***REMOVED***
      ***REMOVED***
      ret.$set[op] = obj[op];
      ops.splice(i, 1);
      if (!~ops.indexOf('$set')) ops.push('$set');
    ***REMOVED*** else if (op === '$set') ***REMOVED***
      if (!ret.$set) ***REMOVED***
        ret[op] = obj[op];
      ***REMOVED***
    ***REMOVED*** else ***REMOVED***
      ret[op] = obj[op];
    ***REMOVED***
  ***REMOVED***

  // cast each value
  i = ops.length;

  // if we get passed ***REMOVED******REMOVED*** for the update, we still need to respect that when it
  // is an overwrite scenario
  if (overwrite) ***REMOVED***
    hasKeys = true;
  ***REMOVED***

  while (i--) ***REMOVED***
    op = ops[i];
    val = ret[op];
    hasDollarKey = hasDollarKey || op.charAt(0) === '$';
    if (val &&
        typeof val === 'object' &&
        (!overwrite || hasDollarKey)) ***REMOVED***
      hasKeys |= this._walkUpdatePath(val, op);
    ***REMOVED*** else if (overwrite && ret && typeof ret === 'object') ***REMOVED***
      // if we are just using overwrite, cast the query and then we will
      // *always* return the value, even if it is an empty object. We need to
      // set hasKeys above because we need to account for the case where the
      // user passes ***REMOVED******REMOVED*** and wants to clobber the whole document
      // Also, _walkUpdatePath expects an operation, so give it $set since that
      // is basically what we're doing
      this._walkUpdatePath(ret, '$set');
    ***REMOVED*** else ***REMOVED***
      var msg = 'Invalid atomic update value for ' + op + '. '
          + 'Expected an object, received ' + typeof val;
      throw new Error(msg);
    ***REMOVED***
  ***REMOVED***

  return hasKeys && ret;
***REMOVED***;

/**
 * Walk each path of obj and cast its values
 * according to its schema.
 *
 * @param ***REMOVED***Object***REMOVED*** obj - part of a query
 * @param ***REMOVED***String***REMOVED*** op - the atomic operator ($pull, $set, etc)
 * @param ***REMOVED***String***REMOVED*** pref - path prefix (internal only)
 * @return ***REMOVED***Bool***REMOVED*** true if this path has keys to update
 * @api private
 */

Query.prototype._walkUpdatePath = function _walkUpdatePath(obj, op, pref) ***REMOVED***
  var prefix = pref ? pref + '.' : '',
      keys = Object.keys(obj),
      i = keys.length,
      hasKeys = false,
      schema,
      key,
      val;

  var useNestedStrict = this.schema.options.useNestedStrict;

  while (i--) ***REMOVED***
    key = keys[i];
    val = obj[key];

    if (val && val.constructor.name === 'Object') ***REMOVED***
      // watch for embedded doc schemas
      schema = this._getSchema(prefix + key);
      if (schema && schema.caster && op in castOps) ***REMOVED***
        // embedded doc schema
        hasKeys = true;

        if ('$each' in val) ***REMOVED***
          obj[key] = ***REMOVED***
            $each: this._castUpdateVal(schema, val.$each, op)
          ***REMOVED***;

          if (val.$slice != null) ***REMOVED***
            obj[key].$slice = val.$slice | 0;
          ***REMOVED***

          if (val.$sort) ***REMOVED***
            obj[key].$sort = val.$sort;
          ***REMOVED***

          if (!!val.$position || val.$position === 0) ***REMOVED***
            obj[key].$position = val.$position;
          ***REMOVED***
        ***REMOVED*** else ***REMOVED***
          obj[key] = this._castUpdateVal(schema, val, op);
        ***REMOVED***
      ***REMOVED*** else if (op === '$currentDate') ***REMOVED***
        // $currentDate can take an object
        obj[key] = this._castUpdateVal(schema, val, op);
        hasKeys = true;
      ***REMOVED*** else if (op === '$set' && schema) ***REMOVED***
        obj[key] = this._castUpdateVal(schema, val, op);
        hasKeys = true;
      ***REMOVED*** else ***REMOVED***
        var pathToCheck = (prefix + key);
        var v = this.model.schema._getPathType(pathToCheck);
        var _strict = 'strict' in this._mongooseOptions ?
          this._mongooseOptions.strict :
          ((useNestedStrict && v.schema) || this.schema).options.strict;
        if (v.pathType === 'undefined') ***REMOVED***
          if (_strict === 'throw') ***REMOVED***
            throw new StrictModeError(pathToCheck);
          ***REMOVED*** else if (_strict) ***REMOVED***
            delete obj[key];
            continue;
          ***REMOVED***
        ***REMOVED***

        // gh-2314
        // we should be able to set a schema-less field
        // to an empty object literal
        hasKeys |= this._walkUpdatePath(val, op, prefix + key) ||
            (utils.isObject(val) && Object.keys(val).length === 0);
      ***REMOVED***
    ***REMOVED*** else ***REMOVED***
      var checkPath = (key === '$each' || key === '$or' || key === '$and') ?
        pref : prefix + key;
      schema = this._getSchema(checkPath);

      var pathDetails = this.model.schema._getPathType(checkPath);
      var isStrict = 'strict' in this._mongooseOptions ?
        this._mongooseOptions.strict :
        ((useNestedStrict && pathDetails.schema) || this.schema).options.strict;

      var skip = isStrict &&
          !schema &&
          !/real|nested/.test(pathDetails.pathType);

      if (skip) ***REMOVED***
        if (isStrict === 'throw') ***REMOVED***
          throw new StrictModeError(prefix + key);
        ***REMOVED*** else ***REMOVED***
          delete obj[key];
        ***REMOVED***
      ***REMOVED*** else ***REMOVED***
        // gh-1845 temporary fix: ignore $rename. See gh-3027 for tracking
        // improving this.
        if (op === '$rename') ***REMOVED***
          hasKeys = true;
          continue;
        ***REMOVED***

        hasKeys = true;
        obj[key] = this._castUpdateVal(schema, val, op, key);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
  return hasKeys;
***REMOVED***;

/**
 * Casts `val` according to `schema` and atomic `op`.
 *
 * @param ***REMOVED***Schema***REMOVED*** schema
 * @param ***REMOVED***Object***REMOVED*** val
 * @param ***REMOVED***String***REMOVED*** op - the atomic operator ($pull, $set, etc)
 * @param ***REMOVED***String***REMOVED*** [$conditional]
 * @api private
 */

Query.prototype._castUpdateVal = function _castUpdateVal(schema, val, op, $conditional) ***REMOVED***
  if (!schema) ***REMOVED***
    // non-existing schema path
    return op in numberOps
        ? Number(val)
        : val;
  ***REMOVED***

  var cond = schema.caster && op in castOps &&
      (utils.isObject(val) || Array.isArray(val));
  if (cond) ***REMOVED***
    // Cast values for ops that add data to MongoDB.
    // Ensures embedded documents get ObjectIds etc.
    var tmp = schema.cast(val);
    if (Array.isArray(val)) ***REMOVED***
      val = tmp;
    ***REMOVED*** else if (Array.isArray(tmp)) ***REMOVED***
      val = tmp[0];
    ***REMOVED*** else ***REMOVED***
      val = tmp;
    ***REMOVED***
  ***REMOVED***

  if (op in numberOps) ***REMOVED***
    if (op === '$inc') ***REMOVED***
      return schema.castForQuery(val);
    ***REMOVED***
    return Number(val);
  ***REMOVED***
  if (op === '$currentDate') ***REMOVED***
    if (typeof val === 'object') ***REMOVED***
      return ***REMOVED***$type: val.$type***REMOVED***;
    ***REMOVED***
    return Boolean(val);
  ***REMOVED***
  if (/^\$/.test($conditional)) ***REMOVED***
    return schema.castForQuery($conditional, val);
  ***REMOVED***

  return schema.castForQuery(val);
***REMOVED***;

/*!
 * castQuery
 * @api private
 */

function castQuery(query) ***REMOVED***
  try ***REMOVED***
    return query.cast(query.model);
  ***REMOVED*** catch (err) ***REMOVED***
    return err;
  ***REMOVED***
***REMOVED***

/*!
 * castDoc
 * @api private
 */

function castDoc(query, overwrite) ***REMOVED***
  try ***REMOVED***
    return query._castUpdate(query._update, overwrite);
  ***REMOVED*** catch (err) ***REMOVED***
    return err;
  ***REMOVED***
***REMOVED***

/**
 * Specifies paths which should be populated with other documents.
 *
 * ####Example:
 *
 *     Kitten.findOne().populate('owner').exec(function (err, kitten) ***REMOVED***
 *       console.log(kitten.owner.name) // Max
 *     ***REMOVED***)
 *
 *     Kitten.find().populate(***REMOVED***
 *         path: 'owner'
 *       , select: 'name'
 *       , match: ***REMOVED*** color: 'black' ***REMOVED***
 *       , options: ***REMOVED*** sort: ***REMOVED*** name: -1 ***REMOVED******REMOVED***
 *     ***REMOVED***).exec(function (err, kittens) ***REMOVED***
 *       console.log(kittens[0].owner.name) // Zoopa
 *     ***REMOVED***)
 *
 *     // alternatively
 *     Kitten.find().populate('owner', 'name', null, ***REMOVED***sort: ***REMOVED*** name: -1 ***REMOVED******REMOVED***).exec(function (err, kittens) ***REMOVED***
 *       console.log(kittens[0].owner.name) // Zoopa
 *     ***REMOVED***)
 *
 * Paths are populated after the query executes and a response is received. A separate query is then executed for each path specified for population. After a response for each query has also been returned, the results are passed to the callback.
 *
 * @param ***REMOVED***Object|String***REMOVED*** path either the path to populate or an object specifying all parameters
 * @param ***REMOVED***Object|String***REMOVED*** [select] Field selection for the population query
 * @param ***REMOVED***Model***REMOVED*** [model] The model you wish to use for population. If not specified, populate will look up the model by the name in the Schema's `ref` field.
 * @param ***REMOVED***Object***REMOVED*** [match] Conditions for the population query
 * @param ***REMOVED***Object***REMOVED*** [options] Options for the population query (sort, etc)
 * @see population ./populate.html
 * @see Query#select #query_Query-select
 * @see Model.populate #model_Model.populate
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

Query.prototype.populate = function() ***REMOVED***
  var res = utils.populate.apply(null, arguments);
  var opts = this._mongooseOptions;

  if (!utils.isObject(opts.populate)) ***REMOVED***
    opts.populate = ***REMOVED******REMOVED***;
  ***REMOVED***

  var pop = opts.populate;

  for (var i = 0; i < res.length; ++i) ***REMOVED***
    var path = res[i].path;
    if (pop[path] && pop[path].populate && res[i].populate) ***REMOVED***
      res[i].populate = pop[path].populate.concat(res[i].populate);
    ***REMOVED***
    pop[res[i].path] = res[i];
  ***REMOVED***

  return this;
***REMOVED***;

/**
 * Casts this query to the schema of `model`
 *
 * ####Note
 *
 * If `obj` is present, it is cast instead of this query.
 *
 * @param ***REMOVED***Model***REMOVED*** model
 * @param ***REMOVED***Object***REMOVED*** [obj]
 * @return ***REMOVED***Object***REMOVED***
 * @api public
 */

Query.prototype.cast = function(model, obj) ***REMOVED***
  obj || (obj = this._conditions);

  try ***REMOVED***
    return cast(model.schema, obj, ***REMOVED***
      upsert: this.options && this.options.upsert,
      strict: (this.options && this.options.strict) ||
        (model.schema.options && model.schema.options.strict)
    ***REMOVED***);
  ***REMOVED*** catch (err) ***REMOVED***
    // CastError, assign model
    if (typeof err.setModel === 'function') ***REMOVED***
      err.setModel(model);
    ***REMOVED***
    throw err;
  ***REMOVED***
***REMOVED***;

/**
 * Casts selected field arguments for field selection with mongo 2.2
 *
 *     query.select(***REMOVED*** ids: ***REMOVED*** $elemMatch: ***REMOVED*** $in: [hexString] ***REMOVED******REMOVED***)
 *
 * @param ***REMOVED***Object***REMOVED*** fields
 * @see https://github.com/Automattic/mongoose/issues/1091
 * @see http://docs.mongodb.org/manual/reference/projection/elemMatch/
 * @api private
 */

Query.prototype._castFields = function _castFields(fields) ***REMOVED***
  var selected,
      elemMatchKeys,
      keys,
      key,
      out,
      i;

  if (fields) ***REMOVED***
    keys = Object.keys(fields);
    elemMatchKeys = [];
    i = keys.length;

    // collect $elemMatch args
    while (i--) ***REMOVED***
      key = keys[i];
      if (fields[key].$elemMatch) ***REMOVED***
        selected || (selected = ***REMOVED******REMOVED***);
        selected[key] = fields[key];
        elemMatchKeys.push(key);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  if (selected) ***REMOVED***
    // they passed $elemMatch, cast em
    try ***REMOVED***
      out = this.cast(this.model, selected);
    ***REMOVED*** catch (err) ***REMOVED***
      return err;
    ***REMOVED***

    // apply the casted field args
    i = elemMatchKeys.length;
    while (i--) ***REMOVED***
      key = elemMatchKeys[i];
      fields[key] = out[key];
    ***REMOVED***
  ***REMOVED***

  return fields;
***REMOVED***;

/**
 * Applies schematype selected options to this query.
 * @api private
 */

Query.prototype._applyPaths = function applyPaths() ***REMOVED***
  // determine if query is selecting or excluding fields

  var fields = this._fields,
      exclude,
      keys,
      ki;

  if (fields) ***REMOVED***
    keys = Object.keys(fields);
    ki = keys.length;

    while (ki--) ***REMOVED***
      if (keys[ki][0] === '+') continue;
      exclude = fields[keys[ki]] === 0;
      break;
    ***REMOVED***
  ***REMOVED***

  // if selecting, apply default schematype select:true fields
  // if excluding, apply schematype select:false fields

  var selected = [],
      excluded = [],
      seen = [];

  var analyzePath = function(path, type) ***REMOVED***
    if (typeof type.selected !== 'boolean') return;

    var plusPath = '+' + path;
    if (fields && plusPath in fields) ***REMOVED***
      // forced inclusion
      delete fields[plusPath];

      // if there are other fields being included, add this one
      // if no other included fields, leave this out (implied inclusion)
      if (exclude === false && keys.length > 1 && !~keys.indexOf(path)) ***REMOVED***
        fields[path] = 1;
      ***REMOVED***

      return;
    ***REMOVED***

    // check for parent exclusions
    var root = path.split('.')[0];
    if (~excluded.indexOf(root)) return;

    (type.selected ? selected : excluded).push(path);
  ***REMOVED***;

  var analyzeSchema = function(schema, prefix) ***REMOVED***
    prefix || (prefix = '');

    // avoid recursion
    if (~seen.indexOf(schema)) return;
    seen.push(schema);

    schema.eachPath(function(path, type) ***REMOVED***
      if (prefix) path = prefix + '.' + path;

      analyzePath(path, type);

      // array of subdocs?
      if (type.schema) ***REMOVED***
        analyzeSchema(type.schema, path);
      ***REMOVED***
    ***REMOVED***);
  ***REMOVED***;

  analyzeSchema(this.model.schema);

  switch (exclude) ***REMOVED***
    case true:
      excluded.length && this.select('-' + excluded.join(' -'));
      break;
    case false:
      if (this.model.schema && this.model.schema.paths['_id'] &&
          this.model.schema.paths['_id'].options && this.model.schema.paths['_id'].options.select === false) ***REMOVED***
        selected.push('-_id');
      ***REMOVED***
      selected.length && this.select(selected.join(' '));
      break;
    case undefined:
      // user didn't specify fields, implies returning all fields.
      // only need to apply excluded fields
      excluded.length && this.select('-' + excluded.join(' -'));
      break;
  ***REMOVED***
  seen = excluded = selected = keys = fields = null;
***REMOVED***;

/**
 * Returns a Node.js 0.8 style [read stream](http://nodejs.org/docs/v0.8.21/api/stream.html#stream_readable_stream) interface.
 *
 * ####Example
 *
 *     // follows the nodejs 0.8 stream api
 *     Thing.find(***REMOVED*** name: /^hello/ ***REMOVED***).stream().pipe(res)
 *
 *     // manual streaming
 *     var stream = Thing.find(***REMOVED*** name: /^hello/ ***REMOVED***).stream();
 *
 *     stream.on('data', function (doc) ***REMOVED***
 *       // do something with the mongoose document
 *     ***REMOVED***).on('error', function (err) ***REMOVED***
 *       // handle the error
 *     ***REMOVED***).on('close', function () ***REMOVED***
 *       // the stream is closed
 *     ***REMOVED***);
 *
 * ####Valid options
 *
 *   - `transform`: optional function which accepts a mongoose document. The return value of the function will be emitted on `data`.
 *
 * ####Example
 *
 *     // JSON.stringify all documents before emitting
 *     var stream = Thing.find().stream(***REMOVED*** transform: JSON.stringify ***REMOVED***);
 *     stream.pipe(writeStream);
 *
 * @return ***REMOVED***QueryStream***REMOVED***
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @see QueryStream
 * @api public
 */

Query.prototype.stream = function stream(opts) ***REMOVED***
  this._applyPaths();
  this._fields = this._castFields(this._fields);
  return new QueryStream(this, opts);
***REMOVED***;
Query.prototype.stream = util.deprecate(Query.prototype.stream, 'Mongoose: ' +
  'Query.prototype.stream() is deprecated in mongoose >= 4.5.0, ' +
  'use Query.prototype.cursor() instead');

/**
 * Returns a wrapper around a [mongodb driver cursor](http://mongodb.github.io/node-mongodb-native/2.1/api/Cursor.html).
 * A QueryCursor exposes a [Streams3](https://strongloop.com/strongblog/whats-new-io-js-beta-streams3/)-compatible
 * interface, as well as a `.next()` function.
 *
 * ####Example
 *
 *     // There are 2 ways to use a cursor. First, as a stream:
 *     Thing.
 *       find(***REMOVED*** name: /^hello/ ***REMOVED***).
 *       cursor().
 *       on('data', function(doc) ***REMOVED*** console.log(doc); ***REMOVED***).
 *       on('end', function() ***REMOVED*** console.log('Done!'); ***REMOVED***);
 *
 *     // Or you can use `.next()` to manually get the next doc in the stream.
 *     // `.next()` returns a promise, so you can use promises or callbacks.
 *     var cursor = Thing.find(***REMOVED*** name: /^hello/ ***REMOVED***).cursor();
 *     cursor.next(function(error, doc) ***REMOVED***
 *       console.log(doc);
 *     ***REMOVED***);
 *
 *     // Because `.next()` returns a promise, you can use co
 *     // to easily iterate through all documents without loading them
 *     // all into memory.
 *     co(function*() ***REMOVED***
 *       const cursor = Thing.find(***REMOVED*** name: /^hello/ ***REMOVED***).cursor();
 *       for (let doc = yield cursor.next(); doc != null; doc = yield cursor.next()) ***REMOVED***
 *         console.log(doc);
 *       ***REMOVED***
 *     ***REMOVED***);
 *
 * ####Valid options
 *
 *   - `transform`: optional function which accepts a mongoose document. The return value of the function will be emitted on `data` and returned by `.next()`.
 *
 * @return ***REMOVED***QueryCursor***REMOVED***
 * @param ***REMOVED***Object***REMOVED*** [options]
 * @see QueryCursor
 * @api public
 */

Query.prototype.cursor = function cursor(opts) ***REMOVED***
  this._applyPaths();
  this._fields = this._castFields(this._fields);
  this.setOptions(***REMOVED*** fields: this._fieldsForExec() ***REMOVED***);
  if (opts) ***REMOVED***
    this.setOptions(opts);
  ***REMOVED***

  try ***REMOVED***
    this.cast(this.model);
  ***REMOVED*** catch (err) ***REMOVED***
    return (new QueryCursor(this, this.options))._markError(err);
  ***REMOVED***

  return new QueryCursor(this, this.options);
***REMOVED***;

// the rest of these are basically to support older Mongoose syntax with mquery

/**
 * _DEPRECATED_ Alias of `maxScan`
 *
 * @deprecated
 * @see maxScan #query_Query-maxScan
 * @method maxscan
 * @memberOf Query
 */

Query.prototype.maxscan = Query.base.maxScan;

/**
 * Sets the tailable option (for use with capped collections).
 *
 * ####Example
 *
 *     query.tailable() // true
 *     query.tailable(true)
 *     query.tailable(false)
 *
 * ####Note
 *
 * Cannot be used with `distinct()`
 *
 * @param ***REMOVED***Boolean***REMOVED*** bool defaults to true
 * @param ***REMOVED***Object***REMOVED*** [opts] options to set
 * @param ***REMOVED***Number***REMOVED*** [opts.numberOfRetries] if cursor is exhausted, retry this many times before giving up
 * @param ***REMOVED***Number***REMOVED*** [opts.tailableRetryInterval] if cursor is exhausted, wait this many milliseconds before retrying
 * @see tailable http://docs.mongodb.org/manual/tutorial/create-tailable-cursor/
 * @api public
 */

Query.prototype.tailable = function(val, opts) ***REMOVED***
  // we need to support the tailable(***REMOVED*** awaitdata : true ***REMOVED***) as well as the
  // tailable(true, ***REMOVED***awaitdata :true***REMOVED***) syntax that mquery does not support
  if (val && val.constructor.name === 'Object') ***REMOVED***
    opts = val;
    val = true;
  ***REMOVED***

  if (val === undefined) ***REMOVED***
    val = true;
  ***REMOVED***

  if (opts && typeof opts === 'object') ***REMOVED***
    for (var key in opts) ***REMOVED***
      if (key === 'awaitdata') ***REMOVED***
        // For backwards compatibility
        this.options[key] = !!opts[key];
      ***REMOVED*** else ***REMOVED***
        this.options[key] = opts[key];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  return Query.base.tailable.call(this, val);
***REMOVED***;

/**
 * Declares an intersects query for `geometry()`.
 *
 * ####Example
 *
 *     query.where('path').intersects().geometry(***REMOVED***
 *         type: 'LineString'
 *       , coordinates: [[180.0, 11.0], [180, 9.0]]
 *     ***REMOVED***)
 *
 *     query.where('path').intersects(***REMOVED***
 *         type: 'LineString'
 *       , coordinates: [[180.0, 11.0], [180, 9.0]]
 *     ***REMOVED***)
 *
 * ####NOTE:
 *
 * **MUST** be used after `where()`.
 *
 * ####NOTE:
 *
 * In Mongoose 3.7, `intersects` changed from a getter to a function. If you need the old syntax, use [this](https://github.com/ebensing/mongoose-within).
 *
 * @method intersects
 * @memberOf Query
 * @param ***REMOVED***Object***REMOVED*** [arg]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $geometry http://docs.mongodb.org/manual/reference/operator/geometry/
 * @see geoIntersects http://docs.mongodb.org/manual/reference/operator/geoIntersects/
 * @api public
 */

/**
 * Specifies a `$geometry` condition
 *
 * ####Example
 *
 *     var polyA = [[[ 10, 20 ], [ 10, 40 ], [ 30, 40 ], [ 30, 20 ]]]
 *     query.where('loc').within().geometry(***REMOVED*** type: 'Polygon', coordinates: polyA ***REMOVED***)
 *
 *     // or
 *     var polyB = [[ 0, 0 ], [ 1, 1 ]]
 *     query.where('loc').within().geometry(***REMOVED*** type: 'LineString', coordinates: polyB ***REMOVED***)
 *
 *     // or
 *     var polyC = [ 0, 0 ]
 *     query.where('loc').within().geometry(***REMOVED*** type: 'Point', coordinates: polyC ***REMOVED***)
 *
 *     // or
 *     query.where('loc').intersects().geometry(***REMOVED*** type: 'Point', coordinates: polyC ***REMOVED***)
 *
 * The argument is assigned to the most recent path passed to `where()`.
 *
 * ####NOTE:
 *
 * `geometry()` **must** come after either `intersects()` or `within()`.
 *
 * The `object` argument must contain `type` and `coordinates` properties.
 * - type ***REMOVED***String***REMOVED***
 * - coordinates ***REMOVED***Array***REMOVED***
 *
 * @method geometry
 * @memberOf Query
 * @param ***REMOVED***Object***REMOVED*** object Must contain a `type` property which is a String and a `coordinates` property which is an Array. See the examples.
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $geometry http://docs.mongodb.org/manual/reference/operator/geometry/
 * @see http://docs.mongodb.org/manual/release-notes/2.4/#new-geospatial-indexes-with-geojson-and-improved-spherical-geometry
 * @see http://www.mongodb.org/display/DOCS/Geospatial+Indexing
 * @api public
 */

/**
 * Specifies a `$near` or `$nearSphere` condition
 *
 * These operators return documents sorted by distance.
 *
 * ####Example
 *
 *     query.where('loc').near(***REMOVED*** center: [10, 10] ***REMOVED***);
 *     query.where('loc').near(***REMOVED*** center: [10, 10], maxDistance: 5 ***REMOVED***);
 *     query.where('loc').near(***REMOVED*** center: [10, 10], maxDistance: 5, spherical: true ***REMOVED***);
 *     query.near('loc', ***REMOVED*** center: [10, 10], maxDistance: 5 ***REMOVED***);
 *
 * @method near
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Object***REMOVED*** val
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $near http://docs.mongodb.org/manual/reference/operator/near/
 * @see $nearSphere http://docs.mongodb.org/manual/reference/operator/nearSphere/
 * @see $maxDistance http://docs.mongodb.org/manual/reference/operator/maxDistance/
 * @see http://www.mongodb.org/display/DOCS/Geospatial+Indexing
 * @api public
 */

/*!
 * Overwriting mquery is needed to support a couple different near() forms found in older
 * versions of mongoose
 * near([1,1])
 * near(1,1)
 * near(field, [1,2])
 * near(field, 1, 2)
 * In addition to all of the normal forms supported by mquery
 */

Query.prototype.near = function() ***REMOVED***
  var params = [];
  var sphere = this._mongooseOptions.nearSphere;

  // TODO refactor

  if (arguments.length === 1) ***REMOVED***
    if (Array.isArray(arguments[0])) ***REMOVED***
      params.push(***REMOVED***center: arguments[0], spherical: sphere***REMOVED***);
    ***REMOVED*** else if (typeof arguments[0] === 'string') ***REMOVED***
      // just passing a path
      params.push(arguments[0]);
    ***REMOVED*** else if (utils.isObject(arguments[0])) ***REMOVED***
      if (typeof arguments[0].spherical !== 'boolean') ***REMOVED***
        arguments[0].spherical = sphere;
      ***REMOVED***
      params.push(arguments[0]);
    ***REMOVED*** else ***REMOVED***
      throw new TypeError('invalid argument');
    ***REMOVED***
  ***REMOVED*** else if (arguments.length === 2) ***REMOVED***
    if (typeof arguments[0] === 'number' && typeof arguments[1] === 'number') ***REMOVED***
      params.push(***REMOVED***center: [arguments[0], arguments[1]], spherical: sphere***REMOVED***);
    ***REMOVED*** else if (typeof arguments[0] === 'string' && Array.isArray(arguments[1])) ***REMOVED***
      params.push(arguments[0]);
      params.push(***REMOVED***center: arguments[1], spherical: sphere***REMOVED***);
    ***REMOVED*** else if (typeof arguments[0] === 'string' && utils.isObject(arguments[1])) ***REMOVED***
      params.push(arguments[0]);
      if (typeof arguments[1].spherical !== 'boolean') ***REMOVED***
        arguments[1].spherical = sphere;
      ***REMOVED***
      params.push(arguments[1]);
    ***REMOVED*** else ***REMOVED***
      throw new TypeError('invalid argument');
    ***REMOVED***
  ***REMOVED*** else if (arguments.length === 3) ***REMOVED***
    if (typeof arguments[0] === 'string' && typeof arguments[1] === 'number'
        && typeof arguments[2] === 'number') ***REMOVED***
      params.push(arguments[0]);
      params.push(***REMOVED***center: [arguments[1], arguments[2]], spherical: sphere***REMOVED***);
    ***REMOVED*** else ***REMOVED***
      throw new TypeError('invalid argument');
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
    throw new TypeError('invalid argument');
  ***REMOVED***

  return Query.base.near.apply(this, params);
***REMOVED***;

/**
 * _DEPRECATED_ Specifies a `$nearSphere` condition
 *
 * ####Example
 *
 *     query.where('loc').nearSphere(***REMOVED*** center: [10, 10], maxDistance: 5 ***REMOVED***);
 *
 * **Deprecated.** Use `query.near()` instead with the `spherical` option set to `true`.
 *
 * ####Example
 *
 *     query.where('loc').near(***REMOVED*** center: [10, 10], spherical: true ***REMOVED***);
 *
 * @deprecated
 * @see near() #query_Query-near
 * @see $near http://docs.mongodb.org/manual/reference/operator/near/
 * @see $nearSphere http://docs.mongodb.org/manual/reference/operator/nearSphere/
 * @see $maxDistance http://docs.mongodb.org/manual/reference/operator/maxDistance/
 */

Query.prototype.nearSphere = function() ***REMOVED***
  this._mongooseOptions.nearSphere = true;
  this.near.apply(this, arguments);
  return this;
***REMOVED***;

/**
 * Specifies a $polygon condition
 *
 * ####Example
 *
 *     query.where('loc').within().polygon([10,20], [13, 25], [7,15])
 *     query.polygon('loc', [10,20], [13, 25], [7,15])
 *
 * @method polygon
 * @memberOf Query
 * @param ***REMOVED***String|Array***REMOVED*** [path]
 * @param ***REMOVED***Array|Object***REMOVED*** [coordinatePairs...]
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $polygon http://docs.mongodb.org/manual/reference/operator/polygon/
 * @see http://www.mongodb.org/display/DOCS/Geospatial+Indexing
 * @api public
 */

/**
 * Specifies a $box condition
 *
 * ####Example
 *
 *     var lowerLeft = [40.73083, -73.99756]
 *     var upperRight= [40.741404,  -73.988135]
 *
 *     query.where('loc').within().box(lowerLeft, upperRight)
 *     query.box(***REMOVED*** ll : lowerLeft, ur : upperRight ***REMOVED***)
 *
 * @method box
 * @memberOf Query
 * @see $box http://docs.mongodb.org/manual/reference/operator/box/
 * @see within() Query#within #query_Query-within
 * @see http://www.mongodb.org/display/DOCS/Geospatial+Indexing
 * @param ***REMOVED***Object***REMOVED*** val
 * @param [Array] Upper Right Coords
 * @return ***REMOVED***Query***REMOVED*** this
 * @api public
 */

/*!
 * this is needed to support the mongoose syntax of:
 * box(field, ***REMOVED*** ll : [x,y], ur : [x2,y2] ***REMOVED***)
 * box(***REMOVED*** ll : [x,y], ur : [x2,y2] ***REMOVED***)
 */

Query.prototype.box = function(ll, ur) ***REMOVED***
  if (!Array.isArray(ll) && utils.isObject(ll)) ***REMOVED***
    ur = ll.ur;
    ll = ll.ll;
  ***REMOVED***
  return Query.base.box.call(this, ll, ur);
***REMOVED***;

/**
 * Specifies a $center or $centerSphere condition.
 *
 * ####Example
 *
 *     var area = ***REMOVED*** center: [50, 50], radius: 10, unique: true ***REMOVED***
 *     query.where('loc').within().circle(area)
 *     // alternatively
 *     query.circle('loc', area);
 *
 *     // spherical calculations
 *     var area = ***REMOVED*** center: [50, 50], radius: 10, unique: true, spherical: true ***REMOVED***
 *     query.where('loc').within().circle(area)
 *     // alternatively
 *     query.circle('loc', area);
 *
 * New in 3.7.0
 *
 * @method circle
 * @memberOf Query
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Object***REMOVED*** area
 * @return ***REMOVED***Query***REMOVED*** this
 * @see $center http://docs.mongodb.org/manual/reference/operator/center/
 * @see $centerSphere http://docs.mongodb.org/manual/reference/operator/centerSphere/
 * @see $geoWithin http://docs.mongodb.org/manual/reference/operator/geoWithin/
 * @see http://www.mongodb.org/display/DOCS/Geospatial+Indexing
 * @api public
 */

/**
 * _DEPRECATED_ Alias for [circle](#query_Query-circle)
 *
 * **Deprecated.** Use [circle](#query_Query-circle) instead.
 *
 * @deprecated
 * @method center
 * @memberOf Query
 * @api public
 */

Query.prototype.center = Query.base.circle;

/**
 * _DEPRECATED_ Specifies a $centerSphere condition
 *
 * **Deprecated.** Use [circle](#query_Query-circle) instead.
 *
 * ####Example
 *
 *     var area = ***REMOVED*** center: [50, 50], radius: 10 ***REMOVED***;
 *     query.where('loc').within().centerSphere(area);
 *
 * @deprecated
 * @param ***REMOVED***String***REMOVED*** [path]
 * @param ***REMOVED***Object***REMOVED*** val
 * @return ***REMOVED***Query***REMOVED*** this
 * @see http://www.mongodb.org/display/DOCS/Geospatial+Indexing
 * @see $centerSphere http://docs.mongodb.org/manual/reference/operator/centerSphere/
 * @api public
 */

Query.prototype.centerSphere = function() ***REMOVED***
  if (arguments[0] && arguments[0].constructor.name === 'Object') ***REMOVED***
    arguments[0].spherical = true;
  ***REMOVED***

  if (arguments[1] && arguments[1].constructor.name === 'Object') ***REMOVED***
    arguments[1].spherical = true;
  ***REMOVED***

  Query.base.circle.apply(this, arguments);
***REMOVED***;

/**
 * Determines if field selection has been made.
 *
 * @method selected
 * @memberOf Query
 * @return ***REMOVED***Boolean***REMOVED***
 * @api public
 */

/**
 * Determines if inclusive field selection has been made.
 *
 *     query.selectedInclusively() // false
 *     query.select('name')
 *     query.selectedInclusively() // true
 *
 * @method selectedInclusively
 * @memberOf Query
 * @return ***REMOVED***Boolean***REMOVED***
 * @api public
 */

/**
 * Determines if exclusive field selection has been made.
 *
 *     query.selectedExclusively() // false
 *     query.select('-name')
 *     query.selectedExclusively() // true
 *     query.selectedInclusively() // false
 *
 * @method selectedExclusively
 * @memberOf Query
 * @return ***REMOVED***Boolean***REMOVED***
 * @api public
 */

/*!
 * Export
 */

module.exports = Query;

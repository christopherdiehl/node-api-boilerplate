/*!
 * Module dependencies.
 */

var mongodb = require('mongodb');
var ReadPref = mongodb.ReadPreference;

/*!
 * Converts arguments to ReadPrefs the driver
 * can understand.
 *
 * @param ***REMOVED***String|Array***REMOVED*** pref
 * @param ***REMOVED***Array***REMOVED*** [tags]
 */

module.exports = function readPref(pref, tags) ***REMOVED***
  if (Array.isArray(pref)) ***REMOVED***
    tags = pref[1];
    pref = pref[0];
  ***REMOVED***

  if (pref instanceof ReadPref) ***REMOVED***
    return pref;
  ***REMOVED***

  switch (pref) ***REMOVED***
    case 'p':
      pref = 'primary';
      break;
    case 'pp':
      pref = 'primaryPreferred';
      break;
    case 's':
      pref = 'secondary';
      break;
    case 'sp':
      pref = 'secondaryPreferred';
      break;
    case 'n':
      pref = 'nearest';
      break;
  ***REMOVED***

  return new ReadPref(pref, tags);
***REMOVED***;

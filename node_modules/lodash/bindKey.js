var baseRest = require('./_baseRest'),
    createWrap = require('./_createWrap'),
    getHolder = require('./_getHolder'),
    replaceHolders = require('./_replaceHolders');

/** Used to compose bitmasks for function metadata. */
var WRAP_BIND_FLAG = 1,
    WRAP_BIND_KEY_FLAG = 2,
    WRAP_PARTIAL_FLAG = 32;

/**
 * Creates a function that invokes the method at `object[key]` with `partials`
 * prepended to the arguments it receives.
 *
 * This method differs from `_.bind` by allowing bound functions to reference
 * methods that may be redefined or don't yet exist. See
 * [Peter Michaux's article](http://peter.michaux.ca/articles/lazy-function-definition-pattern)
 * for more details.
 *
 * The `_.bindKey.placeholder` value, which defaults to `_` in monolithic
 * builds, may be used as a placeholder for partially applied arguments.
 *
 * @static
 * @memberOf _
 * @since 0.10.0
 * @category Function
 * @param ***REMOVED***Object***REMOVED*** object The object to invoke the method on.
 * @param ***REMOVED***string***REMOVED*** key The key of the method.
 * @param ***REMOVED***...****REMOVED*** [partials] The arguments to be partially applied.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new bound function.
 * @example
 *
 * var object = ***REMOVED***
 *   'user': 'fred',
 *   'greet': function(greeting, punctuation) ***REMOVED***
 *     return greeting + ' ' + this.user + punctuation;
 *   ***REMOVED***
 * ***REMOVED***;
 *
 * var bound = _.bindKey(object, 'greet', 'hi');
 * bound('!');
 * // => 'hi fred!'
 *
 * object.greet = function(greeting, punctuation) ***REMOVED***
 *   return greeting + 'ya ' + this.user + punctuation;
 * ***REMOVED***;
 *
 * bound('!');
 * // => 'hiya fred!'
 *
 * // Bound with placeholders.
 * var bound = _.bindKey(object, 'greet', _, '!');
 * bound('hi');
 * // => 'hiya fred!'
 */
var bindKey = baseRest(function(object, key, partials) ***REMOVED***
  var bitmask = WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG;
  if (partials.length) ***REMOVED***
    var holders = replaceHolders(partials, getHolder(bindKey));
    bitmask |= WRAP_PARTIAL_FLAG;
  ***REMOVED***
  return createWrap(key, bitmask, object, partials, holders);
***REMOVED***);

// Assign default placeholders.
bindKey.placeholder = ***REMOVED******REMOVED***;

module.exports = bindKey;

var arrayMap = require('./_arrayMap'),
    baseClone = require('./_baseClone'),
    baseUnset = require('./_baseUnset'),
    castPath = require('./_castPath'),
    copyObject = require('./_copyObject'),
    customOmitClone = require('./_customOmitClone'),
    flatRest = require('./_flatRest'),
    getAllKeysIn = require('./_getAllKeysIn');

/** Used to compose bitmasks for cloning. */
var CLONE_DEEP_FLAG = 1,
    CLONE_FLAT_FLAG = 2,
    CLONE_SYMBOLS_FLAG = 4;

/**
 * The opposite of `_.pick`; this method creates an object composed of the
 * own and inherited enumerable property paths of `object` that are not omitted.
 *
 * **Note:** This method is considerably slower than `_.pick`.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The source object.
 * @param ***REMOVED***...(string|string[])***REMOVED*** [paths] The property paths to omit.
 * @returns ***REMOVED***Object***REMOVED*** Returns the new object.
 * @example
 *
 * var object = ***REMOVED*** 'a': 1, 'b': '2', 'c': 3 ***REMOVED***;
 *
 * _.omit(object, ['a', 'c']);
 * // => ***REMOVED*** 'b': '2' ***REMOVED***
 */
var omit = flatRest(function(object, paths) ***REMOVED***
  var result = ***REMOVED******REMOVED***;
  if (object == null) ***REMOVED***
    return result;
  ***REMOVED***
  var isDeep = false;
  paths = arrayMap(paths, function(path) ***REMOVED***
    path = castPath(path, object);
    isDeep || (isDeep = path.length > 1);
    return path;
  ***REMOVED***);
  copyObject(object, getAllKeysIn(object), result);
  if (isDeep) ***REMOVED***
    result = baseClone(result, CLONE_DEEP_FLAG | CLONE_FLAT_FLAG | CLONE_SYMBOLS_FLAG, customOmitClone);
  ***REMOVED***
  var length = paths.length;
  while (length--) ***REMOVED***
    baseUnset(result, paths[length]);
  ***REMOVED***
  return result;
***REMOVED***);

module.exports = omit;

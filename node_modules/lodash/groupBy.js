var baseAssignValue = require('./_baseAssignValue'),
    createAggregator = require('./_createAggregator');

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Creates an object composed of keys generated from the results of running
 * each element of `collection` thru `iteratee`. The order of grouped values
 * is determined by the order they occur in `collection`. The corresponding
 * value of each key is an array of elements responsible for generating the
 * key. The iteratee is invoked with one argument: (value).
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Collection
 * @param ***REMOVED***Array|Object***REMOVED*** collection The collection to iterate over.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The iteratee to transform keys.
 * @returns ***REMOVED***Object***REMOVED*** Returns the composed aggregate object.
 * @example
 *
 * _.groupBy([6.1, 4.2, 6.3], Math.floor);
 * // => ***REMOVED*** '4': [4.2], '6': [6.1, 6.3] ***REMOVED***
 *
 * // The `_.property` iteratee shorthand.
 * _.groupBy(['one', 'two', 'three'], 'length');
 * // => ***REMOVED*** '3': ['one', 'two'], '5': ['three'] ***REMOVED***
 */
var groupBy = createAggregator(function(result, value, key) ***REMOVED***
  if (hasOwnProperty.call(result, key)) ***REMOVED***
    result[key].push(value);
  ***REMOVED*** else ***REMOVED***
    baseAssignValue(result, key, [value]);
  ***REMOVED***
***REMOVED***);

module.exports = groupBy;

var apply = require('./_apply'),
    baseRest = require('./_baseRest'),
    isError = require('./isError');

/**
 * Attempts to invoke `func`, returning either the result or the caught error
 * object. Any additional arguments are provided to `func` when it's invoked.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Util
 * @param ***REMOVED***Function***REMOVED*** func The function to attempt.
 * @param ***REMOVED***...****REMOVED*** [args] The arguments to invoke `func` with.
 * @returns ***REMOVED*******REMOVED*** Returns the `func` result or error object.
 * @example
 *
 * // Avoid throwing errors for invalid selectors.
 * var elements = _.attempt(function(selector) ***REMOVED***
 *   return document.querySelectorAll(selector);
 * ***REMOVED***, '>_>');
 *
 * if (_.isError(elements)) ***REMOVED***
 *   elements = [];
 * ***REMOVED***
 */
var attempt = baseRest(function(func, args) ***REMOVED***
  try ***REMOVED***
    return apply(func, undefined, args);
  ***REMOVED*** catch (e) ***REMOVED***
    return isError(e) ? e : new Error(e);
  ***REMOVED***
***REMOVED***);

module.exports = attempt;

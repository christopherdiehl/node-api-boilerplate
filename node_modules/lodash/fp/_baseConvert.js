var mapping = require('./_mapping'),
    fallbackHolder = require('./placeholder');

/** Built-in value reference. */
var push = Array.prototype.push;

/**
 * Creates a function, with an arity of `n`, that invokes `func` with the
 * arguments it receives.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** func The function to wrap.
 * @param ***REMOVED***number***REMOVED*** n The arity of the new function.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new function.
 */
function baseArity(func, n) ***REMOVED***
  return n == 2
    ? function(a, b) ***REMOVED*** return func.apply(undefined, arguments); ***REMOVED***
    : function(a) ***REMOVED*** return func.apply(undefined, arguments); ***REMOVED***;
***REMOVED***

/**
 * Creates a function that invokes `func`, with up to `n` arguments, ignoring
 * any additional arguments.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** func The function to cap arguments for.
 * @param ***REMOVED***number***REMOVED*** n The arity cap.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new function.
 */
function baseAry(func, n) ***REMOVED***
  return n == 2
    ? function(a, b) ***REMOVED*** return func(a, b); ***REMOVED***
    : function(a) ***REMOVED*** return func(a); ***REMOVED***;
***REMOVED***

/**
 * Creates a clone of `array`.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to clone.
 * @returns ***REMOVED***Array***REMOVED*** Returns the cloned array.
 */
function cloneArray(array) ***REMOVED***
  var length = array ? array.length : 0,
      result = Array(length);

  while (length--) ***REMOVED***
    result[length] = array[length];
  ***REMOVED***
  return result;
***REMOVED***

/**
 * Creates a function that clones a given object using the assignment `func`.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** func The assignment function.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new cloner function.
 */
function createCloner(func) ***REMOVED***
  return function(object) ***REMOVED***
    return func(***REMOVED******REMOVED***, object);
  ***REMOVED***;
***REMOVED***

/**
 * A specialized version of `_.spread` which flattens the spread array into
 * the arguments of the invoked `func`.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** func The function to spread arguments over.
 * @param ***REMOVED***number***REMOVED*** start The start position of the spread.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new function.
 */
function flatSpread(func, start) ***REMOVED***
  return function() ***REMOVED***
    var length = arguments.length,
        lastIndex = length - 1,
        args = Array(length);

    while (length--) ***REMOVED***
      args[length] = arguments[length];
    ***REMOVED***
    var array = args[start],
        otherArgs = args.slice(0, start);

    if (array) ***REMOVED***
      push.apply(otherArgs, array);
    ***REMOVED***
    if (start != lastIndex) ***REMOVED***
      push.apply(otherArgs, args.slice(start + 1));
    ***REMOVED***
    return func.apply(this, otherArgs);
  ***REMOVED***;
***REMOVED***

/**
 * Creates a function that wraps `func` and uses `cloner` to clone the first
 * argument it receives.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** func The function to wrap.
 * @param ***REMOVED***Function***REMOVED*** cloner The function to clone arguments.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new immutable function.
 */
function wrapImmutable(func, cloner) ***REMOVED***
  return function() ***REMOVED***
    var length = arguments.length;
    if (!length) ***REMOVED***
      return;
    ***REMOVED***
    var args = Array(length);
    while (length--) ***REMOVED***
      args[length] = arguments[length];
    ***REMOVED***
    var result = args[0] = cloner.apply(undefined, args);
    func.apply(undefined, args);
    return result;
  ***REMOVED***;
***REMOVED***

/**
 * The base implementation of `convert` which accepts a `util` object of methods
 * required to perform conversions.
 *
 * @param ***REMOVED***Object***REMOVED*** util The util object.
 * @param ***REMOVED***string***REMOVED*** name The name of the function to convert.
 * @param ***REMOVED***Function***REMOVED*** func The function to convert.
 * @param ***REMOVED***Object***REMOVED*** [options] The options object.
 * @param ***REMOVED***boolean***REMOVED*** [options.cap=true] Specify capping iteratee arguments.
 * @param ***REMOVED***boolean***REMOVED*** [options.curry=true] Specify currying.
 * @param ***REMOVED***boolean***REMOVED*** [options.fixed=true] Specify fixed arity.
 * @param ***REMOVED***boolean***REMOVED*** [options.immutable=true] Specify immutable operations.
 * @param ***REMOVED***boolean***REMOVED*** [options.rearg=true] Specify rearranging arguments.
 * @returns ***REMOVED***Function|Object***REMOVED*** Returns the converted function or object.
 */
function baseConvert(util, name, func, options) ***REMOVED***
  var setPlaceholder,
      isLib = typeof name == 'function',
      isObj = name === Object(name);

  if (isObj) ***REMOVED***
    options = func;
    func = name;
    name = undefined;
  ***REMOVED***
  if (func == null) ***REMOVED***
    throw new TypeError;
  ***REMOVED***
  options || (options = ***REMOVED******REMOVED***);

  var config = ***REMOVED***
    'cap': 'cap' in options ? options.cap : true,
    'curry': 'curry' in options ? options.curry : true,
    'fixed': 'fixed' in options ? options.fixed : true,
    'immutable': 'immutable' in options ? options.immutable : true,
    'rearg': 'rearg' in options ? options.rearg : true
  ***REMOVED***;

  var forceCurry = ('curry' in options) && options.curry,
      forceFixed = ('fixed' in options) && options.fixed,
      forceRearg = ('rearg' in options) && options.rearg,
      placeholder = isLib ? func : fallbackHolder,
      pristine = isLib ? func.runInContext() : undefined;

  var helpers = isLib ? func : ***REMOVED***
    'ary': util.ary,
    'assign': util.assign,
    'clone': util.clone,
    'curry': util.curry,
    'forEach': util.forEach,
    'isArray': util.isArray,
    'isFunction': util.isFunction,
    'iteratee': util.iteratee,
    'keys': util.keys,
    'rearg': util.rearg,
    'toInteger': util.toInteger,
    'toPath': util.toPath
  ***REMOVED***;

  var ary = helpers.ary,
      assign = helpers.assign,
      clone = helpers.clone,
      curry = helpers.curry,
      each = helpers.forEach,
      isArray = helpers.isArray,
      isFunction = helpers.isFunction,
      keys = helpers.keys,
      rearg = helpers.rearg,
      toInteger = helpers.toInteger,
      toPath = helpers.toPath;

  var aryMethodKeys = keys(mapping.aryMethod);

  var wrappers = ***REMOVED***
    'castArray': function(castArray) ***REMOVED***
      return function() ***REMOVED***
        var value = arguments[0];
        return isArray(value)
          ? castArray(cloneArray(value))
          : castArray.apply(undefined, arguments);
      ***REMOVED***;
    ***REMOVED***,
    'iteratee': function(iteratee) ***REMOVED***
      return function() ***REMOVED***
        var func = arguments[0],
            arity = arguments[1],
            result = iteratee(func, arity),
            length = result.length;

        if (config.cap && typeof arity == 'number') ***REMOVED***
          arity = arity > 2 ? (arity - 2) : 1;
          return (length && length <= arity) ? result : baseAry(result, arity);
        ***REMOVED***
        return result;
      ***REMOVED***;
    ***REMOVED***,
    'mixin': function(mixin) ***REMOVED***
      return function(source) ***REMOVED***
        var func = this;
        if (!isFunction(func)) ***REMOVED***
          return mixin(func, Object(source));
        ***REMOVED***
        var pairs = [];
        each(keys(source), function(key) ***REMOVED***
          if (isFunction(source[key])) ***REMOVED***
            pairs.push([key, func.prototype[key]]);
          ***REMOVED***
        ***REMOVED***);

        mixin(func, Object(source));

        each(pairs, function(pair) ***REMOVED***
          var value = pair[1];
          if (isFunction(value)) ***REMOVED***
            func.prototype[pair[0]] = value;
          ***REMOVED*** else ***REMOVED***
            delete func.prototype[pair[0]];
          ***REMOVED***
        ***REMOVED***);
        return func;
      ***REMOVED***;
    ***REMOVED***,
    'nthArg': function(nthArg) ***REMOVED***
      return function(n) ***REMOVED***
        var arity = n < 0 ? 1 : (toInteger(n) + 1);
        return curry(nthArg(n), arity);
      ***REMOVED***;
    ***REMOVED***,
    'rearg': function(rearg) ***REMOVED***
      return function(func, indexes) ***REMOVED***
        var arity = indexes ? indexes.length : 0;
        return curry(rearg(func, indexes), arity);
      ***REMOVED***;
    ***REMOVED***,
    'runInContext': function(runInContext) ***REMOVED***
      return function(context) ***REMOVED***
        return baseConvert(util, runInContext(context), options);
      ***REMOVED***;
    ***REMOVED***
  ***REMOVED***;

  /*--------------------------------------------------------------------------*/

  /**
   * Casts `func` to a function with an arity capped iteratee if needed.
   *
   * @private
   * @param ***REMOVED***string***REMOVED*** name The name of the function to inspect.
   * @param ***REMOVED***Function***REMOVED*** func The function to inspect.
   * @returns ***REMOVED***Function***REMOVED*** Returns the cast function.
   */
  function castCap(name, func) ***REMOVED***
    if (config.cap) ***REMOVED***
      var indexes = mapping.iterateeRearg[name];
      if (indexes) ***REMOVED***
        return iterateeRearg(func, indexes);
      ***REMOVED***
      var n = !isLib && mapping.iterateeAry[name];
      if (n) ***REMOVED***
        return iterateeAry(func, n);
      ***REMOVED***
    ***REMOVED***
    return func;
  ***REMOVED***

  /**
   * Casts `func` to a curried function if needed.
   *
   * @private
   * @param ***REMOVED***string***REMOVED*** name The name of the function to inspect.
   * @param ***REMOVED***Function***REMOVED*** func The function to inspect.
   * @param ***REMOVED***number***REMOVED*** n The arity of `func`.
   * @returns ***REMOVED***Function***REMOVED*** Returns the cast function.
   */
  function castCurry(name, func, n) ***REMOVED***
    return (forceCurry || (config.curry && n > 1))
      ? curry(func, n)
      : func;
  ***REMOVED***

  /**
   * Casts `func` to a fixed arity function if needed.
   *
   * @private
   * @param ***REMOVED***string***REMOVED*** name The name of the function to inspect.
   * @param ***REMOVED***Function***REMOVED*** func The function to inspect.
   * @param ***REMOVED***number***REMOVED*** n The arity cap.
   * @returns ***REMOVED***Function***REMOVED*** Returns the cast function.
   */
  function castFixed(name, func, n) ***REMOVED***
    if (config.fixed && (forceFixed || !mapping.skipFixed[name])) ***REMOVED***
      var data = mapping.methodSpread[name],
          start = data && data.start;

      return start  === undefined ? ary(func, n) : flatSpread(func, start);
    ***REMOVED***
    return func;
  ***REMOVED***

  /**
   * Casts `func` to an rearged function if needed.
   *
   * @private
   * @param ***REMOVED***string***REMOVED*** name The name of the function to inspect.
   * @param ***REMOVED***Function***REMOVED*** func The function to inspect.
   * @param ***REMOVED***number***REMOVED*** n The arity of `func`.
   * @returns ***REMOVED***Function***REMOVED*** Returns the cast function.
   */
  function castRearg(name, func, n) ***REMOVED***
    return (config.rearg && n > 1 && (forceRearg || !mapping.skipRearg[name]))
      ? rearg(func, mapping.methodRearg[name] || mapping.aryRearg[n])
      : func;
  ***REMOVED***

  /**
   * Creates a clone of `object` by `path`.
   *
   * @private
   * @param ***REMOVED***Object***REMOVED*** object The object to clone.
   * @param ***REMOVED***Array|string***REMOVED*** path The path to clone by.
   * @returns ***REMOVED***Object***REMOVED*** Returns the cloned object.
   */
  function cloneByPath(object, path) ***REMOVED***
    path = toPath(path);

    var index = -1,
        length = path.length,
        lastIndex = length - 1,
        result = clone(Object(object)),
        nested = result;

    while (nested != null && ++index < length) ***REMOVED***
      var key = path[index],
          value = nested[key];

      if (value != null) ***REMOVED***
        nested[path[index]] = clone(index == lastIndex ? value : Object(value));
      ***REMOVED***
      nested = nested[key];
    ***REMOVED***
    return result;
  ***REMOVED***

  /**
   * Converts `lodash` to an immutable auto-curried iteratee-first data-last
   * version with conversion `options` applied.
   *
   * @param ***REMOVED***Object***REMOVED*** [options] The options object. See `baseConvert` for more details.
   * @returns ***REMOVED***Function***REMOVED*** Returns the converted `lodash`.
   */
  function convertLib(options) ***REMOVED***
    return _.runInContext.convert(options)(undefined);
  ***REMOVED***

  /**
   * Create a converter function for `func` of `name`.
   *
   * @param ***REMOVED***string***REMOVED*** name The name of the function to convert.
   * @param ***REMOVED***Function***REMOVED*** func The function to convert.
   * @returns ***REMOVED***Function***REMOVED*** Returns the new converter function.
   */
  function createConverter(name, func) ***REMOVED***
    var realName = mapping.aliasToReal[name] || name,
        methodName = mapping.remap[realName] || realName,
        oldOptions = options;

    return function(options) ***REMOVED***
      var newUtil = isLib ? pristine : helpers,
          newFunc = isLib ? pristine[methodName] : func,
          newOptions = assign(assign(***REMOVED******REMOVED***, oldOptions), options);

      return baseConvert(newUtil, realName, newFunc, newOptions);
    ***REMOVED***;
  ***REMOVED***

  /**
   * Creates a function that wraps `func` to invoke its iteratee, with up to `n`
   * arguments, ignoring any additional arguments.
   *
   * @private
   * @param ***REMOVED***Function***REMOVED*** func The function to cap iteratee arguments for.
   * @param ***REMOVED***number***REMOVED*** n The arity cap.
   * @returns ***REMOVED***Function***REMOVED*** Returns the new function.
   */
  function iterateeAry(func, n) ***REMOVED***
    return overArg(func, function(func) ***REMOVED***
      return typeof func == 'function' ? baseAry(func, n) : func;
    ***REMOVED***);
  ***REMOVED***

  /**
   * Creates a function that wraps `func` to invoke its iteratee with arguments
   * arranged according to the specified `indexes` where the argument value at
   * the first index is provided as the first argument, the argument value at
   * the second index is provided as the second argument, and so on.
   *
   * @private
   * @param ***REMOVED***Function***REMOVED*** func The function to rearrange iteratee arguments for.
   * @param ***REMOVED***number[]***REMOVED*** indexes The arranged argument indexes.
   * @returns ***REMOVED***Function***REMOVED*** Returns the new function.
   */
  function iterateeRearg(func, indexes) ***REMOVED***
    return overArg(func, function(func) ***REMOVED***
      var n = indexes.length;
      return baseArity(rearg(baseAry(func, n), indexes), n);
    ***REMOVED***);
  ***REMOVED***

  /**
   * Creates a function that invokes `func` with its first argument transformed.
   *
   * @private
   * @param ***REMOVED***Function***REMOVED*** func The function to wrap.
   * @param ***REMOVED***Function***REMOVED*** transform The argument transform.
   * @returns ***REMOVED***Function***REMOVED*** Returns the new function.
   */
  function overArg(func, transform) ***REMOVED***
    return function() ***REMOVED***
      var length = arguments.length;
      if (!length) ***REMOVED***
        return func();
      ***REMOVED***
      var args = Array(length);
      while (length--) ***REMOVED***
        args[length] = arguments[length];
      ***REMOVED***
      var index = config.rearg ? 0 : (length - 1);
      args[index] = transform(args[index]);
      return func.apply(undefined, args);
    ***REMOVED***;
  ***REMOVED***

  /**
   * Creates a function that wraps `func` and applys the conversions
   * rules by `name`.
   *
   * @private
   * @param ***REMOVED***string***REMOVED*** name The name of the function to wrap.
   * @param ***REMOVED***Function***REMOVED*** func The function to wrap.
   * @returns ***REMOVED***Function***REMOVED*** Returns the converted function.
   */
  function wrap(name, func) ***REMOVED***
    var result,
        realName = mapping.aliasToReal[name] || name,
        wrapped = func,
        wrapper = wrappers[realName];

    if (wrapper) ***REMOVED***
      wrapped = wrapper(func);
    ***REMOVED***
    else if (config.immutable) ***REMOVED***
      if (mapping.mutate.array[realName]) ***REMOVED***
        wrapped = wrapImmutable(func, cloneArray);
      ***REMOVED***
      else if (mapping.mutate.object[realName]) ***REMOVED***
        wrapped = wrapImmutable(func, createCloner(func));
      ***REMOVED***
      else if (mapping.mutate.set[realName]) ***REMOVED***
        wrapped = wrapImmutable(func, cloneByPath);
      ***REMOVED***
    ***REMOVED***
    each(aryMethodKeys, function(aryKey) ***REMOVED***
      each(mapping.aryMethod[aryKey], function(otherName) ***REMOVED***
        if (realName == otherName) ***REMOVED***
          var data = mapping.methodSpread[realName],
              afterRearg = data && data.afterRearg;

          result = afterRearg
            ? castFixed(realName, castRearg(realName, wrapped, aryKey), aryKey)
            : castRearg(realName, castFixed(realName, wrapped, aryKey), aryKey);

          result = castCap(realName, result);
          result = castCurry(realName, result, aryKey);
          return false;
        ***REMOVED***
      ***REMOVED***);
      return !result;
    ***REMOVED***);

    result || (result = wrapped);
    if (result == func) ***REMOVED***
      result = forceCurry ? curry(result, 1) : function() ***REMOVED***
        return func.apply(this, arguments);
      ***REMOVED***;
    ***REMOVED***
    result.convert = createConverter(realName, func);
    if (mapping.placeholder[realName]) ***REMOVED***
      setPlaceholder = true;
      result.placeholder = func.placeholder = placeholder;
    ***REMOVED***
    return result;
  ***REMOVED***

  /*--------------------------------------------------------------------------*/

  if (!isObj) ***REMOVED***
    return wrap(name, func);
  ***REMOVED***
  var _ = func;

  // Convert methods by ary cap.
  var pairs = [];
  each(aryMethodKeys, function(aryKey) ***REMOVED***
    each(mapping.aryMethod[aryKey], function(key) ***REMOVED***
      var func = _[mapping.remap[key] || key];
      if (func) ***REMOVED***
        pairs.push([key, wrap(key, func)]);
      ***REMOVED***
    ***REMOVED***);
  ***REMOVED***);

  // Convert remaining methods.
  each(keys(_), function(key) ***REMOVED***
    var func = _[key];
    if (typeof func == 'function') ***REMOVED***
      var length = pairs.length;
      while (length--) ***REMOVED***
        if (pairs[length][0] == key) ***REMOVED***
          return;
        ***REMOVED***
      ***REMOVED***
      func.convert = createConverter(key, func);
      pairs.push([key, func]);
    ***REMOVED***
  ***REMOVED***);

  // Assign to `_` leaving `_.prototype` unchanged to allow chaining.
  each(pairs, function(pair) ***REMOVED***
    _[pair[0]] = pair[1];
  ***REMOVED***);

  _.convert = convertLib;
  if (setPlaceholder) ***REMOVED***
    _.placeholder = placeholder;
  ***REMOVED***
  // Assign aliases.
  each(keys(_), function(key) ***REMOVED***
    each(mapping.realToAlias[key] || [], function(alias) ***REMOVED***
      _[alias] = _[key];
    ***REMOVED***);
  ***REMOVED***);

  return _;
***REMOVED***

module.exports = baseConvert;

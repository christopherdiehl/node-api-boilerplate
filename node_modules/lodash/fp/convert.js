var baseConvert = require('./_baseConvert'),
    util = require('./_util');

/**
 * Converts `func` of `name` to an immutable auto-curried iteratee-first data-last
 * version with conversion `options` applied. If `name` is an object its methods
 * will be converted.
 *
 * @param ***REMOVED***string***REMOVED*** name The name of the function to wrap.
 * @param ***REMOVED***Function***REMOVED*** [func] The function to wrap.
 * @param ***REMOVED***Object***REMOVED*** [options] The options object. See `baseConvert` for more details.
 * @returns ***REMOVED***Function|Object***REMOVED*** Returns the converted function or object.
 */
function convert(name, func, options) ***REMOVED***
  return baseConvert(util, name, func, options);
***REMOVED***

module.exports = convert;

var baseIteratee = require('./_baseIteratee'),
    baseMean = require('./_baseMean');

/**
 * This method is like `_.mean` except that it accepts `iteratee` which is
 * invoked for each element in `array` to generate the value to be averaged.
 * The iteratee is invoked with one argument: (value).
 *
 * @static
 * @memberOf _
 * @since 4.7.0
 * @category Math
 * @param ***REMOVED***Array***REMOVED*** array The array to iterate over.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The iteratee invoked per element.
 * @returns ***REMOVED***number***REMOVED*** Returns the mean.
 * @example
 *
 * var objects = [***REMOVED*** 'n': 4 ***REMOVED***, ***REMOVED*** 'n': 2 ***REMOVED***, ***REMOVED*** 'n': 8 ***REMOVED***, ***REMOVED*** 'n': 6 ***REMOVED***];
 *
 * _.meanBy(objects, function(o) ***REMOVED*** return o.n; ***REMOVED***);
 * // => 5
 *
 * // The `_.property` iteratee shorthand.
 * _.meanBy(objects, 'n');
 * // => 5
 */
function meanBy(array, iteratee) ***REMOVED***
  return baseMean(array, baseIteratee(iteratee, 2));
***REMOVED***

module.exports = meanBy;

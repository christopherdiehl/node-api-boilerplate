var apply = require('./_apply'),
    arrayMap = require('./_arrayMap'),
    baseIteratee = require('./_baseIteratee'),
    baseRest = require('./_baseRest'),
    baseUnary = require('./_baseUnary'),
    flatRest = require('./_flatRest');

/**
 * Creates a function like `_.over`.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** arrayFunc The function to iterate over iteratees.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new over function.
 */
function createOver(arrayFunc) ***REMOVED***
  return flatRest(function(iteratees) ***REMOVED***
    iteratees = arrayMap(iteratees, baseUnary(baseIteratee));
    return baseRest(function(args) ***REMOVED***
      var thisArg = this;
      return arrayFunc(iteratees, function(iteratee) ***REMOVED***
        return apply(iteratee, thisArg, args);
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);
***REMOVED***

module.exports = createOver;

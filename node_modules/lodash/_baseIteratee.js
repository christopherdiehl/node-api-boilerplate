var baseMatches = require('./_baseMatches'),
    baseMatchesProperty = require('./_baseMatchesProperty'),
    identity = require('./identity'),
    isArray = require('./isArray'),
    property = require('./property');

/**
 * The base implementation of `_.iteratee`.
 *
 * @private
 * @param ***REMOVED*******REMOVED*** [value=_.identity] The value to convert to an iteratee.
 * @returns ***REMOVED***Function***REMOVED*** Returns the iteratee.
 */
function baseIteratee(value) ***REMOVED***
  // Don't store the `typeof` result in a variable to avoid a JIT bug in Safari 9.
  // See https://bugs.webkit.org/show_bug.cgi?id=156034 for more details.
  if (typeof value == 'function') ***REMOVED***
    return value;
  ***REMOVED***
  if (value == null) ***REMOVED***
    return identity;
  ***REMOVED***
  if (typeof value == 'object') ***REMOVED***
    return isArray(value)
      ? baseMatchesProperty(value[0], value[1])
      : baseMatches(value);
  ***REMOVED***
  return property(value);
***REMOVED***

module.exports = baseIteratee;

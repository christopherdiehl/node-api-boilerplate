/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/**
 * The base implementation of `_.delay` and `_.defer` which accepts `args`
 * to provide to `func`.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** func The function to delay.
 * @param ***REMOVED***number***REMOVED*** wait The number of milliseconds to delay invocation.
 * @param ***REMOVED***Array***REMOVED*** args The arguments to provide to `func`.
 * @returns ***REMOVED***number|Object***REMOVED*** Returns the timer id or timeout object.
 */
function baseDelay(func, wait, args) ***REMOVED***
  if (typeof func != 'function') ***REMOVED***
    throw new TypeError(FUNC_ERROR_TEXT);
  ***REMOVED***
  return setTimeout(function() ***REMOVED*** func.apply(undefined, args); ***REMOVED***, wait);
***REMOVED***

module.exports = baseDelay;

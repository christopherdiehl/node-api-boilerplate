var isSymbol = require('./isSymbol');

/** Used as references for various `Number` constants. */
var INFINITY = 1 / 0;

/**
 * Converts `value` to a string key if it's not a string or symbol.
 *
 * @private
 * @param ***REMOVED*******REMOVED*** value The value to inspect.
 * @returns ***REMOVED***string|symbol***REMOVED*** Returns the key.
 */
function toKey(value) ***REMOVED***
  if (typeof value == 'string' || isSymbol(value)) ***REMOVED***
    return value;
  ***REMOVED***
  var result = (value + '');
  return (result == '0' && (1 / value) == -INFINITY) ? '-0' : result;
***REMOVED***

module.exports = toKey;

var identity = require('./identity');

/**
 * Casts `value` to `identity` if it's not a function.
 *
 * @private
 * @param ***REMOVED*******REMOVED*** value The value to inspect.
 * @returns ***REMOVED***Function***REMOVED*** Returns cast function.
 */
function castFunction(value) ***REMOVED***
  return typeof value == 'function' ? value : identity;
***REMOVED***

module.exports = castFunction;

var copyArray = require('./_copyArray'),
    isIndex = require('./_isIndex');

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMin = Math.min;

/**
 * Reorder `array` according to the specified indexes where the element at
 * the first index is assigned as the first element, the element at
 * the second index is assigned as the second element, and so on.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to reorder.
 * @param ***REMOVED***Array***REMOVED*** indexes The arranged array indexes.
 * @returns ***REMOVED***Array***REMOVED*** Returns `array`.
 */
function reorder(array, indexes) ***REMOVED***
  var arrLength = array.length,
      length = nativeMin(indexes.length, arrLength),
      oldArray = copyArray(array);

  while (length--) ***REMOVED***
    var index = indexes[length];
    array[length] = isIndex(index, arrLength) ? oldArray[index] : undefined;
  ***REMOVED***
  return array;
***REMOVED***

module.exports = reorder;

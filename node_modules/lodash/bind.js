var baseRest = require('./_baseRest'),
    createWrap = require('./_createWrap'),
    getHolder = require('./_getHolder'),
    replaceHolders = require('./_replaceHolders');

/** Used to compose bitmasks for function metadata. */
var WRAP_BIND_FLAG = 1,
    WRAP_PARTIAL_FLAG = 32;

/**
 * Creates a function that invokes `func` with the `this` binding of `thisArg`
 * and `partials` prepended to the arguments it receives.
 *
 * The `_.bind.placeholder` value, which defaults to `_` in monolithic builds,
 * may be used as a placeholder for partially applied arguments.
 *
 * **Note:** Unlike native `Function#bind`, this method doesn't set the "length"
 * property of bound functions.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param ***REMOVED***Function***REMOVED*** func The function to bind.
 * @param ***REMOVED*******REMOVED*** thisArg The `this` binding of `func`.
 * @param ***REMOVED***...****REMOVED*** [partials] The arguments to be partially applied.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new bound function.
 * @example
 *
 * function greet(greeting, punctuation) ***REMOVED***
 *   return greeting + ' ' + this.user + punctuation;
 * ***REMOVED***
 *
 * var object = ***REMOVED*** 'user': 'fred' ***REMOVED***;
 *
 * var bound = _.bind(greet, object, 'hi');
 * bound('!');
 * // => 'hi fred!'
 *
 * // Bound with placeholders.
 * var bound = _.bind(greet, object, _, '!');
 * bound('hi');
 * // => 'hi fred!'
 */
var bind = baseRest(function(func, thisArg, partials) ***REMOVED***
  var bitmask = WRAP_BIND_FLAG;
  if (partials.length) ***REMOVED***
    var holders = replaceHolders(partials, getHolder(bind));
    bitmask |= WRAP_PARTIAL_FLAG;
  ***REMOVED***
  return createWrap(func, bitmask, thisArg, partials, holders);
***REMOVED***);

// Assign default placeholders.
bind.placeholder = ***REMOVED******REMOVED***;

module.exports = bind;

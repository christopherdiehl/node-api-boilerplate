var baseToPairs = require('./_baseToPairs'),
    getTag = require('./_getTag'),
    mapToArray = require('./_mapToArray'),
    setToPairs = require('./_setToPairs');

/** `Object#toString` result references. */
var mapTag = '[object Map]',
    setTag = '[object Set]';

/**
 * Creates a `_.toPairs` or `_.toPairsIn` function.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** keysFunc The function to get the keys of a given object.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new pairs function.
 */
function createToPairs(keysFunc) ***REMOVED***
  return function(object) ***REMOVED***
    var tag = getTag(object);
    if (tag == mapTag) ***REMOVED***
      return mapToArray(object);
    ***REMOVED***
    if (tag == setTag) ***REMOVED***
      return setToPairs(object);
    ***REMOVED***
    return baseToPairs(object, keysFunc(object));
  ***REMOVED***;
***REMOVED***

module.exports = createToPairs;

var baseSlice = require('./_baseSlice'),
    isIterateeCall = require('./_isIterateeCall'),
    toInteger = require('./toInteger');

/**
 * Creates a slice of `array` from `start` up to, but not including, `end`.
 *
 * **Note:** This method is used instead of
 * [`Array#slice`](https://mdn.io/Array/slice) to ensure dense arrays are
 * returned.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Array
 * @param ***REMOVED***Array***REMOVED*** array The array to slice.
 * @param ***REMOVED***number***REMOVED*** [start=0] The start position.
 * @param ***REMOVED***number***REMOVED*** [end=array.length] The end position.
 * @returns ***REMOVED***Array***REMOVED*** Returns the slice of `array`.
 */
function slice(array, start, end) ***REMOVED***
  var length = array == null ? 0 : array.length;
  if (!length) ***REMOVED***
    return [];
  ***REMOVED***
  if (end && typeof end != 'number' && isIterateeCall(array, start, end)) ***REMOVED***
    start = 0;
    end = length;
  ***REMOVED***
  else ***REMOVED***
    start = start == null ? 0 : toInteger(start);
    end = end === undefined ? length : toInteger(end);
  ***REMOVED***
  return baseSlice(array, start, end);
***REMOVED***

module.exports = slice;

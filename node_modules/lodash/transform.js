var arrayEach = require('./_arrayEach'),
    baseCreate = require('./_baseCreate'),
    baseForOwn = require('./_baseForOwn'),
    baseIteratee = require('./_baseIteratee'),
    getPrototype = require('./_getPrototype'),
    isArray = require('./isArray'),
    isBuffer = require('./isBuffer'),
    isFunction = require('./isFunction'),
    isObject = require('./isObject'),
    isTypedArray = require('./isTypedArray');

/**
 * An alternative to `_.reduce`; this method transforms `object` to a new
 * `accumulator` object which is the result of running each of its own
 * enumerable string keyed properties thru `iteratee`, with each invocation
 * potentially mutating the `accumulator` object. If `accumulator` is not
 * provided, a new object with the same `[[Prototype]]` will be used. The
 * iteratee is invoked with four arguments: (accumulator, value, key, object).
 * Iteratee functions may exit iteration early by explicitly returning `false`.
 *
 * @static
 * @memberOf _
 * @since 1.3.0
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The object to iterate over.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The function invoked per iteration.
 * @param ***REMOVED*******REMOVED*** [accumulator] The custom accumulator value.
 * @returns ***REMOVED*******REMOVED*** Returns the accumulated value.
 * @example
 *
 * _.transform([2, 3, 4], function(result, n) ***REMOVED***
 *   result.push(n *= n);
 *   return n % 2 == 0;
 * ***REMOVED***, []);
 * // => [4, 9]
 *
 * _.transform(***REMOVED*** 'a': 1, 'b': 2, 'c': 1 ***REMOVED***, function(result, value, key) ***REMOVED***
 *   (result[value] || (result[value] = [])).push(key);
 * ***REMOVED***, ***REMOVED******REMOVED***);
 * // => ***REMOVED*** '1': ['a', 'c'], '2': ['b'] ***REMOVED***
 */
function transform(object, iteratee, accumulator) ***REMOVED***
  var isArr = isArray(object),
      isArrLike = isArr || isBuffer(object) || isTypedArray(object);

  iteratee = baseIteratee(iteratee, 4);
  if (accumulator == null) ***REMOVED***
    var Ctor = object && object.constructor;
    if (isArrLike) ***REMOVED***
      accumulator = isArr ? new Ctor : [];
    ***REMOVED***
    else if (isObject(object)) ***REMOVED***
      accumulator = isFunction(Ctor) ? baseCreate(getPrototype(object)) : ***REMOVED******REMOVED***;
    ***REMOVED***
    else ***REMOVED***
      accumulator = ***REMOVED******REMOVED***;
    ***REMOVED***
  ***REMOVED***
  (isArrLike ? arrayEach : baseForOwn)(object, function(value, index, object) ***REMOVED***
    return iteratee(accumulator, value, index, object);
  ***REMOVED***);
  return accumulator;
***REMOVED***

module.exports = transform;

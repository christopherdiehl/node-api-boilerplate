/**
 * This method is like `_.tap` except that it returns the result of `interceptor`.
 * The purpose of this method is to "pass thru" values replacing intermediate
 * results in a method chain sequence.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Seq
 * @param ***REMOVED*******REMOVED*** value The value to provide to `interceptor`.
 * @param ***REMOVED***Function***REMOVED*** interceptor The function to invoke.
 * @returns ***REMOVED*******REMOVED*** Returns the result of `interceptor`.
 * @example
 *
 * _('  abc  ')
 *  .chain()
 *  .trim()
 *  .thru(function(value) ***REMOVED***
 *    return [value];
 *  ***REMOVED***)
 *  .value();
 * // => ['abc']
 */
function thru(value, interceptor) ***REMOVED***
  return interceptor(value);
***REMOVED***

module.exports = thru;

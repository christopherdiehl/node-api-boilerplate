/** Used to match wrap detail comments. */
var reWrapComment = /\***REMOVED***(?:\n\/\* \[wrapped with .+\] \*\/)?\n?/;

/**
 * Inserts wrapper `details` in a comment at the top of the `source` body.
 *
 * @private
 * @param ***REMOVED***string***REMOVED*** source The source to modify.
 * @returns ***REMOVED***Array***REMOVED*** details The details to insert.
 * @returns ***REMOVED***string***REMOVED*** Returns the modified source.
 */
function insertWrapDetails(source, details) ***REMOVED***
  var length = details.length;
  if (!length) ***REMOVED***
    return source;
  ***REMOVED***
  var lastIndex = length - 1;
  details[lastIndex] = (length > 1 ? '& ' : '') + details[lastIndex];
  details = details.join(length > 2 ? ', ' : ' ');
  return source.replace(reWrapComment, '***REMOVED***\n/* [wrapped with ' + details + '] */\n');
***REMOVED***

module.exports = insertWrapDetails;

var arrayFilter = require('./_arrayFilter'),
    isFunction = require('./isFunction');

/**
 * The base implementation of `_.functions` which creates an array of
 * `object` function property names filtered from `props`.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** object The object to inspect.
 * @param ***REMOVED***Array***REMOVED*** props The property names to filter.
 * @returns ***REMOVED***Array***REMOVED*** Returns the function names.
 */
function baseFunctions(object, props) ***REMOVED***
  return arrayFilter(props, function(key) ***REMOVED***
    return isFunction(object[key]);
  ***REMOVED***);
***REMOVED***

module.exports = baseFunctions;

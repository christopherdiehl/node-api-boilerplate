var toString = require('./toString');

/** Used to generate unique IDs. */
var idCounter = 0;

/**
 * Generates a unique ID. If `prefix` is given, the ID is appended to it.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Util
 * @param ***REMOVED***string***REMOVED*** [prefix=''] The value to prefix the ID with.
 * @returns ***REMOVED***string***REMOVED*** Returns the unique ID.
 * @example
 *
 * _.uniqueId('contact_');
 * // => 'contact_104'
 *
 * _.uniqueId();
 * // => '105'
 */
function uniqueId(prefix) ***REMOVED***
  var id = ++idCounter;
  return toString(prefix) + id;
***REMOVED***

module.exports = uniqueId;

var arrayEach = require('./_arrayEach'),
    baseAssignValue = require('./_baseAssignValue'),
    bind = require('./bind'),
    flatRest = require('./_flatRest'),
    toKey = require('./_toKey');

/**
 * Binds methods of an object to the object itself, overwriting the existing
 * method.
 *
 * **Note:** This method doesn't set the "length" property of bound functions.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Util
 * @param ***REMOVED***Object***REMOVED*** object The object to bind and assign the bound methods to.
 * @param ***REMOVED***...(string|string[])***REMOVED*** methodNames The object method names to bind.
 * @returns ***REMOVED***Object***REMOVED*** Returns `object`.
 * @example
 *
 * var view = ***REMOVED***
 *   'label': 'docs',
 *   'click': function() ***REMOVED***
 *     console.log('clicked ' + this.label);
 *   ***REMOVED***
 * ***REMOVED***;
 *
 * _.bindAll(view, ['click']);
 * jQuery(element).on('click', view.click);
 * // => Logs 'clicked docs' when clicked.
 */
var bindAll = flatRest(function(object, methodNames) ***REMOVED***
  arrayEach(methodNames, function(key) ***REMOVED***
    key = toKey(key);
    baseAssignValue(object, key, bind(object[key], object));
  ***REMOVED***);
  return object;
***REMOVED***);

module.exports = bindAll;

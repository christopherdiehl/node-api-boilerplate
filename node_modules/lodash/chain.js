var lodash = require('./wrapperLodash');

/**
 * Creates a `lodash` wrapper instance that wraps `value` with explicit method
 * chain sequences enabled. The result of such sequences must be unwrapped
 * with `_#value`.
 *
 * @static
 * @memberOf _
 * @since 1.3.0
 * @category Seq
 * @param ***REMOVED*******REMOVED*** value The value to wrap.
 * @returns ***REMOVED***Object***REMOVED*** Returns the new `lodash` wrapper instance.
 * @example
 *
 * var users = [
 *   ***REMOVED*** 'user': 'barney',  'age': 36 ***REMOVED***,
 *   ***REMOVED*** 'user': 'fred',    'age': 40 ***REMOVED***,
 *   ***REMOVED*** 'user': 'pebbles', 'age': 1 ***REMOVED***
 * ];
 *
 * var youngest = _
 *   .chain(users)
 *   .sortBy('age')
 *   .map(function(o) ***REMOVED***
 *     return o.user + ' is ' + o.age;
 *   ***REMOVED***)
 *   .head()
 *   .value();
 * // => 'pebbles is 1'
 */
function chain(value) ***REMOVED***
  var result = lodash(value);
  result.__chain__ = true;
  return result;
***REMOVED***

module.exports = chain;

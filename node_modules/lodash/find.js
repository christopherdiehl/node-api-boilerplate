var createFind = require('./_createFind'),
    findIndex = require('./findIndex');

/**
 * Iterates over elements of `collection`, returning the first element
 * `predicate` returns truthy for. The predicate is invoked with three
 * arguments: (value, index|key, collection).
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Collection
 * @param ***REMOVED***Array|Object***REMOVED*** collection The collection to inspect.
 * @param ***REMOVED***Function***REMOVED*** [predicate=_.identity] The function invoked per iteration.
 * @param ***REMOVED***number***REMOVED*** [fromIndex=0] The index to search from.
 * @returns ***REMOVED*******REMOVED*** Returns the matched element, else `undefined`.
 * @example
 *
 * var users = [
 *   ***REMOVED*** 'user': 'barney',  'age': 36, 'active': true ***REMOVED***,
 *   ***REMOVED*** 'user': 'fred',    'age': 40, 'active': false ***REMOVED***,
 *   ***REMOVED*** 'user': 'pebbles', 'age': 1,  'active': true ***REMOVED***
 * ];
 *
 * _.find(users, function(o) ***REMOVED*** return o.age < 40; ***REMOVED***);
 * // => object for 'barney'
 *
 * // The `_.matches` iteratee shorthand.
 * _.find(users, ***REMOVED*** 'age': 1, 'active': true ***REMOVED***);
 * // => object for 'pebbles'
 *
 * // The `_.matchesProperty` iteratee shorthand.
 * _.find(users, ['active', false]);
 * // => object for 'fred'
 *
 * // The `_.property` iteratee shorthand.
 * _.find(users, 'active');
 * // => object for 'barney'
 */
var find = createFind(findIndex);

module.exports = find;

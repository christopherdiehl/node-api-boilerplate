var baseRepeat = require('./_baseRepeat'),
    isIterateeCall = require('./_isIterateeCall'),
    toInteger = require('./toInteger'),
    toString = require('./toString');

/**
 * Repeats the given string `n` times.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category String
 * @param ***REMOVED***string***REMOVED*** [string=''] The string to repeat.
 * @param ***REMOVED***number***REMOVED*** [n=1] The number of times to repeat the string.
 * @param- ***REMOVED***Object***REMOVED*** [guard] Enables use as an iteratee for methods like `_.map`.
 * @returns ***REMOVED***string***REMOVED*** Returns the repeated string.
 * @example
 *
 * _.repeat('*', 3);
 * // => '***'
 *
 * _.repeat('abc', 2);
 * // => 'abcabc'
 *
 * _.repeat('abc', 0);
 * // => ''
 */
function repeat(string, n, guard) ***REMOVED***
  if ((guard ? isIterateeCall(string, n, guard) : n === undefined)) ***REMOVED***
    n = 1;
  ***REMOVED*** else ***REMOVED***
    n = toInteger(n);
  ***REMOVED***
  return baseRepeat(toString(string), n);
***REMOVED***

module.exports = repeat;

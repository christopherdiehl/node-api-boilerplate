var arrayAggregator = require('./_arrayAggregator'),
    baseAggregator = require('./_baseAggregator'),
    baseIteratee = require('./_baseIteratee'),
    isArray = require('./isArray');

/**
 * Creates a function like `_.groupBy`.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** setter The function to set accumulator values.
 * @param ***REMOVED***Function***REMOVED*** [initializer] The accumulator object initializer.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new aggregator function.
 */
function createAggregator(setter, initializer) ***REMOVED***
  return function(collection, iteratee) ***REMOVED***
    var func = isArray(collection) ? arrayAggregator : baseAggregator,
        accumulator = initializer ? initializer() : ***REMOVED******REMOVED***;

    return func(collection, setter, baseIteratee(iteratee, 2), accumulator);
  ***REMOVED***;
***REMOVED***

module.exports = createAggregator;

var baseSortedIndex = require('./_baseSortedIndex'),
    eq = require('./eq');

/**
 * This method is like `_.indexOf` except that it performs a binary
 * search on a sorted `array`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param ***REMOVED***Array***REMOVED*** array The array to inspect.
 * @param ***REMOVED*******REMOVED*** value The value to search for.
 * @returns ***REMOVED***number***REMOVED*** Returns the index of the matched value, else `-1`.
 * @example
 *
 * _.sortedIndexOf([4, 5, 5, 5, 6], 5);
 * // => 1
 */
function sortedIndexOf(array, value) ***REMOVED***
  var length = array == null ? 0 : array.length;
  if (length) ***REMOVED***
    var index = baseSortedIndex(array, value);
    if (index < length && eq(array[index], value)) ***REMOVED***
      return index;
    ***REMOVED***
  ***REMOVED***
  return -1;
***REMOVED***

module.exports = sortedIndexOf;

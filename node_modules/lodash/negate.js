/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/**
 * Creates a function that negates the result of the predicate `func`. The
 * `func` predicate is invoked with the `this` binding and arguments of the
 * created function.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Function
 * @param ***REMOVED***Function***REMOVED*** predicate The predicate to negate.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new negated function.
 * @example
 *
 * function isEven(n) ***REMOVED***
 *   return n % 2 == 0;
 * ***REMOVED***
 *
 * _.filter([1, 2, 3, 4, 5, 6], _.negate(isEven));
 * // => [1, 3, 5]
 */
function negate(predicate) ***REMOVED***
  if (typeof predicate != 'function') ***REMOVED***
    throw new TypeError(FUNC_ERROR_TEXT);
  ***REMOVED***
  return function() ***REMOVED***
    var args = arguments;
    switch (args.length) ***REMOVED***
      case 0: return !predicate.call(this);
      case 1: return !predicate.call(this, args[0]);
      case 2: return !predicate.call(this, args[0], args[1]);
      case 3: return !predicate.call(this, args[0], args[1], args[2]);
    ***REMOVED***
    return !predicate.apply(this, args);
  ***REMOVED***;
***REMOVED***

module.exports = negate;

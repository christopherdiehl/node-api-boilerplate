var baseFlatten = require('./_baseFlatten'),
    baseIteratee = require('./_baseIteratee'),
    baseRest = require('./_baseRest'),
    baseUniq = require('./_baseUniq'),
    isArrayLikeObject = require('./isArrayLikeObject'),
    last = require('./last');

/**
 * This method is like `_.union` except that it accepts `iteratee` which is
 * invoked for each element of each `arrays` to generate the criterion by
 * which uniqueness is computed. Result values are chosen from the first
 * array in which the value occurs. The iteratee is invoked with one argument:
 * (value).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param ***REMOVED***...Array***REMOVED*** [arrays] The arrays to inspect.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The iteratee invoked per element.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new array of combined values.
 * @example
 *
 * _.unionBy([2.1], [1.2, 2.3], Math.floor);
 * // => [2.1, 1.2]
 *
 * // The `_.property` iteratee shorthand.
 * _.unionBy([***REMOVED*** 'x': 1 ***REMOVED***], [***REMOVED*** 'x': 2 ***REMOVED***, ***REMOVED*** 'x': 1 ***REMOVED***], 'x');
 * // => [***REMOVED*** 'x': 1 ***REMOVED***, ***REMOVED*** 'x': 2 ***REMOVED***]
 */
var unionBy = baseRest(function(arrays) ***REMOVED***
  var iteratee = last(arrays);
  if (isArrayLikeObject(iteratee)) ***REMOVED***
    iteratee = undefined;
  ***REMOVED***
  return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true), baseIteratee(iteratee, 2));
***REMOVED***);

module.exports = unionBy;

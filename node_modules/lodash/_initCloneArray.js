/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Initializes an array clone.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to clone.
 * @returns ***REMOVED***Array***REMOVED*** Returns the initialized clone.
 */
function initCloneArray(array) ***REMOVED***
  var length = array.length,
      result = array.constructor(length);

  // Add properties assigned by `RegExp#exec`.
  if (length && typeof array[0] == 'string' && hasOwnProperty.call(array, 'index')) ***REMOVED***
    result.index = array.index;
    result.input = array.input;
  ***REMOVED***
  return result;
***REMOVED***

module.exports = initCloneArray;

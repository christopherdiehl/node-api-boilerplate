var baseIteratee = require('./_baseIteratee'),
    baseUniq = require('./_baseUniq');

/**
 * This method is like `_.uniq` except that it accepts `iteratee` which is
 * invoked for each element in `array` to generate the criterion by which
 * uniqueness is computed. The order of result values is determined by the
 * order they occur in the array. The iteratee is invoked with one argument:
 * (value).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param ***REMOVED***Array***REMOVED*** array The array to inspect.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The iteratee invoked per element.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new duplicate free array.
 * @example
 *
 * _.uniqBy([2.1, 1.2, 2.3], Math.floor);
 * // => [2.1, 1.2]
 *
 * // The `_.property` iteratee shorthand.
 * _.uniqBy([***REMOVED*** 'x': 1 ***REMOVED***, ***REMOVED*** 'x': 2 ***REMOVED***, ***REMOVED*** 'x': 1 ***REMOVED***], 'x');
 * // => [***REMOVED*** 'x': 1 ***REMOVED***, ***REMOVED*** 'x': 2 ***REMOVED***]
 */
function uniqBy(array, iteratee) ***REMOVED***
  return (array && array.length) ? baseUniq(array, baseIteratee(iteratee, 2)) : [];
***REMOVED***

module.exports = uniqBy;

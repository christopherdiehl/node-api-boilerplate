var isArray = require('./isArray');

/**
 * Casts `value` as an array if it's not one.
 *
 * @static
 * @memberOf _
 * @since 4.4.0
 * @category Lang
 * @param ***REMOVED*******REMOVED*** value The value to inspect.
 * @returns ***REMOVED***Array***REMOVED*** Returns the cast array.
 * @example
 *
 * _.castArray(1);
 * // => [1]
 *
 * _.castArray(***REMOVED*** 'a': 1 ***REMOVED***);
 * // => [***REMOVED*** 'a': 1 ***REMOVED***]
 *
 * _.castArray('abc');
 * // => ['abc']
 *
 * _.castArray(null);
 * // => [null]
 *
 * _.castArray(undefined);
 * // => [undefined]
 *
 * _.castArray();
 * // => []
 *
 * var array = [1, 2, 3];
 * console.log(_.castArray(array) === array);
 * // => true
 */
function castArray() ***REMOVED***
  if (!arguments.length) ***REMOVED***
    return [];
  ***REMOVED***
  var value = arguments[0];
  return isArray(value) ? value : [value];
***REMOVED***

module.exports = castArray;

var assignValue = require('./_assignValue'),
    copyObject = require('./_copyObject'),
    createAssigner = require('./_createAssigner'),
    isArrayLike = require('./isArrayLike'),
    isPrototype = require('./_isPrototype'),
    keys = require('./keys');

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Assigns own enumerable string keyed properties of source objects to the
 * destination object. Source objects are applied from left to right.
 * Subsequent sources overwrite property assignments of previous sources.
 *
 * **Note:** This method mutates `object` and is loosely based on
 * [`Object.assign`](https://mdn.io/Object/assign).
 *
 * @static
 * @memberOf _
 * @since 0.10.0
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The destination object.
 * @param ***REMOVED***...Object***REMOVED*** [sources] The source objects.
 * @returns ***REMOVED***Object***REMOVED*** Returns `object`.
 * @see _.assignIn
 * @example
 *
 * function Foo() ***REMOVED***
 *   this.a = 1;
 * ***REMOVED***
 *
 * function Bar() ***REMOVED***
 *   this.c = 3;
 * ***REMOVED***
 *
 * Foo.prototype.b = 2;
 * Bar.prototype.d = 4;
 *
 * _.assign(***REMOVED*** 'a': 0 ***REMOVED***, new Foo, new Bar);
 * // => ***REMOVED*** 'a': 1, 'c': 3 ***REMOVED***
 */
var assign = createAssigner(function(object, source) ***REMOVED***
  if (isPrototype(source) || isArrayLike(source)) ***REMOVED***
    copyObject(source, keys(source), object);
    return;
  ***REMOVED***
  for (var key in source) ***REMOVED***
    if (hasOwnProperty.call(source, key)) ***REMOVED***
      assignValue(object, key, source[key]);
    ***REMOVED***
  ***REMOVED***
***REMOVED***);

module.exports = assign;

var Symbol = require('./_Symbol');

/** Used to convert symbols to primitives and strings. */
var symbolProto = Symbol ? Symbol.prototype : undefined,
    symbolValueOf = symbolProto ? symbolProto.valueOf : undefined;

/**
 * Creates a clone of the `symbol` object.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** symbol The symbol object to clone.
 * @returns ***REMOVED***Object***REMOVED*** Returns the cloned symbol object.
 */
function cloneSymbol(symbol) ***REMOVED***
  return symbolValueOf ? Object(symbolValueOf.call(symbol)) : ***REMOVED******REMOVED***;
***REMOVED***

module.exports = cloneSymbol;

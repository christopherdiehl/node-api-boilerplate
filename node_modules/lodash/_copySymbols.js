var copyObject = require('./_copyObject'),
    getSymbols = require('./_getSymbols');

/**
 * Copies own symbols of `source` to `object`.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** source The object to copy symbols from.
 * @param ***REMOVED***Object***REMOVED*** [object=***REMOVED******REMOVED***] The object to copy symbols to.
 * @returns ***REMOVED***Object***REMOVED*** Returns `object`.
 */
function copySymbols(source, object) ***REMOVED***
  return copyObject(source, getSymbols(source), object);
***REMOVED***

module.exports = copySymbols;

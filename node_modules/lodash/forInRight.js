var baseForRight = require('./_baseForRight'),
    castFunction = require('./_castFunction'),
    keysIn = require('./keysIn');

/**
 * This method is like `_.forIn` except that it iterates over properties of
 * `object` in the opposite order.
 *
 * @static
 * @memberOf _
 * @since 2.0.0
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The object to iterate over.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The function invoked per iteration.
 * @returns ***REMOVED***Object***REMOVED*** Returns `object`.
 * @see _.forIn
 * @example
 *
 * function Foo() ***REMOVED***
 *   this.a = 1;
 *   this.b = 2;
 * ***REMOVED***
 *
 * Foo.prototype.c = 3;
 *
 * _.forInRight(new Foo, function(value, key) ***REMOVED***
 *   console.log(key);
 * ***REMOVED***);
 * // => Logs 'c', 'b', then 'a' assuming `_.forIn` logs 'a', 'b', then 'c'.
 */
function forInRight(object, iteratee) ***REMOVED***
  return object == null
    ? object
    : baseForRight(object, castFunction(iteratee), keysIn);
***REMOVED***

module.exports = forInRight;

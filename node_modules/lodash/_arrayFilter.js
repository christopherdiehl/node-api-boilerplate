/**
 * A specialized version of `_.filter` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** [array] The array to iterate over.
 * @param ***REMOVED***Function***REMOVED*** predicate The function invoked per iteration.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new filtered array.
 */
function arrayFilter(array, predicate) ***REMOVED***
  var index = -1,
      length = array == null ? 0 : array.length,
      resIndex = 0,
      result = [];

  while (++index < length) ***REMOVED***
    var value = array[index];
    if (predicate(value, index, array)) ***REMOVED***
      result[resIndex++] = value;
    ***REMOVED***
  ***REMOVED***
  return result;
***REMOVED***

module.exports = arrayFilter;

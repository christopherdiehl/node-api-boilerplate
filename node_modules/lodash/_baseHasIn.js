/**
 * The base implementation of `_.hasIn` without support for deep paths.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** [object] The object to query.
 * @param ***REMOVED***Array|string***REMOVED*** key The key to check.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if `key` exists, else `false`.
 */
function baseHasIn(object, key) ***REMOVED***
  return object != null && key in Object(object);
***REMOVED***

module.exports = baseHasIn;

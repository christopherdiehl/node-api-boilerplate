var baseRandom = require('./_baseRandom'),
    isIterateeCall = require('./_isIterateeCall'),
    toFinite = require('./toFinite');

/** Built-in method references without a dependency on `root`. */
var freeParseFloat = parseFloat;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMin = Math.min,
    nativeRandom = Math.random;

/**
 * Produces a random number between the inclusive `lower` and `upper` bounds.
 * If only one argument is provided a number between `0` and the given number
 * is returned. If `floating` is `true`, or either `lower` or `upper` are
 * floats, a floating-point number is returned instead of an integer.
 *
 * **Note:** JavaScript follows the IEEE-754 standard for resolving
 * floating-point values which can produce unexpected results.
 *
 * @static
 * @memberOf _
 * @since 0.7.0
 * @category Number
 * @param ***REMOVED***number***REMOVED*** [lower=0] The lower bound.
 * @param ***REMOVED***number***REMOVED*** [upper=1] The upper bound.
 * @param ***REMOVED***boolean***REMOVED*** [floating] Specify returning a floating-point number.
 * @returns ***REMOVED***number***REMOVED*** Returns the random number.
 * @example
 *
 * _.random(0, 5);
 * // => an integer between 0 and 5
 *
 * _.random(5);
 * // => also an integer between 0 and 5
 *
 * _.random(5, true);
 * // => a floating-point number between 0 and 5
 *
 * _.random(1.2, 5.2);
 * // => a floating-point number between 1.2 and 5.2
 */
function random(lower, upper, floating) ***REMOVED***
  if (floating && typeof floating != 'boolean' && isIterateeCall(lower, upper, floating)) ***REMOVED***
    upper = floating = undefined;
  ***REMOVED***
  if (floating === undefined) ***REMOVED***
    if (typeof upper == 'boolean') ***REMOVED***
      floating = upper;
      upper = undefined;
    ***REMOVED***
    else if (typeof lower == 'boolean') ***REMOVED***
      floating = lower;
      lower = undefined;
    ***REMOVED***
  ***REMOVED***
  if (lower === undefined && upper === undefined) ***REMOVED***
    lower = 0;
    upper = 1;
  ***REMOVED***
  else ***REMOVED***
    lower = toFinite(lower);
    if (upper === undefined) ***REMOVED***
      upper = lower;
      lower = 0;
    ***REMOVED*** else ***REMOVED***
      upper = toFinite(upper);
    ***REMOVED***
  ***REMOVED***
  if (lower > upper) ***REMOVED***
    var temp = lower;
    lower = upper;
    upper = temp;
  ***REMOVED***
  if (floating || lower % 1 || upper % 1) ***REMOVED***
    var rand = nativeRandom();
    return nativeMin(lower + (rand * (upper - lower + freeParseFloat('1e-' + ((rand + '').length - 1)))), upper);
  ***REMOVED***
  return baseRandom(lower, upper);
***REMOVED***

module.exports = random;

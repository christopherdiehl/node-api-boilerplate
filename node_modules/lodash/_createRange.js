var baseRange = require('./_baseRange'),
    isIterateeCall = require('./_isIterateeCall'),
    toFinite = require('./toFinite');

/**
 * Creates a `_.range` or `_.rangeRight` function.
 *
 * @private
 * @param ***REMOVED***boolean***REMOVED*** [fromRight] Specify iterating from right to left.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new range function.
 */
function createRange(fromRight) ***REMOVED***
  return function(start, end, step) ***REMOVED***
    if (step && typeof step != 'number' && isIterateeCall(start, end, step)) ***REMOVED***
      end = step = undefined;
    ***REMOVED***
    // Ensure the sign of `-0` is preserved.
    start = toFinite(start);
    if (end === undefined) ***REMOVED***
      end = start;
      start = 0;
    ***REMOVED*** else ***REMOVED***
      end = toFinite(end);
    ***REMOVED***
    step = step === undefined ? (start < end ? 1 : -1) : toFinite(step);
    return baseRange(start, end, step, fromRight);
  ***REMOVED***;
***REMOVED***

module.exports = createRange;

var SetCache = require('./_SetCache'),
    arrayIncludes = require('./_arrayIncludes'),
    arrayIncludesWith = require('./_arrayIncludesWith'),
    cacheHas = require('./_cacheHas'),
    createSet = require('./_createSet'),
    setToArray = require('./_setToArray');

/** Used as the size to enable large array optimizations. */
var LARGE_ARRAY_SIZE = 200;

/**
 * The base implementation of `_.uniqBy` without support for iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to inspect.
 * @param ***REMOVED***Function***REMOVED*** [iteratee] The iteratee invoked per element.
 * @param ***REMOVED***Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new duplicate free array.
 */
function baseUniq(array, iteratee, comparator) ***REMOVED***
  var index = -1,
      includes = arrayIncludes,
      length = array.length,
      isCommon = true,
      result = [],
      seen = result;

  if (comparator) ***REMOVED***
    isCommon = false;
    includes = arrayIncludesWith;
  ***REMOVED***
  else if (length >= LARGE_ARRAY_SIZE) ***REMOVED***
    var set = iteratee ? null : createSet(array);
    if (set) ***REMOVED***
      return setToArray(set);
    ***REMOVED***
    isCommon = false;
    includes = cacheHas;
    seen = new SetCache;
  ***REMOVED***
  else ***REMOVED***
    seen = iteratee ? [] : result;
  ***REMOVED***
  outer:
  while (++index < length) ***REMOVED***
    var value = array[index],
        computed = iteratee ? iteratee(value) : value;

    value = (comparator || value !== 0) ? value : 0;
    if (isCommon && computed === computed) ***REMOVED***
      var seenIndex = seen.length;
      while (seenIndex--) ***REMOVED***
        if (seen[seenIndex] === computed) ***REMOVED***
          continue outer;
        ***REMOVED***
      ***REMOVED***
      if (iteratee) ***REMOVED***
        seen.push(computed);
      ***REMOVED***
      result.push(value);
    ***REMOVED***
    else if (!includes(seen, computed, comparator)) ***REMOVED***
      if (seen !== result) ***REMOVED***
        seen.push(computed);
      ***REMOVED***
      result.push(value);
    ***REMOVED***
  ***REMOVED***
  return result;
***REMOVED***

module.exports = baseUniq;

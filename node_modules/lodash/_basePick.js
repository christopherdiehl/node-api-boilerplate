var basePickBy = require('./_basePickBy'),
    hasIn = require('./hasIn');

/**
 * The base implementation of `_.pick` without support for individual
 * property identifiers.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** object The source object.
 * @param ***REMOVED***string[]***REMOVED*** paths The property paths to pick.
 * @returns ***REMOVED***Object***REMOVED*** Returns the new object.
 */
function basePick(object, paths) ***REMOVED***
  return basePickBy(object, paths, function(value, path) ***REMOVED***
    return hasIn(object, path);
  ***REMOVED***);
***REMOVED***

module.exports = basePick;

var baseIteratee = require('./_baseIteratee'),
    baseWhile = require('./_baseWhile');

/**
 * Creates a slice of `array` excluding elements dropped from the beginning.
 * Elements are dropped until `predicate` returns falsey. The predicate is
 * invoked with three arguments: (value, index, array).
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Array
 * @param ***REMOVED***Array***REMOVED*** array The array to query.
 * @param ***REMOVED***Function***REMOVED*** [predicate=_.identity] The function invoked per iteration.
 * @returns ***REMOVED***Array***REMOVED*** Returns the slice of `array`.
 * @example
 *
 * var users = [
 *   ***REMOVED*** 'user': 'barney',  'active': false ***REMOVED***,
 *   ***REMOVED*** 'user': 'fred',    'active': false ***REMOVED***,
 *   ***REMOVED*** 'user': 'pebbles', 'active': true ***REMOVED***
 * ];
 *
 * _.dropWhile(users, function(o) ***REMOVED*** return !o.active; ***REMOVED***);
 * // => objects for ['pebbles']
 *
 * // The `_.matches` iteratee shorthand.
 * _.dropWhile(users, ***REMOVED*** 'user': 'barney', 'active': false ***REMOVED***);
 * // => objects for ['fred', 'pebbles']
 *
 * // The `_.matchesProperty` iteratee shorthand.
 * _.dropWhile(users, ['active', false]);
 * // => objects for ['pebbles']
 *
 * // The `_.property` iteratee shorthand.
 * _.dropWhile(users, 'active');
 * // => objects for ['barney', 'fred', 'pebbles']
 */
function dropWhile(array, predicate) ***REMOVED***
  return (array && array.length)
    ? baseWhile(array, baseIteratee(predicate, 3), true)
    : [];
***REMOVED***

module.exports = dropWhile;

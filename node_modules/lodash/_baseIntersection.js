var SetCache = require('./_SetCache'),
    arrayIncludes = require('./_arrayIncludes'),
    arrayIncludesWith = require('./_arrayIncludesWith'),
    arrayMap = require('./_arrayMap'),
    baseUnary = require('./_baseUnary'),
    cacheHas = require('./_cacheHas');

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMin = Math.min;

/**
 * The base implementation of methods like `_.intersection`, without support
 * for iteratee shorthands, that accepts an array of arrays to inspect.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** arrays The arrays to inspect.
 * @param ***REMOVED***Function***REMOVED*** [iteratee] The iteratee invoked per element.
 * @param ***REMOVED***Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new array of shared values.
 */
function baseIntersection(arrays, iteratee, comparator) ***REMOVED***
  var includes = comparator ? arrayIncludesWith : arrayIncludes,
      length = arrays[0].length,
      othLength = arrays.length,
      othIndex = othLength,
      caches = Array(othLength),
      maxLength = Infinity,
      result = [];

  while (othIndex--) ***REMOVED***
    var array = arrays[othIndex];
    if (othIndex && iteratee) ***REMOVED***
      array = arrayMap(array, baseUnary(iteratee));
    ***REMOVED***
    maxLength = nativeMin(array.length, maxLength);
    caches[othIndex] = !comparator && (iteratee || (length >= 120 && array.length >= 120))
      ? new SetCache(othIndex && array)
      : undefined;
  ***REMOVED***
  array = arrays[0];

  var index = -1,
      seen = caches[0];

  outer:
  while (++index < length && result.length < maxLength) ***REMOVED***
    var value = array[index],
        computed = iteratee ? iteratee(value) : value;

    value = (comparator || value !== 0) ? value : 0;
    if (!(seen
          ? cacheHas(seen, computed)
          : includes(result, computed, comparator)
        )) ***REMOVED***
      othIndex = othLength;
      while (--othIndex) ***REMOVED***
        var cache = caches[othIndex];
        if (!(cache
              ? cacheHas(cache, computed)
              : includes(arrays[othIndex], computed, comparator))
            ) ***REMOVED***
          continue outer;
        ***REMOVED***
      ***REMOVED***
      if (seen) ***REMOVED***
        seen.push(computed);
      ***REMOVED***
      result.push(value);
    ***REMOVED***
  ***REMOVED***
  return result;
***REMOVED***

module.exports = baseIntersection;

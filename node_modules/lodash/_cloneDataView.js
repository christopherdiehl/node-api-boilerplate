var cloneArrayBuffer = require('./_cloneArrayBuffer');

/**
 * Creates a clone of `dataView`.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** dataView The data view to clone.
 * @param ***REMOVED***boolean***REMOVED*** [isDeep] Specify a deep clone.
 * @returns ***REMOVED***Object***REMOVED*** Returns the cloned data view.
 */
function cloneDataView(dataView, isDeep) ***REMOVED***
  var buffer = isDeep ? cloneArrayBuffer(dataView.buffer) : dataView.buffer;
  return new dataView.constructor(buffer, dataView.byteOffset, dataView.byteLength);
***REMOVED***

module.exports = cloneDataView;

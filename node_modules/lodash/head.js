/**
 * Gets the first element of `array`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @alias first
 * @category Array
 * @param ***REMOVED***Array***REMOVED*** array The array to query.
 * @returns ***REMOVED*******REMOVED*** Returns the first element of `array`.
 * @example
 *
 * _.head([1, 2, 3]);
 * // => 1
 *
 * _.head([]);
 * // => undefined
 */
function head(array) ***REMOVED***
  return (array && array.length) ? array[0] : undefined;
***REMOVED***

module.exports = head;

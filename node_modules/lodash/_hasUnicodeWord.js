/** Used to detect strings that need a more robust regexp to match words. */
var reHasUnicodeWord = /[a-z][A-Z]|[A-Z]***REMOVED***2,***REMOVED***[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;

/**
 * Checks if `string` contains a word composed of Unicode symbols.
 *
 * @private
 * @param ***REMOVED***string***REMOVED*** string The string to inspect.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if a word is found, else `false`.
 */
function hasUnicodeWord(string) ***REMOVED***
  return reHasUnicodeWord.test(string);
***REMOVED***

module.exports = hasUnicodeWord;

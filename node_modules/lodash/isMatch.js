var baseIsMatch = require('./_baseIsMatch'),
    getMatchData = require('./_getMatchData');

/**
 * Performs a partial deep comparison between `object` and `source` to
 * determine if `object` contains equivalent property values.
 *
 * **Note:** This method is equivalent to `_.matches` when `source` is
 * partially applied.
 *
 * Partial comparisons will match empty array and empty object `source`
 * values against any array or object value, respectively. See `_.isEqual`
 * for a list of supported value comparisons.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param ***REMOVED***Object***REMOVED*** object The object to inspect.
 * @param ***REMOVED***Object***REMOVED*** source The object of property values to match.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if `object` is a match, else `false`.
 * @example
 *
 * var object = ***REMOVED*** 'a': 1, 'b': 2 ***REMOVED***;
 *
 * _.isMatch(object, ***REMOVED*** 'b': 2 ***REMOVED***);
 * // => true
 *
 * _.isMatch(object, ***REMOVED*** 'b': 1 ***REMOVED***);
 * // => false
 */
function isMatch(object, source) ***REMOVED***
  return object === source || baseIsMatch(object, source, getMatchData(source));
***REMOVED***

module.exports = isMatch;

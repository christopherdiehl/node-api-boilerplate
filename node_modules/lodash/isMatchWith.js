var baseIsMatch = require('./_baseIsMatch'),
    getMatchData = require('./_getMatchData');

/**
 * This method is like `_.isMatch` except that it accepts `customizer` which
 * is invoked to compare values. If `customizer` returns `undefined`, comparisons
 * are handled by the method instead. The `customizer` is invoked with five
 * arguments: (objValue, srcValue, index|key, object, source).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param ***REMOVED***Object***REMOVED*** object The object to inspect.
 * @param ***REMOVED***Object***REMOVED*** source The object of property values to match.
 * @param ***REMOVED***Function***REMOVED*** [customizer] The function to customize comparisons.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if `object` is a match, else `false`.
 * @example
 *
 * function isGreeting(value) ***REMOVED***
 *   return /^h(?:i|ello)$/.test(value);
 * ***REMOVED***
 *
 * function customizer(objValue, srcValue) ***REMOVED***
 *   if (isGreeting(objValue) && isGreeting(srcValue)) ***REMOVED***
 *     return true;
 *   ***REMOVED***
 * ***REMOVED***
 *
 * var object = ***REMOVED*** 'greeting': 'hello' ***REMOVED***;
 * var source = ***REMOVED*** 'greeting': 'hi' ***REMOVED***;
 *
 * _.isMatchWith(object, source, customizer);
 * // => true
 */
function isMatchWith(object, source, customizer) ***REMOVED***
  customizer = typeof customizer == 'function' ? customizer : undefined;
  return baseIsMatch(object, source, getMatchData(source), customizer);
***REMOVED***

module.exports = isMatchWith;

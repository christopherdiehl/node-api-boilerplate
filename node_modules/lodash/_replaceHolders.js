/** Used as the internal argument placeholder. */
var PLACEHOLDER = '__lodash_placeholder__';

/**
 * Replaces all `placeholder` elements in `array` with an internal placeholder
 * and returns an array of their indexes.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to modify.
 * @param ***REMOVED*******REMOVED*** placeholder The placeholder to replace.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new array of placeholder indexes.
 */
function replaceHolders(array, placeholder) ***REMOVED***
  var index = -1,
      length = array.length,
      resIndex = 0,
      result = [];

  while (++index < length) ***REMOVED***
    var value = array[index];
    if (value === placeholder || value === PLACEHOLDER) ***REMOVED***
      array[index] = PLACEHOLDER;
      result[resIndex++] = index;
    ***REMOVED***
  ***REMOVED***
  return result;
***REMOVED***

module.exports = replaceHolders;

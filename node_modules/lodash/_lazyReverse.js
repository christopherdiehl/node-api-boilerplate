var LazyWrapper = require('./_LazyWrapper');

/**
 * Reverses the direction of lazy iteration.
 *
 * @private
 * @name reverse
 * @memberOf LazyWrapper
 * @returns ***REMOVED***Object***REMOVED*** Returns the new reversed `LazyWrapper` object.
 */
function lazyReverse() ***REMOVED***
  if (this.__filtered__) ***REMOVED***
    var result = new LazyWrapper(this);
    result.__dir__ = -1;
    result.__filtered__ = true;
  ***REMOVED*** else ***REMOVED***
    result = this.clone();
    result.__dir__ *= -1;
  ***REMOVED***
  return result;
***REMOVED***

module.exports = lazyReverse;

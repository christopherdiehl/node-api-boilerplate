/**
 * The base implementation of `_.lt` which doesn't coerce arguments.
 *
 * @private
 * @param ***REMOVED*******REMOVED*** value The value to compare.
 * @param ***REMOVED*******REMOVED*** other The other value to compare.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if `value` is less than `other`,
 *  else `false`.
 */
function baseLt(value, other) ***REMOVED***
  return value < other;
***REMOVED***

module.exports = baseLt;

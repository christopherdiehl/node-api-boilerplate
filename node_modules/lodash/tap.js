/**
 * This method invokes `interceptor` and returns `value`. The interceptor
 * is invoked with one argument; (value). The purpose of this method is to
 * "tap into" a method chain sequence in order to modify intermediate results.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Seq
 * @param ***REMOVED*******REMOVED*** value The value to provide to `interceptor`.
 * @param ***REMOVED***Function***REMOVED*** interceptor The function to invoke.
 * @returns ***REMOVED*******REMOVED*** Returns `value`.
 * @example
 *
 * _([1, 2, 3])
 *  .tap(function(array) ***REMOVED***
 *    // Mutate input array.
 *    array.pop();
 *  ***REMOVED***)
 *  .reverse()
 *  .value();
 * // => [2, 1]
 */
function tap(value, interceptor) ***REMOVED***
  interceptor(value);
  return value;
***REMOVED***

module.exports = tap;

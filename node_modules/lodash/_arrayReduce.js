/**
 * A specialized version of `_.reduce` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** [array] The array to iterate over.
 * @param ***REMOVED***Function***REMOVED*** iteratee The function invoked per iteration.
 * @param ***REMOVED*******REMOVED*** [accumulator] The initial value.
 * @param ***REMOVED***boolean***REMOVED*** [initAccum] Specify using the first element of `array` as
 *  the initial value.
 * @returns ***REMOVED*******REMOVED*** Returns the accumulated value.
 */
function arrayReduce(array, iteratee, accumulator, initAccum) ***REMOVED***
  var index = -1,
      length = array == null ? 0 : array.length;

  if (initAccum && length) ***REMOVED***
    accumulator = array[++index];
  ***REMOVED***
  while (++index < length) ***REMOVED***
    accumulator = iteratee(accumulator, array[index], index, array);
  ***REMOVED***
  return accumulator;
***REMOVED***

module.exports = arrayReduce;

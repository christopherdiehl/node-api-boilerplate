var isArray = require('./isArray'),
    isKey = require('./_isKey'),
    stringToPath = require('./_stringToPath'),
    toString = require('./toString');

/**
 * Casts `value` to a path array if it's not one.
 *
 * @private
 * @param ***REMOVED*******REMOVED*** value The value to inspect.
 * @param ***REMOVED***Object***REMOVED*** [object] The object to query keys on.
 * @returns ***REMOVED***Array***REMOVED*** Returns the cast property path array.
 */
function castPath(value, object) ***REMOVED***
  if (isArray(value)) ***REMOVED***
    return value;
  ***REMOVED***
  return isKey(value, object) ? [value] : stringToPath(toString(value));
***REMOVED***

module.exports = castPath;

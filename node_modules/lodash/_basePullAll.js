var arrayMap = require('./_arrayMap'),
    baseIndexOf = require('./_baseIndexOf'),
    baseIndexOfWith = require('./_baseIndexOfWith'),
    baseUnary = require('./_baseUnary'),
    copyArray = require('./_copyArray');

/** Used for built-in method references. */
var arrayProto = Array.prototype;

/** Built-in value references. */
var splice = arrayProto.splice;

/**
 * The base implementation of `_.pullAllBy` without support for iteratee
 * shorthands.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to modify.
 * @param ***REMOVED***Array***REMOVED*** values The values to remove.
 * @param ***REMOVED***Function***REMOVED*** [iteratee] The iteratee invoked per element.
 * @param ***REMOVED***Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns ***REMOVED***Array***REMOVED*** Returns `array`.
 */
function basePullAll(array, values, iteratee, comparator) ***REMOVED***
  var indexOf = comparator ? baseIndexOfWith : baseIndexOf,
      index = -1,
      length = values.length,
      seen = array;

  if (array === values) ***REMOVED***
    values = copyArray(values);
  ***REMOVED***
  if (iteratee) ***REMOVED***
    seen = arrayMap(array, baseUnary(iteratee));
  ***REMOVED***
  while (++index < length) ***REMOVED***
    var fromIndex = 0,
        value = values[index],
        computed = iteratee ? iteratee(value) : value;

    while ((fromIndex = indexOf(seen, computed, fromIndex, comparator)) > -1) ***REMOVED***
      if (seen !== array) ***REMOVED***
        splice.call(seen, fromIndex, 1);
      ***REMOVED***
      splice.call(array, fromIndex, 1);
    ***REMOVED***
  ***REMOVED***
  return array;
***REMOVED***

module.exports = basePullAll;

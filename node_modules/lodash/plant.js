var baseLodash = require('./_baseLodash'),
    wrapperClone = require('./_wrapperClone');

/**
 * Creates a clone of the chain sequence planting `value` as the wrapped value.
 *
 * @name plant
 * @memberOf _
 * @since 3.2.0
 * @category Seq
 * @param ***REMOVED*******REMOVED*** value The value to plant.
 * @returns ***REMOVED***Object***REMOVED*** Returns the new `lodash` wrapper instance.
 * @example
 *
 * function square(n) ***REMOVED***
 *   return n * n;
 * ***REMOVED***
 *
 * var wrapped = _([1, 2]).map(square);
 * var other = wrapped.plant([3, 4]);
 *
 * other.value();
 * // => [9, 16]
 *
 * wrapped.value();
 * // => [1, 4]
 */
function wrapperPlant(value) ***REMOVED***
  var result,
      parent = this;

  while (parent instanceof baseLodash) ***REMOVED***
    var clone = wrapperClone(parent);
    clone.__index__ = 0;
    clone.__values__ = undefined;
    if (result) ***REMOVED***
      previous.__wrapped__ = clone;
    ***REMOVED*** else ***REMOVED***
      result = clone;
    ***REMOVED***
    var previous = clone;
    parent = parent.__wrapped__;
  ***REMOVED***
  previous.__wrapped__ = value;
  return result;
***REMOVED***

module.exports = wrapperPlant;

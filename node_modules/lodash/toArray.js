var Symbol = require('./_Symbol'),
    copyArray = require('./_copyArray'),
    getTag = require('./_getTag'),
    isArrayLike = require('./isArrayLike'),
    isString = require('./isString'),
    iteratorToArray = require('./_iteratorToArray'),
    mapToArray = require('./_mapToArray'),
    setToArray = require('./_setToArray'),
    stringToArray = require('./_stringToArray'),
    values = require('./values');

/** `Object#toString` result references. */
var mapTag = '[object Map]',
    setTag = '[object Set]';

/** Built-in value references. */
var symIterator = Symbol ? Symbol.iterator : undefined;

/**
 * Converts `value` to an array.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Lang
 * @param ***REMOVED*******REMOVED*** value The value to convert.
 * @returns ***REMOVED***Array***REMOVED*** Returns the converted array.
 * @example
 *
 * _.toArray(***REMOVED*** 'a': 1, 'b': 2 ***REMOVED***);
 * // => [1, 2]
 *
 * _.toArray('abc');
 * // => ['a', 'b', 'c']
 *
 * _.toArray(1);
 * // => []
 *
 * _.toArray(null);
 * // => []
 */
function toArray(value) ***REMOVED***
  if (!value) ***REMOVED***
    return [];
  ***REMOVED***
  if (isArrayLike(value)) ***REMOVED***
    return isString(value) ? stringToArray(value) : copyArray(value);
  ***REMOVED***
  if (symIterator && value[symIterator]) ***REMOVED***
    return iteratorToArray(value[symIterator]());
  ***REMOVED***
  var tag = getTag(value),
      func = tag == mapTag ? mapToArray : (tag == setTag ? setToArray : values);

  return func(value);
***REMOVED***

module.exports = toArray;

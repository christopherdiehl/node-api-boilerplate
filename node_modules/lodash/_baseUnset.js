var castPath = require('./_castPath'),
    last = require('./last'),
    parent = require('./_parent'),
    toKey = require('./_toKey');

/**
 * The base implementation of `_.unset`.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** object The object to modify.
 * @param ***REMOVED***Array|string***REMOVED*** path The property path to unset.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if the property is deleted, else `false`.
 */
function baseUnset(object, path) ***REMOVED***
  path = castPath(path, object);
  object = parent(object, path);
  return object == null || delete object[toKey(last(path))];
***REMOVED***

module.exports = baseUnset;

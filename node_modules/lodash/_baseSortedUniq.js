var eq = require('./eq');

/**
 * The base implementation of `_.sortedUniq` and `_.sortedUniqBy` without
 * support for iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** array The array to inspect.
 * @param ***REMOVED***Function***REMOVED*** [iteratee] The iteratee invoked per element.
 * @returns ***REMOVED***Array***REMOVED*** Returns the new duplicate free array.
 */
function baseSortedUniq(array, iteratee) ***REMOVED***
  var index = -1,
      length = array.length,
      resIndex = 0,
      result = [];

  while (++index < length) ***REMOVED***
    var value = array[index],
        computed = iteratee ? iteratee(value) : value;

    if (!index || !eq(computed, seen)) ***REMOVED***
      var seen = computed;
      result[resIndex++] = value === 0 ? 0 : value;
    ***REMOVED***
  ***REMOVED***
  return result;
***REMOVED***

module.exports = baseSortedUniq;

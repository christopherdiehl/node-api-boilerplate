var baseToNumber = require('./_baseToNumber'),
    baseToString = require('./_baseToString');

/**
 * Creates a function that performs a mathematical operation on two values.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** operator The function to perform the operation.
 * @param ***REMOVED***number***REMOVED*** [defaultValue] The value used for `undefined` arguments.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new mathematical operation function.
 */
function createMathOperation(operator, defaultValue) ***REMOVED***
  return function(value, other) ***REMOVED***
    var result;
    if (value === undefined && other === undefined) ***REMOVED***
      return defaultValue;
    ***REMOVED***
    if (value !== undefined) ***REMOVED***
      result = value;
    ***REMOVED***
    if (other !== undefined) ***REMOVED***
      if (result === undefined) ***REMOVED***
        return other;
      ***REMOVED***
      if (typeof value == 'string' || typeof other == 'string') ***REMOVED***
        value = baseToString(value);
        other = baseToString(other);
      ***REMOVED*** else ***REMOVED***
        value = baseToNumber(value);
        other = baseToNumber(other);
      ***REMOVED***
      result = operator(value, other);
    ***REMOVED***
    return result;
  ***REMOVED***;
***REMOVED***

module.exports = createMathOperation;

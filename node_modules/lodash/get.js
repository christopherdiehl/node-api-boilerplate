var baseGet = require('./_baseGet');

/**
 * Gets the value at `path` of `object`. If the resolved value is
 * `undefined`, the `defaultValue` is returned in its place.
 *
 * @static
 * @memberOf _
 * @since 3.7.0
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The object to query.
 * @param ***REMOVED***Array|string***REMOVED*** path The path of the property to get.
 * @param ***REMOVED*******REMOVED*** [defaultValue] The value returned for `undefined` resolved values.
 * @returns ***REMOVED*******REMOVED*** Returns the resolved value.
 * @example
 *
 * var object = ***REMOVED*** 'a': [***REMOVED*** 'b': ***REMOVED*** 'c': 3 ***REMOVED*** ***REMOVED***] ***REMOVED***;
 *
 * _.get(object, 'a[0].b.c');
 * // => 3
 *
 * _.get(object, ['a', '0', 'b', 'c']);
 * // => 3
 *
 * _.get(object, 'a.b.c', 'default');
 * // => 'default'
 */
function get(object, path, defaultValue) ***REMOVED***
  var result = object == null ? undefined : baseGet(object, path);
  return result === undefined ? defaultValue : result;
***REMOVED***

module.exports = get;

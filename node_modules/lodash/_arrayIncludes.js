var baseIndexOf = require('./_baseIndexOf');

/**
 * A specialized version of `_.includes` for arrays without support for
 * specifying an index to search from.
 *
 * @private
 * @param ***REMOVED***Array***REMOVED*** [array] The array to inspect.
 * @param ***REMOVED*******REMOVED*** target The value to search for.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if `target` is found, else `false`.
 */
function arrayIncludes(array, value) ***REMOVED***
  var length = array == null ? 0 : array.length;
  return !!length && baseIndexOf(array, value, 0) > -1;
***REMOVED***

module.exports = arrayIncludes;

var baseIsEqualDeep = require('./_baseIsEqualDeep'),
    isObjectLike = require('./isObjectLike');

/**
 * The base implementation of `_.isEqual` which supports partial comparisons
 * and tracks traversed objects.
 *
 * @private
 * @param ***REMOVED*******REMOVED*** value The value to compare.
 * @param ***REMOVED*******REMOVED*** other The other value to compare.
 * @param ***REMOVED***boolean***REMOVED*** bitmask The bitmask flags.
 *  1 - Unordered comparison
 *  2 - Partial comparison
 * @param ***REMOVED***Function***REMOVED*** [customizer] The function to customize comparisons.
 * @param ***REMOVED***Object***REMOVED*** [stack] Tracks traversed `value` and `other` objects.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if the values are equivalent, else `false`.
 */
function baseIsEqual(value, other, bitmask, customizer, stack) ***REMOVED***
  if (value === other) ***REMOVED***
    return true;
  ***REMOVED***
  if (value == null || other == null || (!isObjectLike(value) && !isObjectLike(other))) ***REMOVED***
    return value !== value && other !== other;
  ***REMOVED***
  return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
***REMOVED***

module.exports = baseIsEqual;

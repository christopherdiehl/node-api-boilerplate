/**
 * Adds the key-value `pair` to `map`.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** map The map to modify.
 * @param ***REMOVED***Array***REMOVED*** pair The key-value pair to add.
 * @returns ***REMOVED***Object***REMOVED*** Returns `map`.
 */
function addMapEntry(map, pair) ***REMOVED***
  // Don't return `map.set` because it's not chainable in IE 11.
  map.set(pair[0], pair[1]);
  return map;
***REMOVED***

module.exports = addMapEntry;

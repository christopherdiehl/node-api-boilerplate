/**
 * The base implementation of `_.conformsTo` which accepts `props` to check.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** object The object to inspect.
 * @param ***REMOVED***Object***REMOVED*** source The object of property predicates to conform to.
 * @returns ***REMOVED***boolean***REMOVED*** Returns `true` if `object` conforms, else `false`.
 */
function baseConformsTo(object, source, props) ***REMOVED***
  var length = props.length;
  if (object == null) ***REMOVED***
    return !length;
  ***REMOVED***
  object = Object(object);
  while (length--) ***REMOVED***
    var key = props[length],
        predicate = source[key],
        value = object[key];

    if ((value === undefined && !(key in object)) || !predicate(value)) ***REMOVED***
      return false;
    ***REMOVED***
  ***REMOVED***
  return true;
***REMOVED***

module.exports = baseConformsTo;

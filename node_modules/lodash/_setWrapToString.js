var getWrapDetails = require('./_getWrapDetails'),
    insertWrapDetails = require('./_insertWrapDetails'),
    setToString = require('./_setToString'),
    updateWrapDetails = require('./_updateWrapDetails');

/**
 * Sets the `toString` method of `wrapper` to mimic the source of `reference`
 * with wrapper details in a comment at the top of the source body.
 *
 * @private
 * @param ***REMOVED***Function***REMOVED*** wrapper The function to modify.
 * @param ***REMOVED***Function***REMOVED*** reference The reference function.
 * @param ***REMOVED***number***REMOVED*** bitmask The bitmask flags. See `createWrap` for more details.
 * @returns ***REMOVED***Function***REMOVED*** Returns `wrapper`.
 */
function setWrapToString(wrapper, reference, bitmask) ***REMOVED***
  var source = (reference + '');
  return setToString(wrapper, insertWrapDetails(source, updateWrapDetails(getWrapDetails(source), bitmask)));
***REMOVED***

module.exports = setWrapToString;

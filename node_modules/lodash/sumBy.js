var baseIteratee = require('./_baseIteratee'),
    baseSum = require('./_baseSum');

/**
 * This method is like `_.sum` except that it accepts `iteratee` which is
 * invoked for each element in `array` to generate the value to be summed.
 * The iteratee is invoked with one argument: (value).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Math
 * @param ***REMOVED***Array***REMOVED*** array The array to iterate over.
 * @param ***REMOVED***Function***REMOVED*** [iteratee=_.identity] The iteratee invoked per element.
 * @returns ***REMOVED***number***REMOVED*** Returns the sum.
 * @example
 *
 * var objects = [***REMOVED*** 'n': 4 ***REMOVED***, ***REMOVED*** 'n': 2 ***REMOVED***, ***REMOVED*** 'n': 8 ***REMOVED***, ***REMOVED*** 'n': 6 ***REMOVED***];
 *
 * _.sumBy(objects, function(o) ***REMOVED*** return o.n; ***REMOVED***);
 * // => 20
 *
 * // The `_.property` iteratee shorthand.
 * _.sumBy(objects, 'n');
 * // => 20
 */
function sumBy(array, iteratee) ***REMOVED***
  return (array && array.length)
    ? baseSum(array, baseIteratee(iteratee, 2))
    : 0;
***REMOVED***

module.exports = sumBy;

var basePick = require('./_basePick'),
    flatRest = require('./_flatRest');

/**
 * Creates an object composed of the picked `object` properties.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param ***REMOVED***Object***REMOVED*** object The source object.
 * @param ***REMOVED***...(string|string[])***REMOVED*** [paths] The property paths to pick.
 * @returns ***REMOVED***Object***REMOVED*** Returns the new object.
 * @example
 *
 * var object = ***REMOVED*** 'a': 1, 'b': '2', 'c': 3 ***REMOVED***;
 *
 * _.pick(object, ['a', 'c']);
 * // => ***REMOVED*** 'a': 1, 'c': 3 ***REMOVED***
 */
var pick = flatRest(function(object, paths) ***REMOVED***
  return object == null ? ***REMOVED******REMOVED*** : basePick(object, paths);
***REMOVED***);

module.exports = pick;

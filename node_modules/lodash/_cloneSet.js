var addSetEntry = require('./_addSetEntry'),
    arrayReduce = require('./_arrayReduce'),
    setToArray = require('./_setToArray');

/** Used to compose bitmasks for cloning. */
var CLONE_DEEP_FLAG = 1;

/**
 * Creates a clone of `set`.
 *
 * @private
 * @param ***REMOVED***Object***REMOVED*** set The set to clone.
 * @param ***REMOVED***Function***REMOVED*** cloneFunc The function to clone values.
 * @param ***REMOVED***boolean***REMOVED*** [isDeep] Specify a deep clone.
 * @returns ***REMOVED***Object***REMOVED*** Returns the cloned set.
 */
function cloneSet(set, isDeep, cloneFunc) ***REMOVED***
  var array = isDeep ? cloneFunc(setToArray(set), CLONE_DEEP_FLAG) : setToArray(set);
  return arrayReduce(array, addSetEntry, new set.constructor);
***REMOVED***

module.exports = cloneSet;

var baseRest = require('./_baseRest'),
    createWrap = require('./_createWrap'),
    getHolder = require('./_getHolder'),
    replaceHolders = require('./_replaceHolders');

/** Used to compose bitmasks for function metadata. */
var WRAP_PARTIAL_RIGHT_FLAG = 64;

/**
 * This method is like `_.partial` except that partially applied arguments
 * are appended to the arguments it receives.
 *
 * The `_.partialRight.placeholder` value, which defaults to `_` in monolithic
 * builds, may be used as a placeholder for partially applied arguments.
 *
 * **Note:** This method doesn't set the "length" property of partially
 * applied functions.
 *
 * @static
 * @memberOf _
 * @since 1.0.0
 * @category Function
 * @param ***REMOVED***Function***REMOVED*** func The function to partially apply arguments to.
 * @param ***REMOVED***...****REMOVED*** [partials] The arguments to be partially applied.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new partially applied function.
 * @example
 *
 * function greet(greeting, name) ***REMOVED***
 *   return greeting + ' ' + name;
 * ***REMOVED***
 *
 * var greetFred = _.partialRight(greet, 'fred');
 * greetFred('hi');
 * // => 'hi fred'
 *
 * // Partially applied with placeholders.
 * var sayHelloTo = _.partialRight(greet, 'hello', _);
 * sayHelloTo('fred');
 * // => 'hello fred'
 */
var partialRight = baseRest(function(func, partials) ***REMOVED***
  var holders = replaceHolders(partials, getHolder(partialRight));
  return createWrap(func, WRAP_PARTIAL_RIGHT_FLAG, undefined, partials, holders);
***REMOVED***);

// Assign default placeholders.
partialRight.placeholder = ***REMOVED******REMOVED***;

module.exports = partialRight;

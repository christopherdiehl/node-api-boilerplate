var baseNth = require('./_baseNth'),
    toInteger = require('./toInteger');

/**
 * Gets the element at index `n` of `array`. If `n` is negative, the nth
 * element from the end is returned.
 *
 * @static
 * @memberOf _
 * @since 4.11.0
 * @category Array
 * @param ***REMOVED***Array***REMOVED*** array The array to query.
 * @param ***REMOVED***number***REMOVED*** [n=0] The index of the element to return.
 * @returns ***REMOVED*******REMOVED*** Returns the nth element of `array`.
 * @example
 *
 * var array = ['a', 'b', 'c', 'd'];
 *
 * _.nth(array, 1);
 * // => 'b'
 *
 * _.nth(array, -2);
 * // => 'c';
 */
function nth(array, n) ***REMOVED***
  return (array && array.length) ? baseNth(array, toInteger(n)) : undefined;
***REMOVED***

module.exports = nth;

var baseGet = require('./_baseGet');

/**
 * A specialized version of `baseProperty` which supports deep paths.
 *
 * @private
 * @param ***REMOVED***Array|string***REMOVED*** path The path of the property to get.
 * @returns ***REMOVED***Function***REMOVED*** Returns the new accessor function.
 */
function basePropertyDeep(path) ***REMOVED***
  return function(object) ***REMOVED***
    return baseGet(object, path);
  ***REMOVED***;
***REMOVED***

module.exports = basePropertyDeep;

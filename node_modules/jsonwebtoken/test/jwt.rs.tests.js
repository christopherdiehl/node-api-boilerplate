var jwt = require('../index');
var fs = require('fs');
var path = require('path');

var expect = require('chai').expect;
var assert = require('chai').assert;
var ms = require('ms');

describe('RS256', function() ***REMOVED***
  var pub = fs.readFileSync(path.join(__dirname, 'pub.pem'));
  var priv = fs.readFileSync(path.join(__dirname, 'priv.pem'));
  var invalid_pub = fs.readFileSync(path.join(__dirname, 'invalid_pub.pem'));

  describe('when signing a token', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);

    it('should be syntactically valid', function() ***REMOVED***
      expect(token).to.be.a('string');
      expect(token.split('.')).to.have.length(3);
    ***REMOVED***);

    context('asynchronous', function() ***REMOVED***
      it('should validate with public key', function(done) ***REMOVED***
        jwt.verify(token, pub, function(err, decoded) ***REMOVED***
          assert.ok(decoded.foo);
          assert.equal('bar', decoded.foo);
          done();
        ***REMOVED***);
      ***REMOVED***);

      it('should throw with invalid public key', function(done) ***REMOVED***
        jwt.verify(token, invalid_pub, function(err, decoded) ***REMOVED***
          assert.isUndefined(decoded);
          assert.isNotNull(err);
          done();
        ***REMOVED***);
      ***REMOVED***);
    ***REMOVED***);

    context('synchronous', function() ***REMOVED***
      it('should validate with public key', function() ***REMOVED***
        var decoded = jwt.verify(token, pub);
        assert.ok(decoded.foo);
        assert.equal('bar', decoded.foo);
      ***REMOVED***);

      it('should throw with invalid public key', function() ***REMOVED***
        var jwtVerify = jwt.verify.bind(null, token, invalid_pub)
        assert.throw(jwtVerify, 'invalid signature');
      ***REMOVED***);
    ***REMOVED***);

  ***REMOVED***);

  describe('when signing a token with expiration', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', expiresIn: '10m' ***REMOVED***);

    it('should be valid expiration', function(done) ***REMOVED***
      jwt.verify(token, pub, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should be invalid', function(done) ***REMOVED***
      // expired token
      token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', expiresIn: -1 * ms('10m') ***REMOVED***);

      jwt.verify(token, pub, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'TokenExpiredError');
        assert.instanceOf(err.expiredAt, Date);
        assert.instanceOf(err, jwt.TokenExpiredError);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should NOT be invalid', function(done) ***REMOVED***
      // expired token
      token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', expiresIn: -1 * ms('10m') ***REMOVED***);

      jwt.verify(token, pub, ***REMOVED*** ignoreExpiration: true ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.ok(decoded.foo);
        assert.equal('bar', decoded.foo);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token with not before', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', notBefore: -10 * 3600 ***REMOVED***);

    it('should be valid expiration', function(done) ***REMOVED***
      jwt.verify(token, pub, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should be invalid', function(done) ***REMOVED***
      // not active token
      token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', notBefore: '10m' ***REMOVED***);

      jwt.verify(token, pub, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'NotBeforeError');
        assert.instanceOf(err.date, Date);
        assert.instanceOf(err, jwt.NotBeforeError);
        done();
      ***REMOVED***);
    ***REMOVED***);


    it('should valid when date are equals', function(done) ***REMOVED***
      Date.fix(1451908031);

      token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', notBefore: 0 ***REMOVED***);

      jwt.verify(token, pub, function(err, decoded) ***REMOVED***
        assert.isNull(err);
        assert.isNotNull(decoded);
        Date.unfix();
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should NOT be invalid', function(done) ***REMOVED***
      // not active token
      token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', notBefore: '10m' ***REMOVED***);

      jwt.verify(token, pub, ***REMOVED*** ignoreNotBefore: true ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.ok(decoded.foo);
        assert.equal('bar', decoded.foo);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token with audience', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', audience: 'urn:foo' ***REMOVED***);

    it('should check audience', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: 'urn:foo' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should check audience in array', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: ['urn:foo', 'urn:other'] ***REMOVED***, function (err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid audience', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: 'urn:wrong' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid audience in array', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: ['urn:wrong', 'urn:morewrong'] ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);

  ***REMOVED***);

  describe('when signing a token with array audience', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', audience: [ 'urn:foo', 'urn:bar' ] ***REMOVED***);

    it('should check audience', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: 'urn:foo' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should check other audience', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: 'urn:bar' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should check audience in array', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: ['urn:foo', 'urn:other'] ***REMOVED***, function (err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid audience', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: 'urn:wrong' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid audience in array', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: ['urn:wrong', 'urn:morewrong'] ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);

  ***REMOVED***);

  describe('when signing a token without audience', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);

    it('should check audience', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: 'urn:wrong' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should check audience in array', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** audience: ['urn:wrong', 'urn:morewrong'] ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);

  ***REMOVED***);

  describe('when signing a token with issuer', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', issuer: 'urn:foo' ***REMOVED***);

    it('should check issuer', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** issuer: 'urn:foo' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should check the issuer when providing a list of valid issuers', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** issuer: [ 'urn:foo', 'urn:bar' ] ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid issuer', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** issuer: 'urn:wrong' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token without issuer', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);

    it('should check issuer', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** issuer: 'urn:foo' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token with subject', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', subject: 'subject' ***REMOVED***);

    it('should check subject', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** subject: 'subject' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid subject', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** subject: 'wrongSubject' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token without subject', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);

    it('should check subject', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** subject: 'subject' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token with jwt id', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256', jwtid: 'jwtid' ***REMOVED***);

    it('should check jwt id', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** jwtid: 'jwtid' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isNotNull(decoded);
        assert.isNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);

    it('should throw when invalid jwt id', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** jwtid: 'wrongJwtid' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when signing a token without jwt id', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);

    it('should check jwt id', function(done) ***REMOVED***
      jwt.verify(token, pub, ***REMOVED*** jwtid: 'jwtid' ***REMOVED***, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        assert.instanceOf(err, jwt.JsonWebTokenError);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when verifying a malformed token', function() ***REMOVED***
    it('should throw', function(done) ***REMOVED***
      jwt.verify('fruit.fruit.fruit', pub, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        assert.equal(err.name, 'JsonWebTokenError');
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when decoding a jwt token with additional parts', function() ***REMOVED***
    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);

    it('should throw', function(done) ***REMOVED***
      jwt.verify(token + '.foo', pub, function(err, decoded) ***REMOVED***
        assert.isUndefined(decoded);
        assert.isNotNull(err);
        done();
      ***REMOVED***);
    ***REMOVED***);
  ***REMOVED***);

  describe('when decoding a invalid jwt token', function() ***REMOVED***
    it('should return null', function(done) ***REMOVED***
      var payload = jwt.decode('whatever.token');
      assert.isNull(payload);
      done();
    ***REMOVED***);
  ***REMOVED***);

  describe('when decoding a valid jwt token', function() ***REMOVED***
    it('should return the payload', function(done) ***REMOVED***
      var obj     = ***REMOVED*** foo: 'bar' ***REMOVED***;
      var token   = jwt.sign(obj, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);
      var payload = jwt.decode(token);
      assert.equal(payload.foo, obj.foo);
      done();
    ***REMOVED***);
    it('should return the header and payload and signature if complete option is set', function(done) ***REMOVED***
      var obj     = ***REMOVED*** foo: 'bar' ***REMOVED***;
      var token   = jwt.sign(obj, priv, ***REMOVED*** algorithm: 'RS256' ***REMOVED***);
      var decoded = jwt.decode(token, ***REMOVED*** complete: true ***REMOVED***);
      assert.equal(decoded.payload.foo, obj.foo);
      assert.deepEqual(decoded.header, ***REMOVED*** typ: 'JWT', alg: 'RS256' ***REMOVED***);
      assert.ok(typeof decoded.signature == 'string');
      done();
    ***REMOVED***);
  ***REMOVED***);
***REMOVED***);

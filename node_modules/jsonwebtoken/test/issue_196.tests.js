var expect = require('chai').expect;
var jwt = require('./..');
var atob = require('atob');

describe('issue 196', function () ***REMOVED***
  function b64_to_utf8 (str) ***REMOVED***
    return decodeURIComponent(escape(atob( str )));
  ***REMOVED***

  it('should use issuer provided in payload.iss', function () ***REMOVED***
    var token = jwt.sign(***REMOVED*** iss: 'foo' ***REMOVED***, 'shhhhh');
    var decoded_issuer = JSON.parse(b64_to_utf8(token.split('.')[1])).iss;
    expect(decoded_issuer).to.equal('foo');
  ***REMOVED***);
***REMOVED***);

var jwt = require('../index');
var expect = require('chai').expect;

describe('issue 147 - signing with a sealed payload', function() ***REMOVED***

  it('should put the expiration claim', function () ***REMOVED***
    var token = jwt.sign(Object.seal(***REMOVED***foo: 123***REMOVED***), '123', ***REMOVED*** expiresIn: 10 ***REMOVED***);
    var result = jwt.verify(token, '123');
    expect(result.exp).to.be.closeTo(Math.floor(Date.now() / 1000) + 10, 0.2);
  ***REMOVED***);

***REMOVED***);
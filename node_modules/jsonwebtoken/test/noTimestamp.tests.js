var jwt = require('../index');
var expect = require('chai').expect;

describe('noTimestamp', function() ***REMOVED***

  it('should work with string', function () ***REMOVED***
    var token = jwt.sign(***REMOVED***foo: 123***REMOVED***, '123', ***REMOVED*** expiresIn: '5m' , noTimestamp: true ***REMOVED***);
    var result = jwt.verify(token, '123');
    expect(result.exp).to.be.closeTo(Math.floor(Date.now() / 1000) + (5*60), 0.5);
  ***REMOVED***);

***REMOVED***);

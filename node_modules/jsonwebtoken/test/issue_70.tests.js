var jwt = require('../');

describe('issue 70 - public key start with BEING PUBLIC KEY', function () ***REMOVED***

  it('should work', function (done) ***REMOVED***
    var fs = require('fs');
    var cert_pub = fs.readFileSync(__dirname + '/rsa-public.pem');
    var cert_priv = fs.readFileSync(__dirname + '/rsa-private.pem');

    var token = jwt.sign(***REMOVED*** foo: 'bar' ***REMOVED***, cert_priv, ***REMOVED*** algorithm: 'RS256'***REMOVED***);

    jwt.verify(token, cert_pub, done);
  ***REMOVED***);

***REMOVED***);
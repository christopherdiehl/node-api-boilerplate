var ms = require('ms');

module.exports = function (time, iat) ***REMOVED***
  var timestamp = iat || Math.floor(Date.now() / 1000);

  if (typeof time === 'string') ***REMOVED***
    var milliseconds = ms(time);
    if (typeof milliseconds === 'undefined') ***REMOVED***
      return;
    ***REMOVED***
    return Math.floor(timestamp + milliseconds / 1000);
  ***REMOVED*** else if (typeof time === 'number') ***REMOVED***
    return timestamp + time;
  ***REMOVED*** else ***REMOVED***
    return;
  ***REMOVED***

***REMOVED***;
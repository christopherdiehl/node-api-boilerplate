'use strict';

var timeSafeCompare = require('../../lib/index');

function random(length) ***REMOVED***

    length = length || 32;
    var result = "";
    var possible = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()-+/*[]***REMOVED******REMOVED***-=\|;\':\"<>?,./";

    for( var i=0; i < length; i++ )***REMOVED***
        result += possible.charAt(Math.floor(Math.random() * possible.length));
    ***REMOVED***
    return result;
***REMOVED***

function run(count) ***REMOVED***
    count = count || 100*1000;
    console.log('benchmark count: ' + count/1000 + 'k');
    console.time('benchmark');
    
    while(count--)***REMOVED***
        timeSafeCompare(random(), random());
    ***REMOVED***
    console.timeEnd('benchmark');
***REMOVED***

run(100000);

module.exports = run;

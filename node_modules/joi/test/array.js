// Load modules

var Lab = require('lab');
var Code = require('code');
var Joi = require('../lib');
var Helper = require('./helper');


// Declare internals

var internals = ***REMOVED******REMOVED***;


// Test shortcuts

var lab = exports.lab = Lab.script();
var describe = lab.describe;
var it = lab.it;
var expect = Code.expect;


describe('array', function () ***REMOVED***

    it('converts a string to an array', function (done) ***REMOVED***

        Joi.array().validate('[1,2,3]', function (err, value) ***REMOVED***

            expect(err).to.not.exist();
            expect(value.length).to.equal(3);
            done();
        ***REMOVED***);
    ***REMOVED***);

    it('errors on non-array string', function (done) ***REMOVED***

        Joi.array().validate('***REMOVED*** "something": false ***REMOVED***', function (err, value) ***REMOVED***

            expect(err).to.exist();
            expect(err.message).to.equal('"value" must be an array');
            done();
        ***REMOVED***);
    ***REMOVED***);

    it('errors on number', function (done) ***REMOVED***

        Joi.array().validate(3, function (err, value) ***REMOVED***

            expect(err).to.exist();
            expect(value).to.equal(3);
            done();
        ***REMOVED***);
    ***REMOVED***);

    it('converts a non-array string with number type', function (done) ***REMOVED***

        Joi.array().validate('3', function (err, value) ***REMOVED***

            expect(err).to.exist();
            expect(value).to.equal('3');
            done();
        ***REMOVED***);
    ***REMOVED***);

    it('errors on a non-array string', function (done) ***REMOVED***

        Joi.array().validate('asdf', function (err, value) ***REMOVED***

            expect(err).to.exist();
            expect(value).to.equal('asdf');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#items', function () ***REMOVED***

        it('converts members', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number());
            var input = ['1', '2', '3'];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal([1, 2, 3]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('shows path to errors in array items', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().items(***REMOVED***
                    a: ***REMOVED***
                        b: ***REMOVED***
                            c: ***REMOVED***
                                d: undefined
                            ***REMOVED***
                        ***REMOVED***
                    ***REMOVED***
                ***REMOVED***);
            ***REMOVED***).to.throw(Error, 'Invalid schema content: (0.a.b.c.d)');

            expect(function () ***REMOVED***

                Joi.array().items(***REMOVED*** foo: 'bar' ***REMOVED***, undefined);
            ***REMOVED***).to.throw(Error, 'Invalid schema content: (1)');

            done();
        ***REMOVED***);

        it('allows zero size', function (done) ***REMOVED***

            var schema = Joi.object(***REMOVED***
                test: Joi.array().items(Joi.object(***REMOVED***
                    foo: Joi.string().required()
                ***REMOVED***))
            ***REMOVED***);
            var input = ***REMOVED*** test: [] ***REMOVED***;

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('returns the first error when only one inclusion', function (done) ***REMOVED***

            var schema = Joi.object(***REMOVED***
                test: Joi.array().items(Joi.object(***REMOVED***
                    foo: Joi.string().required()
                ***REMOVED***))
            ***REMOVED***);
            var input = ***REMOVED*** test: [***REMOVED*** foo: 'a' ***REMOVED***, ***REMOVED*** bar: 2 ***REMOVED***] ***REMOVED***;

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err.message).to.equal('child "test" fails because ["test" at position 1 fails because [child "foo" fails because ["foo" is required]]]');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates multiple types added in two calls', function (done) ***REMOVED***

            var schema = Joi.array()
                .items(Joi.number())
                .items(Joi.string());

            Helper.validate(schema, [
                [[1, 2, 3], true],
                [[50, 100, 1000], true],
                [[1, 'a', 5, 10], true],
                [['joi', 'everydaylowprices', 5000], true]
            ], done);
        ***REMOVED***);

        it('validates multiple types with stripUnknown', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number(), Joi.string()).options(***REMOVED*** stripUnknown: true ***REMOVED***);

            Helper.validate(schema, [
                [[1, 2, 'a'], true, null, [1, 2, 'a']],
                [[1, ***REMOVED*** foo: 'bar' ***REMOVED***, 'a', 2], true, null, [1, 'a', 2]]
            ], done);
        ***REMOVED***);

        it('allows forbidden to restrict values', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').forbidden(), Joi.string());
            var input = ['one', 'two', 'three', 'four'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 3 contains an excluded value');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates that a required value exists', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').required(), Joi.string());
            var input = ['one', 'two', 'three'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" does not contain 1 required value(s)');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates that a required value exists with abortEarly = false', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').required(), Joi.string()).options(***REMOVED*** abortEarly: false ***REMOVED***);
            var input = ['one', 'two', 'three'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" does not contain 1 required value(s)');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('does not re-run required tests that have already been matched', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').required(), Joi.string());
            var input = ['one', 'two', 'three', 'four', 'four', 'four'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(input);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('does not re-run required tests that have already failed', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').required(), Joi.boolean().required(), Joi.number());
            var input = ['one', 'two', 'three', 'four', 'four', 'four'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 0 does not match any of the allowed types');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('can require duplicates of the same schema and fail', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').required(), Joi.string().valid('four').required(), Joi.string());
            var input = ['one', 'two', 'three', 'four'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" does not contain 1 required value(s)');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('can require duplicates of the same schema and pass', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().valid('four').required(), Joi.string().valid('four').required(), Joi.string());
            var input = ['one', 'two', 'three', 'four', 'four'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(input);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('continues to validate after a required match', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().required(), Joi.boolean());
            var input = [true, 'one', false, 'two'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(input);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('can use a label on a required parameter', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().required().label('required string'), Joi.boolean());
            var input = [true, false];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" does not contain [required string]');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('can use a label on one required parameter, and no label on another', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string().required().label('required string'), Joi.string().required(), Joi.boolean());
            var input = [true, false];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" does not contain [required string] and 1 other required value(s)');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('can strip matching items', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string(), Joi.any().strip());
            schema.validate(['one', 'two', 3, 4], function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['one', 'two']);
                done();
            ***REMOVED***);
        ***REMOVED***);
    ***REMOVED***);

    describe('#min', function () ***REMOVED***

        it('validates array size', function (done) ***REMOVED***

            var schema = Joi.array().min(2);
            Helper.validate(schema, [
                [[1, 2], true],
                [[1], false]
            ], done);
        ***REMOVED***);

        it('throws when limit is not a number', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().min('a');
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);

        it('throws when limit is not an integer', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().min(1.2);
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#max', function () ***REMOVED***

        it('validates array size', function (done) ***REMOVED***

            var schema = Joi.array().max(1);
            Helper.validate(schema, [
                [[1, 2], false],
                [[1], true]
            ], done);
        ***REMOVED***);

        it('throws when limit is not a number', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().max('a');
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);

        it('throws when limit is not an integer', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().max(1.2);
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#length', function () ***REMOVED***

        it('validates array size', function (done) ***REMOVED***

            var schema = Joi.array().length(2);
            Helper.validate(schema, [
                [[1, 2], true],
                [[1], false]
            ], done);
        ***REMOVED***);

        it('throws when limit is not a number', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().length('a');
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);

        it('throws when limit is not an integer', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().length(1.2);
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#validate', function () ***REMOVED***

        it('should, by default, allow undefined, allow empty array', function (done) ***REMOVED***

            Helper.validate(Joi.array(), [
                [undefined, true],
                [[], true]
            ], done);
        ***REMOVED***);

        it('should, when .required(), deny undefined', function (done) ***REMOVED***

            Helper.validate(Joi.array().required(), [
                [undefined, false]
            ], done);
        ***REMOVED***);

        it('allows empty arrays', function (done) ***REMOVED***

            Helper.validate(Joi.array(), [
                [undefined, true],
                [[], true]
            ], done);
        ***REMOVED***);

        it('excludes values when items are forbidden', function (done) ***REMOVED***

            Helper.validate(Joi.array().items(Joi.string().forbidden()), [
                [['2', '1'], false],
                [['1'], false],
                [[2], true]
            ], done);
        ***REMOVED***);

        it('allows types to be forbidden', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number().forbidden());

            var n = [1, 2, 'hippo'];
            schema.validate(n, function (err, value) ***REMOVED***

                expect(err).to.exist();

                var m = ['x', 'y', 'z'];
                schema.validate(m, function (err2, value2) ***REMOVED***

                    expect(err2).to.not.exist();
                    done();
                ***REMOVED***);
            ***REMOVED***);
        ***REMOVED***);

        it('validates array of Numbers', function (done) ***REMOVED***

            Helper.validate(Joi.array().items(Joi.number()), [
                [[1, 2, 3], true],
                [[50, 100, 1000], true],
                [['a', 1, 2], false],
                [['1', '2', 4], true]
            ], done);
        ***REMOVED***);

        it('validates array of mixed Numbers & Strings', function (done) ***REMOVED***

            Helper.validate(Joi.array().items(Joi.number(), Joi.string()), [
                [[1, 2, 3], true],
                [[50, 100, 1000], true],
                [[1, 'a', 5, 10], true],
                [['joi', 'everydaylowprices', 5000], true]
            ], done);
        ***REMOVED***);

        it('validates array of objects with schema', function (done) ***REMOVED***

            Helper.validate(Joi.array().items(Joi.object(***REMOVED*** h1: Joi.number().required() ***REMOVED***)), [
                [[***REMOVED*** h1: 1 ***REMOVED***, ***REMOVED*** h1: 2 ***REMOVED***, ***REMOVED*** h1: 3 ***REMOVED***], true],
                [[***REMOVED*** h2: 1, h3: 'somestring' ***REMOVED***, ***REMOVED*** h1: 2 ***REMOVED***, ***REMOVED*** h1: 3 ***REMOVED***], false],
                [[1, 2, [1]], false]
            ], done);
        ***REMOVED***);

        it('errors on array of unallowed mixed types (Array)', function (done) ***REMOVED***

            Helper.validate(Joi.array().items(Joi.number()), [
                [[1, 2, 3], true],
                [[1, 2, [1]], false]
            ], done);
        ***REMOVED***);

        it('errors on invalid number rule using includes', function (done) ***REMOVED***

            var schema = Joi.object(***REMOVED***
                arr: Joi.array().items(Joi.number().integer())
            ***REMOVED***);

            var input = ***REMOVED*** arr: [1, 2, 2.1] ***REMOVED***;
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('child "arr" fails because ["arr" at position 2 fails because ["2" must be an integer]]');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates an array within an object', function (done) ***REMOVED***

            var schema = Joi.object(***REMOVED***
                array: Joi.array().items(Joi.string().min(5), Joi.number().min(3))
            ***REMOVED***).options(***REMOVED*** convert: false ***REMOVED***);

            Helper.validate(schema, [
                [***REMOVED*** array: ['12345'] ***REMOVED***, true],
                [***REMOVED*** array: ['1'] ***REMOVED***, false],
                [***REMOVED*** array: [3] ***REMOVED***, true],
                [***REMOVED*** array: ['12345', 3] ***REMOVED***, true]
            ], done);
        ***REMOVED***);

        it('should not change original value', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number()).unique();
            var input = ['1', '2'];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal([1, 2]);
                expect(input).to.deep.equal(['1', '2']);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('should have multiple errors if abort early is false', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number(), Joi.object()).items(Joi.boolean().forbidden());
            var input = [1, undefined, true, 'a'];

            Joi.validate(input, schema, ***REMOVED*** abortEarly: false ***REMOVED***, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err).to.have.length(4);
                expect(err.details).to.deep.equal([***REMOVED***
                    message: '"value" must not be a sparse array',
                    path: '1',
                    type: 'array.sparse',
                    context: ***REMOVED***
                        key: 'value'
                    ***REMOVED***
                ***REMOVED***, ***REMOVED***
                    message: '"value" at position 2 contains an excluded value',
                    path: '2',
                    type: 'array.excludes',
                    context: ***REMOVED***
                        pos: 2,
                        key: 'value',
                        value: true
                    ***REMOVED***
                ***REMOVED***, ***REMOVED***
                    message: '"value" at position 3 does not match any of the allowed types',
                    path: '3',
                    type: 'array.includes',
                    context: ***REMOVED***
                        pos: 3,
                        key: 'value',
                        value: 'a'
                    ***REMOVED***
                ***REMOVED***]);
                done();
            ***REMOVED***);
        ***REMOVED***);
    ***REMOVED***);

    describe('#describe', function () ***REMOVED***

        it('returns an empty description when no rules are applied', function (done) ***REMOVED***

            var schema = Joi.array();
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: false ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);

        it('returns an updated description when sparse rule is applied', function (done) ***REMOVED***

            var schema = Joi.array().sparse();
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: true ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);

        it('returns an items array only if items are specified', function (done) ***REMOVED***

            var schema = Joi.array().items().max(5);
            var desc = schema.describe();
            expect(desc.items).to.not.exist();
            done();
        ***REMOVED***);

        it('returns a recursively defined array of items when specified', function (done) ***REMOVED***

            var schema = Joi.array()
                .items(Joi.number(), Joi.string())
                .items(Joi.boolean().forbidden())
                .ordered(Joi.number(), Joi.string())
                .ordered(Joi.string().required());
            var desc = schema.describe();
            expect(desc.items).to.have.length(3);
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: false ***REMOVED***,
                orderedItems: [***REMOVED*** type: 'number', invalids: [Infinity, -Infinity] ***REMOVED***, ***REMOVED*** type: 'string', invalids: [''] ***REMOVED***, ***REMOVED*** type: 'string', invalids: [''], flags: ***REMOVED*** presence: 'required' ***REMOVED*** ***REMOVED***],
                items: [***REMOVED*** type: 'number', invalids: [Infinity, -Infinity] ***REMOVED***, ***REMOVED*** type: 'string', invalids: [''] ***REMOVED***, ***REMOVED*** type: 'boolean', flags: ***REMOVED*** presence: 'forbidden' ***REMOVED*** ***REMOVED***]
            ***REMOVED***);

            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#unique', function () ***REMOVED***

        it('errors if duplicate numbers, strings, objects, binaries, functions, dates and booleans', function (done) ***REMOVED***

            var buffer = new Buffer('hello world');
            var func = function () ***REMOVED******REMOVED***;
            var now = new Date();
            var schema = Joi.array().sparse().unique();

            Helper.validate(schema, [
                [[2, 2], false],
                [[02, 2], false], // eslint-disable-line no-octal
                [[0x2, 2], false],
                [['duplicate', 'duplicate'], false],
                [[***REMOVED*** a: 'b' ***REMOVED***, ***REMOVED*** a: 'b' ***REMOVED***], false],
                [[buffer, buffer], false],
                [[func, func], false],
                [[now, now], false],
                [[true, true], false],
                [[undefined, undefined], false]
            ], done);
        ***REMOVED***);

        it('ignores duplicates if they are of different types', function (done) ***REMOVED***

            var schema = Joi.array().unique();

            Helper.validate(schema, [
                [[2, '2'], true]
            ], done);
        ***REMOVED***);

        it('validates without duplicates', function (done) ***REMOVED***

            var buffer = new Buffer('hello world');
            var buffer2 = new Buffer('Hello world');
            var func = function () ***REMOVED******REMOVED***;
            var func2 = function () ***REMOVED******REMOVED***;
            var now = new Date();
            var now2 = new Date(+now + 100);
            var schema = Joi.array().unique();

            Helper.validate(schema, [
                [[1, 2], true],
                [['s1', 's2'], true],
                [[***REMOVED*** a: 'b' ***REMOVED***, ***REMOVED*** a: 'c' ***REMOVED***], true],
                [[buffer, buffer2], true],
                [[func, func2], true],
                [[now, now2], true],
                [[true, false], true]
            ], done);
        ***REMOVED***);
    ***REMOVED***);

    describe('#sparse', function () ***REMOVED***

        it('errors on undefined value', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number());

            Helper.validate(schema, [
                [[undefined], false],
                [[2, undefined], false]
            ], done);
        ***REMOVED***);

        it('validates on undefined value with sparse', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number()).sparse();

            Helper.validate(schema, [
                [[undefined], true],
                [[2, undefined], true]
            ], done);
        ***REMOVED***);

        it('switches the sparse flag', function (done) ***REMOVED***

            var schema = Joi.array().sparse();
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: true ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);

        it('switches the sparse flag with explicit value', function (done) ***REMOVED***

            var schema = Joi.array().sparse(true);
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: true ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);

        it('switches the sparse flag back', function (done) ***REMOVED***

            var schema = Joi.array().sparse().sparse(false);
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: false ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#single', function () ***REMOVED***

        it('should allow a single element', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number()).items(Joi.boolean().forbidden()).single();

            Helper.validate(schema, [
                [[1, 2, 3], true],
                [1, true],
                [['a'], false, null, '"value" at position 0 fails because ["0" must be a number]'],
                ['a', false, null, 'single value of "value" fails because ["0" must be a number]'],
                [true, false, null, 'single value of "value" contains an excluded value']
            ], done);
        ***REMOVED***);

        it('should allow a single element with multiple types', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.number(), Joi.string()).single();

            Helper.validate(schema, [
                [[1, 2, 3], true],
                [1, true],
                [[1, 'a'], true],
                ['a', true],
                [true, false, null, 'single value of "value" does not match any of the allowed types']
            ], done);
        ***REMOVED***);

        it('should allow nested arrays', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.array().items(Joi.number())).single();

            Helper.validate(schema, [
                [[[1], [2], [3]], true],
                [[1, 2, 3], true],
                [[['a']], false, null, '"value" at position 0 fails because ["0" at position 0 fails because ["0" must be a number]]'],
                [['a'], false, null, '"value" at position 0 fails because ["0" must be an array]'],
                ['a', false, null, 'single value of "value" fails because ["0" must be an array]'],
                [1, false, null, 'single value of "value" fails because ["0" must be an array]'],
                [true, false, null, 'single value of "value" fails because ["0" must be an array]']
            ], done);
        ***REMOVED***);

        it('should allow nested arrays with multiple types', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.array().items(Joi.number(), Joi.boolean())).single();

            Helper.validate(schema, [
                [[[1, true]], true],
                [[1, true], true],
                [[[1, 'a']], false, null, '"value" at position 0 fails because ["0" at position 1 does not match any of the allowed types]'],
                [[1, 'a'], false, null, '"value" at position 0 fails because ["0" must be an array]']
            ], done);
        ***REMOVED***);

        it('switches the single flag with explicit value', function (done) ***REMOVED***

            var schema = Joi.array().single(true);
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: false, single: true ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);

        it('switches the single flag back', function (done) ***REMOVED***

            var schema = Joi.array().single().single(false);
            var desc = schema.describe();
            expect(desc).to.deep.equal(***REMOVED***
                type: 'array',
                flags: ***REMOVED*** sparse: false, single: false ***REMOVED***
            ***REMOVED***);
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#options', function () ***REMOVED***

        it('respects stripUnknown', function (done) ***REMOVED***

            var schema = Joi.array().items(Joi.string()).options(***REMOVED*** stripUnknown: true ***REMOVED***);
            schema.validate(['one', 'two', 3, 4, true, false], function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['one', 'two']);
                done();
            ***REMOVED***);
        ***REMOVED***);
    ***REMOVED***);

    describe('#ordered', function () ***REMOVED***

        it('shows path to errors in array ordered items', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.array().ordered(***REMOVED***
                    a: ***REMOVED***
                        b: ***REMOVED***
                            c: ***REMOVED***
                                d: undefined
                            ***REMOVED***
                        ***REMOVED***
                    ***REMOVED***
                ***REMOVED***);
            ***REMOVED***).to.throw(Error, 'Invalid schema content: (0.a.b.c.d)');

            expect(function () ***REMOVED***

                Joi.array().ordered(***REMOVED*** foo: 'bar' ***REMOVED***, undefined);
            ***REMOVED***).to.throw(Error, 'Invalid schema content: (1)');

            done();
        ***REMOVED***);

        it('validates input against items in order', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().required()]);
            var input = ['s1', 2];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', 2]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates input with optional item', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().required(), Joi.number()]);
            var input = ['s1', 2, 3];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', 2, 3]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates input without optional item', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().required(), Joi.number()]);
            var input = ['s1', 2];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', 2]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates input without optional item', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().required(), Joi.number()]).sparse(true);
            var input = ['s1', 2, undefined];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', 2, undefined]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates input without optional item in a sparse array', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number(), Joi.number().required()]).sparse(true);
            var input = ['s1', undefined, 3];

            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', undefined, 3]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('validates when input matches ordered items and matches regular items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().required()]).items(Joi.number());
            var input = ['s1', 2, 3, 4, 5];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', 2, 3, 4, 5]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input does not match ordered items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.number().required(), Joi.string().required()]);
            var input = ['s1', 2];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 0 fails because ["0" must be a number]');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input has more items than ordered items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.number().required(), Joi.string().required()]);
            var input = [1, 's2', 3];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 2 fails because array must contain at most 2 items');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input has more items than ordered items with abortEarly = false', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string(), Joi.number()]).options(***REMOVED*** abortEarly: false ***REMOVED***);
            var input = [1, 2, 3, 4, 5];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 0 fails because ["0" must be a string]. "value" at position 2 fails because array must contain at most 2 items. "value" at position 3 fails because array must contain at most 2 items. "value" at position 4 fails because array must contain at most 2 items');
                expect(err.details).to.have.length(4);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input has less items than ordered items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.number().required(), Joi.string().required()]);
            var input = [1];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" does not contain 1 required value(s)');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input matches ordered items but not matches regular items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().required()]).items(Joi.number()).options(***REMOVED*** abortEarly: false ***REMOVED***);
            var input = ['s1', 2, 3, 4, 's5'];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 4 fails because ["4" must be a number]');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input does not match ordered items but matches regular items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string(), Joi.number()]).items(Joi.number()).options(***REMOVED*** abortEarly: false ***REMOVED***);
            var input = [1, 2, 3, 4, 5];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 0 fails because ["0" must be a string]');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors when input does not match ordered items not matches regular items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string(), Joi.number()]).items(Joi.string()).options(***REMOVED*** abortEarly: false ***REMOVED***);
            var input = [1, 2, 3, 4, 5];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 0 fails because ["0" must be a string]. "value" at position 2 fails because ["2" must be a string]. "value" at position 3 fails because ["3" must be a string]. "value" at position 4 fails because ["4" must be a string]');
                expect(err.details).to.have.length(4);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('errors but continues when abortEarly is set to false', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.number().required(), Joi.string().required()]).options(***REMOVED*** abortEarly: false ***REMOVED***);
            var input = ['s1', 2];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" at position 0 fails because ["0" must be a number]. "value" at position 1 fails because ["1" must be a string]');
                expect(err.details).to.have.length(2);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('strips item', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().required(), Joi.number().strip(), Joi.number().required()]);
            var input = ['s1', 2, 3];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal(['s1', 3]);
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('strips multiple items', function (done) ***REMOVED***

            var schema = Joi.array().ordered([Joi.string().strip(), Joi.number(), Joi.number().strip()]);
            var input = ['s1', 2, 3];
            schema.validate(input, function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value).to.deep.equal([2]);
                done();
            ***REMOVED***);
        ***REMOVED***);
    ***REMOVED***);
***REMOVED***);

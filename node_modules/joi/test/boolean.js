// Load modules

var Lab = require('lab');
var Code = require('code');
var Joi = require('../lib');
var Helper = require('./helper');


// Declare internals

var internals = ***REMOVED******REMOVED***;


// Test shortcuts

var lab = exports.lab = Lab.script();
var describe = lab.describe;
var it = lab.it;
var expect = Code.expect;


describe('boolean', function () ***REMOVED***

    it('converts a string to a boolean', function (done) ***REMOVED***

        Joi.boolean().validate('true', function (err, value) ***REMOVED***

            expect(err).to.not.exist();
            expect(value).to.equal(true);
            done();
        ***REMOVED***);
    ***REMOVED***);

    it('errors on a number', function (done) ***REMOVED***

        Joi.boolean().validate(1, function (err, value) ***REMOVED***

            expect(err).to.exist();
            expect(value).to.equal(1);
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#validate', function () ***REMOVED***

        it('converts string values and validates', function (done) ***REMOVED***

            var rule = Joi.boolean();
            Helper.validate(rule, [
                ['1234', false],
                [false, true],
                [true, true],
                [null, false],
                ['on', true],
                ['off', true],
                ['true', true],
                ['false', true],
                ['yes', true],
                ['no', true]
            ], done);
        ***REMOVED***);

        it('should handle work with required', function (done) ***REMOVED***

            var rule = Joi.boolean().required();
            Helper.validate(rule, [
                ['1234', false],
                ['true', true],
                [false, true],
                [true, true],
                [null, false]
            ], done);
        ***REMOVED***);

        it('should handle work with allow', function (done) ***REMOVED***

            var rule = Joi.boolean().allow(false);
            Helper.validate(rule, [
                ['1234', false],
                [false, true],
                [null, false]
            ], done);
        ***REMOVED***);

        it('should handle work with invalid', function (done) ***REMOVED***

            var rule = Joi.boolean().invalid(false);
            Helper.validate(rule, [
                ['1234', false],
                [false, false],
                [true, true],
                [null, false]
            ], done);
        ***REMOVED***);

        it('should handle work with invalid and null allowed', function (done) ***REMOVED***

            var rule = Joi.boolean().invalid(false).allow(null);
            Helper.validate(rule, [
                ['1234', false],
                [false, false],
                [true, true],
                [null, true]
            ], done);
        ***REMOVED***);

        it('should handle work with allow and invalid', function (done) ***REMOVED***

            var rule = Joi.boolean().invalid(true).allow(false);
            Helper.validate(rule, [
                ['1234', false],
                [false, true],
                [true, false],
                [null, false]
            ], done);
        ***REMOVED***);

        it('should handle work with allow, invalid, and null allowed', function (done) ***REMOVED***

            var rule = Joi.boolean().invalid(true).allow(false).allow(null);
            Helper.validate(rule, [
                ['1234', false],
                [false, true],
                [true, false],
                [null, true]
            ], done);
        ***REMOVED***);
    ***REMOVED***);
***REMOVED***);

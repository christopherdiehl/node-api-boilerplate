// Load modules

var Lab = require('lab');
var Code = require('code');
var Joi = require('../lib');
var Helper = require('./helper');


// Declare internals

var internals = ***REMOVED******REMOVED***;


// Test shortcuts

var lab = exports.lab = Lab.script();
var describe = lab.describe;
var it = lab.it;
var expect = Code.expect;


describe('binary', function () ***REMOVED***

    it('converts a string to a buffer', function (done) ***REMOVED***

        Joi.binary().validate('test', function (err, value) ***REMOVED***

            expect(err).to.not.exist();
            expect(value instanceof Buffer).to.equal(true);
            expect(value.length).to.equal(4);
            expect(value.toString('utf8')).to.equal('test');
            done();
        ***REMOVED***);
    ***REMOVED***);

    it('validates allowed buffer content', function (done) ***REMOVED***

        var hello = new Buffer('hello');
        var schema = Joi.binary().valid(hello);

        Helper.validate(schema, [
            ['hello', true],
            [hello, true],
            [new Buffer('hello'), true],
            ['goodbye', false],
            [new Buffer('goodbye'), false],
            [new Buffer('HELLO'), false]
        ], done);
    ***REMOVED***);

    describe('#validate', function () ***REMOVED***

        it('returns an error when a non-buffer or non-string is used', function (done) ***REMOVED***

            Joi.binary().validate(5, function (err, value) ***REMOVED***

                expect(err).to.exist();
                expect(err.message).to.equal('"value" must be a buffer or a string');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('accepts a buffer object', function (done) ***REMOVED***

            Joi.binary().validate(new Buffer('hello world'), function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value.toString('utf8')).to.equal('hello world');
                done();
            ***REMOVED***);
        ***REMOVED***);
    ***REMOVED***);

    describe('#encoding', function () ***REMOVED***

        it('applies encoding', function (done) ***REMOVED***

            var schema = Joi.binary().encoding('base64');
            var input = new Buffer('abcdef');
            schema.validate(input.toString('base64'), function (err, value) ***REMOVED***

                expect(err).to.not.exist();
                expect(value instanceof Buffer).to.equal(true);
                expect(value.toString()).to.equal('abcdef');
                done();
            ***REMOVED***);
        ***REMOVED***);

        it('throws when encoding is invalid', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().encoding('base6');
            ***REMOVED***).to.throw('Invalid encoding: base6');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#min', function () ***REMOVED***

        it('validates buffer size', function (done) ***REMOVED***

            var schema = Joi.binary().min(5);
            Helper.validate(schema, [
                [new Buffer('testing'), true],
                [new Buffer('test'), false]
            ], done);
        ***REMOVED***);

        it('throws when min is not a number', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().min('a');
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);

        it('throws when min is not an integer', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().min(1.2);
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#max', function () ***REMOVED***

        it('validates buffer size', function (done) ***REMOVED***

            var schema = Joi.binary().max(5);
            Helper.validate(schema, [
                [new Buffer('testing'), false],
                [new Buffer('test'), true]
            ], done);
        ***REMOVED***);

        it('throws when max is not a number', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().max('a');
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);

        it('throws when max is not an integer', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().max(1.2);
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);
    ***REMOVED***);

    describe('#length', function () ***REMOVED***

        it('validates buffer size', function (done) ***REMOVED***

            var schema = Joi.binary().length(4);
            Helper.validate(schema, [
                [new Buffer('test'), true],
                [new Buffer('testing'), false]
            ], done);
        ***REMOVED***);

        it('throws when length is not a number', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().length('a');
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);

        it('throws when length is not an integer', function (done) ***REMOVED***

            expect(function () ***REMOVED***

                Joi.binary().length(1.2);
            ***REMOVED***).to.throw('limit must be a positive integer');
            done();
        ***REMOVED***);
    ***REMOVED***);
***REMOVED***);

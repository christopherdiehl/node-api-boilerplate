// Load modules

var Any = require('./any');
var Errors = require('./errors');
var Hoek = require('hoek');


// Declare internals

var internals = ***REMOVED******REMOVED***;


internals.Boolean = function () ***REMOVED***

    Any.call(this);
    this._type = 'boolean';
***REMOVED***;

Hoek.inherits(internals.Boolean, Any);


internals.Boolean.prototype._base = function (value, state, options) ***REMOVED***

    var result = ***REMOVED***
        value: value
    ***REMOVED***;

    if (typeof value === 'string' &&
        options.convert) ***REMOVED***

        var lower = value.toLowerCase();
        result.value = (lower === 'true' || lower === 'yes' || lower === 'on' ? true
                                                                              : (lower === 'false' || lower === 'no' || lower === 'off' ? false : value));
    ***REMOVED***

    result.errors = (typeof result.value === 'boolean') ? null : Errors.create('boolean.base', null, state, options);
    return result;
***REMOVED***;


module.exports = new internals.Boolean();

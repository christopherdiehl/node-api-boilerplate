var RFC3986 = require('./rfc3986');

var internals = ***REMOVED***
    Ip: ***REMOVED***
        cidrs: ***REMOVED***
            required: '\\/(?:' + RFC3986.cidr + ')',
            optional: '(?:\\/(?:' + RFC3986.cidr + '))?',
            forbidden: ''
        ***REMOVED***,
        versions: ***REMOVED***
            ipv4: RFC3986.IPv4address,
            ipv6: RFC3986.IPv6address,
            ipvfuture: RFC3986.IPvFuture
        ***REMOVED***
    ***REMOVED***
***REMOVED***;

internals.Ip.createIpRegex = function (versions, cidr) ***REMOVED***

    var regex;
    for (var i = 0, il = versions.length; i < il; ++i) ***REMOVED***
        var version = versions[i];
        if (!regex) ***REMOVED***
            regex = '^(?:' + internals.Ip.versions[version];
        ***REMOVED***
        regex += '|' + internals.Ip.versions[version];
    ***REMOVED***

    return new RegExp(regex + ')' + internals.Ip.cidrs[cidr] + '$');
***REMOVED***;

module.exports = internals.Ip;

'use strict';

var env = require('../env')

if ('unknown' == env.type) ***REMOVED***
  throw new Error('Unknown environment')
***REMOVED***

module.exports =
  env.isNode ? require('./node') :
  env.isMongo ? require('./collection') :
  require('./collection');


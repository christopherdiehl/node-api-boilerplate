
var assert = require('assert')
var slice = require('sliced')
var mongo = require('mongodb')
var utils = require('../../').utils;

var uri = process.env.MQUERY_URI || 'mongodb://localhost/mquery';
var db;

exports.getCollection = function (cb) ***REMOVED***
  mongo.MongoClient.connect(uri, function (err, db_) ***REMOVED***
    assert.ifError(err);
    db = db_;

    var collection = db.collection('stuff');

    // clean test db before starting
    db.dropDatabase(function () ***REMOVED***
      cb(null, collection);
    ***REMOVED***);
  ***REMOVED***)
***REMOVED***

exports.dropCollection = function (cb) ***REMOVED***
  db.dropDatabase(function () ***REMOVED***
    db.close(cb);
  ***REMOVED***)
***REMOVED***

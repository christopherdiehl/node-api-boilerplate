
var sliced = require('../')
var assert = require('assert')

describe('sliced', function()***REMOVED***
  it('exports a function', function()***REMOVED***
    assert.equal('function', typeof sliced);
  ***REMOVED***)
  describe('with 1 arg', function()***REMOVED***
    it('returns an array of the arg', function()***REMOVED***
      var o = [3, "4", ***REMOVED******REMOVED***];
      var r = sliced(o);
      assert.equal(3, r.length);
      assert.equal(o[0], r[0]);
      assert.equal(o[1], r[1]);
      assert.equal(o[1], r[1]);
    ***REMOVED***)
  ***REMOVED***)
  describe('with 2 args', function()***REMOVED***
    it('returns an array of the arg starting at the 2nd arg', function()***REMOVED***
      var o = [3, "4", 5, null];
      var r = sliced(o, 2);
      assert.equal(2, r.length);
      assert.equal(o[2], r[0]);
      assert.equal(o[3], r[1]);
    ***REMOVED***)
  ***REMOVED***)
  describe('with 3 args', function()***REMOVED***
    it('returns an array of the arg from the 2nd to the 3rd arg', function()***REMOVED***
      var o = [3, "4", 5, null];
      var r = sliced(o, 1, 2);
      assert.equal(1, r.length);
      assert.equal(o[1], r[0]);
    ***REMOVED***)
  ***REMOVED***)
  describe('with negative start and no end', function()***REMOVED***
    it('begins at an offset from the end and includes all following elements', function()***REMOVED***
      var o = [3, "4", 5, null];
      var r = sliced(o, -2);
      assert.equal(2, r.length);
      assert.equal(o[2], r[0]);
      assert.equal(o[3], r[1]);

      var r = sliced(o, -12);
      assert.equal(4, r.length);
      assert.equal(o[0], r[0]);
      assert.equal(o[1], r[1]);
    ***REMOVED***)
  ***REMOVED***)
  describe('with negative start and positive end', function()***REMOVED***
    it('begins at an offset from the end and includes `end` elements', function()***REMOVED***
      var o = [3, "4", ***REMOVED***x:1***REMOVED***, null];

      var r = sliced(o, -2, 1);
      assert.equal(0, r.length);

      var r = sliced(o, -2, 2);
      assert.equal(0, r.length);

      var r = sliced(o, -2, 3);
      assert.equal(1, r.length);
      assert.equal(o[2], r[0]);
    ***REMOVED***)
  ***REMOVED***)
  describe('with negative start and negative end', function()***REMOVED***
    it('begins at `start` offset from the end and includes all elements up to `end` offset from the end', function()***REMOVED***
      var o = [3, "4", ***REMOVED***x:1***REMOVED***, null];
      var r = sliced(o, -3, -1);
      assert.equal(2, r.length);
      assert.equal(o[1], r[0]);
      assert.equal(o[2], r[1]);

      var r = sliced(o, -3, -3);
      assert.equal(0, r.length);

      var r = sliced(o, -3, -4);
      assert.equal(0, r.length);
    ***REMOVED***)
  ***REMOVED***)
***REMOVED***)

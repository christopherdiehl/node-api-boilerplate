'use strict';

var base64url = module.exports;

base64url.unescape = function unescape (str) ***REMOVED***
  return (str + '==='.slice((str.length + 3) % 4))
    .replace(/\-/g, '+')
    .replace(/_/g, '/');
***REMOVED***;

base64url.escape = function escape (str) ***REMOVED***
  return str.replace(/\+/g, '-')
    .replace(/\//g, '_')
    .replace(/=/g, '');
***REMOVED***;

base64url.encode = function encode (str) ***REMOVED***
  return this.escape(new Buffer(str).toString('base64'));
***REMOVED***;

base64url.decode = function decode (str) ***REMOVED***
  return new Buffer(this.unescape(str), 'base64').toString();
***REMOVED***;

/**
 * Module dependencies.
 */
var SessionStrategy = require('./strategies/session');


/**
 * `Authenticator` constructor.
 *
 * @api public
 */
function Authenticator() ***REMOVED***
  this._key = 'passport';
  this._strategies = ***REMOVED******REMOVED***;
  this._serializers = [];
  this._deserializers = [];
  this._infoTransformers = [];
  this._framework = null;
  this._userProperty = 'user';
  
  this.init();
***REMOVED***

/**
 * Initialize authenticator.
 *
 * @api protected
 */
Authenticator.prototype.init = function() ***REMOVED***
  this.framework(require('./framework/connect')());
  this.use(new SessionStrategy());
***REMOVED***;

/**
 * Utilize the given `strategy` with optional `name`, overridding the strategy's
 * default name.
 *
 * Examples:
 *
 *     passport.use(new TwitterStrategy(...));
 *
 *     passport.use('api', new http.BasicStrategy(...));
 *
 * @param ***REMOVED***String|Strategy***REMOVED*** name
 * @param ***REMOVED***Strategy***REMOVED*** strategy
 * @return ***REMOVED***Authenticator***REMOVED*** for chaining
 * @api public
 */
Authenticator.prototype.use = function(name, strategy) ***REMOVED***
  if (!strategy) ***REMOVED***
    strategy = name;
    name = strategy.name;
  ***REMOVED***
  if (!name) ***REMOVED*** throw new Error('Authentication strategies must have a name'); ***REMOVED***
  
  this._strategies[name] = strategy;
  return this;
***REMOVED***;

/**
 * Un-utilize the `strategy` with given `name`.
 *
 * In typical applications, the necessary authentication strategies are static,
 * configured once and always available.  As such, there is often no need to
 * invoke this function.
 *
 * However, in certain situations, applications may need dynamically configure
 * and de-configure authentication strategies.  The `use()`/`unuse()`
 * combination satisfies these scenarios.
 *
 * Examples:
 *
 *     passport.unuse('legacy-api');
 *
 * @param ***REMOVED***String***REMOVED*** name
 * @return ***REMOVED***Authenticator***REMOVED*** for chaining
 * @api public
 */
Authenticator.prototype.unuse = function(name) ***REMOVED***
  delete this._strategies[name];
  return this;
***REMOVED***;

/**
 * Setup Passport to be used under framework.
 *
 * By default, Passport exposes middleware that operate using Connect-style
 * middleware using a `fn(req, res, next)` signature.  Other popular frameworks
 * have different expectations, and this function allows Passport to be adapted
 * to operate within such environments.
 *
 * If you are using a Connect-compatible framework, including Express, there is
 * no need to invoke this function.
 *
 * Examples:
 *
 *     passport.framework(require('hapi-passport')());
 *
 * @param ***REMOVED***Object***REMOVED*** name
 * @return ***REMOVED***Authenticator***REMOVED*** for chaining
 * @api public
 */
Authenticator.prototype.framework = function(fw) ***REMOVED***
  this._framework = fw;
  return this;
***REMOVED***;

/**
 * Passport's primary initialization middleware.
 *
 * This middleware must be in use by the Connect/Express application for
 * Passport to operate.
 *
 * Options:
 *   - `userProperty`  Property to set on `req` upon login, defaults to _user_
 *
 * Examples:
 *
 *     app.use(passport.initialize());
 *
 *     app.use(passport.initialize(***REMOVED*** userProperty: 'currentUser' ***REMOVED***));
 *
 * @param ***REMOVED***Object***REMOVED*** options
 * @return ***REMOVED***Function***REMOVED*** middleware
 * @api public
 */
Authenticator.prototype.initialize = function(options) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;
  this._userProperty = options.userProperty || 'user';
  
  return this._framework.initialize(this, options);
***REMOVED***;

/**
 * Middleware that will authenticate a request using the given `strategy` name,
 * with optional `options` and `callback`.
 *
 * Examples:
 *
 *     passport.authenticate('local', ***REMOVED*** successRedirect: '/', failureRedirect: '/login' ***REMOVED***)(req, res);
 *
 *     passport.authenticate('local', function(err, user) ***REMOVED***
 *       if (!user) ***REMOVED*** return res.redirect('/login'); ***REMOVED***
 *       res.end('Authenticated!');
 *     ***REMOVED***)(req, res);
 *
 *     passport.authenticate('basic', ***REMOVED*** session: false ***REMOVED***)(req, res);
 *
 *     app.get('/auth/twitter', passport.authenticate('twitter'), function(req, res) ***REMOVED***
 *       // request will be redirected to Twitter
 *     ***REMOVED***);
 *     app.get('/auth/twitter/callback', passport.authenticate('twitter'), function(req, res) ***REMOVED***
 *       res.json(req.user);
 *     ***REMOVED***);
 *
 * @param ***REMOVED***String***REMOVED*** strategy
 * @param ***REMOVED***Object***REMOVED*** options
 * @param ***REMOVED***Function***REMOVED*** callback
 * @return ***REMOVED***Function***REMOVED*** middleware
 * @api public
 */
Authenticator.prototype.authenticate = function(strategy, options, callback) ***REMOVED***
  return this._framework.authenticate(this, strategy, options, callback);
***REMOVED***;

/**
 * Middleware that will authorize a third-party account using the given
 * `strategy` name, with optional `options`.
 *
 * If authorization is successful, the result provided by the strategy's verify
 * callback will be assigned to `req.account`.  The existing login session and
 * `req.user` will be unaffected.
 *
 * This function is particularly useful when connecting third-party accounts
 * to the local account of a user that is currently authenticated.
 *
 * Examples:
 *
 *    passport.authorize('twitter-authz', ***REMOVED*** failureRedirect: '/account' ***REMOVED***);
 *
 * @param ***REMOVED***String***REMOVED*** strategy
 * @param ***REMOVED***Object***REMOVED*** options
 * @return ***REMOVED***Function***REMOVED*** middleware
 * @api public
 */
Authenticator.prototype.authorize = function(strategy, options, callback) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;
  options.assignProperty = 'account';
  
  var fn = this._framework.authorize || this._framework.authenticate;
  return fn(this, strategy, options, callback);
***REMOVED***;

/**
 * Middleware that will restore login state from a session.
 *
 * Web applications typically use sessions to maintain login state between
 * requests.  For example, a user will authenticate by entering credentials into
 * a form which is submitted to the server.  If the credentials are valid, a
 * login session is established by setting a cookie containing a session
 * identifier in the user's web browser.  The web browser will send this cookie
 * in subsequent requests to the server, allowing a session to be maintained.
 *
 * If sessions are being utilized, and a login session has been established,
 * this middleware will populate `req.user` with the current user.
 *
 * Note that sessions are not strictly required for Passport to operate.
 * However, as a general rule, most web applications will make use of sessions.
 * An exception to this rule would be an API server, which expects each HTTP
 * request to provide credentials in an Authorization header.
 *
 * Examples:
 *
 *     app.use(connect.cookieParser());
 *     app.use(connect.session(***REMOVED*** secret: 'keyboard cat' ***REMOVED***));
 *     app.use(passport.initialize());
 *     app.use(passport.session());
 *
 * Options:
 *   - `pauseStream`      Pause the request stream before deserializing the user
 *                        object from the session.  Defaults to _false_.  Should
 *                        be set to true in cases where middleware consuming the
 *                        request body is configured after passport and the
 *                        deserializeUser method is asynchronous.
 *
 * @param ***REMOVED***Object***REMOVED*** options
 * @return ***REMOVED***Function***REMOVED*** middleware
 * @api public
 */
Authenticator.prototype.session = function(options) ***REMOVED***
  return this.authenticate('session', options);
***REMOVED***;

/**
 * Registers a function used to serialize user objects into the session.
 *
 * Examples:
 *
 *     passport.serializeUser(function(user, done) ***REMOVED***
 *       done(null, user.id);
 *     ***REMOVED***);
 *
 * @api public
 */
Authenticator.prototype.serializeUser = function(fn, req, done) ***REMOVED***
  if (typeof fn === 'function') ***REMOVED***
    return this._serializers.push(fn);
  ***REMOVED***
  
  // private implementation that traverses the chain of serializers, attempting
  // to serialize a user
  var user = fn;

  // For backwards compatibility
  if (typeof req === 'function') ***REMOVED***
    done = req;
    req = undefined;
  ***REMOVED***
  
  var stack = this._serializers;
  (function pass(i, err, obj) ***REMOVED***
    // serializers use 'pass' as an error to skip processing
    if ('pass' === err) ***REMOVED***
      err = undefined;
    ***REMOVED***
    // an error or serialized object was obtained, done
    if (err || obj || obj === 0) ***REMOVED*** return done(err, obj); ***REMOVED***
    
    var layer = stack[i];
    if (!layer) ***REMOVED***
      return done(new Error('Failed to serialize user into session'));
    ***REMOVED***
    
    
    function serialized(e, o) ***REMOVED***
      pass(i + 1, e, o);
    ***REMOVED***
    
    try ***REMOVED***
      var arity = layer.length;
      if (arity == 3) ***REMOVED***
        layer(req, user, serialized);
      ***REMOVED*** else ***REMOVED***
        layer(user, serialized);
      ***REMOVED***
    ***REMOVED*** catch(e) ***REMOVED***
      return done(e);
    ***REMOVED***
  ***REMOVED***)(0);
***REMOVED***;

/**
 * Registers a function used to deserialize user objects out of the session.
 *
 * Examples:
 *
 *     passport.deserializeUser(function(id, done) ***REMOVED***
 *       User.findById(id, function (err, user) ***REMOVED***
 *         done(err, user);
 *       ***REMOVED***);
 *     ***REMOVED***);
 *
 * @api public
 */
Authenticator.prototype.deserializeUser = function(fn, req, done) ***REMOVED***
  if (typeof fn === 'function') ***REMOVED***
    return this._deserializers.push(fn);
  ***REMOVED***
  
  // private implementation that traverses the chain of deserializers,
  // attempting to deserialize a user
  var obj = fn;

  // For backwards compatibility
  if (typeof req === 'function') ***REMOVED***
    done = req;
    req = undefined;
  ***REMOVED***
  
  var stack = this._deserializers;
  (function pass(i, err, user) ***REMOVED***
    // deserializers use 'pass' as an error to skip processing
    if ('pass' === err) ***REMOVED***
      err = undefined;
    ***REMOVED***
    // an error or deserialized user was obtained, done
    if (err || user) ***REMOVED*** return done(err, user); ***REMOVED***
    // a valid user existed when establishing the session, but that user has
    // since been removed
    if (user === null || user === false) ***REMOVED*** return done(null, false); ***REMOVED***
    
    var layer = stack[i];
    if (!layer) ***REMOVED***
      return done(new Error('Failed to deserialize user out of session'));
    ***REMOVED***
    
    
    function deserialized(e, u) ***REMOVED***
      pass(i + 1, e, u);
    ***REMOVED***
    
    try ***REMOVED***
      var arity = layer.length;
      if (arity == 3) ***REMOVED***
        layer(req, obj, deserialized);
      ***REMOVED*** else ***REMOVED***
        layer(obj, deserialized);
      ***REMOVED***
    ***REMOVED*** catch(e) ***REMOVED***
      return done(e);
    ***REMOVED***
  ***REMOVED***)(0);
***REMOVED***;

/**
 * Registers a function used to transform auth info.
 *
 * In some circumstances authorization details are contained in authentication
 * credentials or loaded as part of verification.
 *
 * For example, when using bearer tokens for API authentication, the tokens may
 * encode (either directly or indirectly in a database), details such as scope
 * of access or the client to which the token was issued.
 *
 * Such authorization details should be enforced separately from authentication.
 * Because Passport deals only with the latter, this is the responsiblity of
 * middleware or routes further along the chain.  However, it is not optimal to
 * decode the same data or execute the same database query later.  To avoid
 * this, Passport accepts optional `info` along with the authenticated `user`
 * in a strategy's `success()` action.  This info is set at `req.authInfo`,
 * where said later middlware or routes can access it.
 *
 * Optionally, applications can register transforms to proccess this info,
 * which take effect prior to `req.authInfo` being set.  This is useful, for
 * example, when the info contains a client ID.  The transform can load the
 * client from the database and include the instance in the transformed info,
 * allowing the full set of client properties to be convieniently accessed.
 *
 * If no transforms are registered, `info` supplied by the strategy will be left
 * unmodified.
 *
 * Examples:
 *
 *     passport.transformAuthInfo(function(info, done) ***REMOVED***
 *       Client.findById(info.clientID, function (err, client) ***REMOVED***
 *         info.client = client;
 *         done(err, info);
 *       ***REMOVED***);
 *     ***REMOVED***);
 *
 * @api public
 */
Authenticator.prototype.transformAuthInfo = function(fn, req, done) ***REMOVED***
  if (typeof fn === 'function') ***REMOVED***
    return this._infoTransformers.push(fn);
  ***REMOVED***
  
  // private implementation that traverses the chain of transformers,
  // attempting to transform auth info
  var info = fn;

  // For backwards compatibility
  if (typeof req === 'function') ***REMOVED***
    done = req;
    req = undefined;
  ***REMOVED***
  
  var stack = this._infoTransformers;
  (function pass(i, err, tinfo) ***REMOVED***
    // transformers use 'pass' as an error to skip processing
    if ('pass' === err) ***REMOVED***
      err = undefined;
    ***REMOVED***
    // an error or transformed info was obtained, done
    if (err || tinfo) ***REMOVED*** return done(err, tinfo); ***REMOVED***
    
    var layer = stack[i];
    if (!layer) ***REMOVED***
      // if no transformers are registered (or they all pass), the default
      // behavior is to use the un-transformed info as-is
      return done(null, info);
    ***REMOVED***
    
    
    function transformed(e, t) ***REMOVED***
      pass(i + 1, e, t);
    ***REMOVED***
    
    try ***REMOVED***
      var arity = layer.length;
      if (arity == 1) ***REMOVED***
        // sync
        var t = layer(info);
        transformed(null, t);
      ***REMOVED*** else if (arity == 3) ***REMOVED***
        layer(req, info, transformed);
      ***REMOVED*** else ***REMOVED***
        layer(info, transformed);
      ***REMOVED***
    ***REMOVED*** catch(e) ***REMOVED***
      return done(e);
    ***REMOVED***
  ***REMOVED***)(0);
***REMOVED***;

/**
 * Return strategy with given `name`. 
 *
 * @param ***REMOVED***String***REMOVED*** name
 * @return ***REMOVED***Strategy***REMOVED***
 * @api private
 */
Authenticator.prototype._strategy = function(name) ***REMOVED***
  return this._strategies[name];
***REMOVED***;


/**
 * Expose `Authenticator`.
 */
module.exports = Authenticator;

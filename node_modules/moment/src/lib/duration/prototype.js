import ***REMOVED*** Duration ***REMOVED*** from './constructor';

var proto = Duration.prototype;

import ***REMOVED*** abs ***REMOVED*** from './abs';
import ***REMOVED*** add, subtract ***REMOVED*** from './add-subtract';
import ***REMOVED*** as, asMilliseconds, asSeconds, asMinutes, asHours, asDays, asWeeks, asMonths, asYears, valueOf ***REMOVED*** from './as';
import ***REMOVED*** bubble ***REMOVED*** from './bubble';
import ***REMOVED*** get, milliseconds, seconds, minutes, hours, days, months, years, weeks ***REMOVED*** from './get';
import ***REMOVED*** humanize ***REMOVED*** from './humanize';
import ***REMOVED*** toISOString ***REMOVED*** from './iso-string';
import ***REMOVED*** lang, locale, localeData ***REMOVED*** from '../moment/locale';

proto.abs            = abs;
proto.add            = add;
proto.subtract       = subtract;
proto.as             = as;
proto.asMilliseconds = asMilliseconds;
proto.asSeconds      = asSeconds;
proto.asMinutes      = asMinutes;
proto.asHours        = asHours;
proto.asDays         = asDays;
proto.asWeeks        = asWeeks;
proto.asMonths       = asMonths;
proto.asYears        = asYears;
proto.valueOf        = valueOf;
proto._bubble        = bubble;
proto.get            = get;
proto.milliseconds   = milliseconds;
proto.seconds        = seconds;
proto.minutes        = minutes;
proto.hours          = hours;
proto.days           = days;
proto.weeks          = weeks;
proto.months         = months;
proto.years          = years;
proto.humanize       = humanize;
proto.toISOString    = toISOString;
proto.toString       = toISOString;
proto.toJSON         = toISOString;
proto.locale         = locale;
proto.localeData     = localeData;

// Deprecations
import ***REMOVED*** deprecate ***REMOVED*** from '../utils/deprecate';

proto.toIsoString = deprecate('toIsoString() is deprecated. Please use toISOString() instead (notice the capitals)', toISOString);
proto.lang = lang;

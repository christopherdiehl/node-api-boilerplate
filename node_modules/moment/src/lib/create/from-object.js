import ***REMOVED*** normalizeObjectUnits ***REMOVED*** from '../units/aliases';
import ***REMOVED*** configFromArray ***REMOVED*** from './from-array';
import map from '../utils/map';

export function configFromObject(config) ***REMOVED***
    if (config._d) ***REMOVED***
        return;
    ***REMOVED***

    var i = normalizeObjectUnits(config._i);
    config._a = map([i.year, i.month, i.day || i.date, i.hour, i.minute, i.second, i.millisecond], function (obj) ***REMOVED***
        return obj && parseInt(obj, 10);
    ***REMOVED***);

    configFromArray(config);
***REMOVED***

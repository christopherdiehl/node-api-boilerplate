import ***REMOVED*** createLocalOrUTC ***REMOVED*** from './from-anything';

export function createUTC (input, format, locale, strict) ***REMOVED***
    return createLocalOrUTC(input, format, locale, strict, true).utc();
***REMOVED***

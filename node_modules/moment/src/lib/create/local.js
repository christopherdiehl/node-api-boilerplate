import ***REMOVED*** createLocalOrUTC ***REMOVED*** from './from-anything';

export function createLocal (input, format, locale, strict) ***REMOVED***
    return createLocalOrUTC(input, format, locale, strict, false);
***REMOVED***

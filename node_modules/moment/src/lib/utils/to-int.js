import absFloor from './abs-floor';

export default function toInt(argumentForCoercion) ***REMOVED***
    var coercedNumber = +argumentForCoercion,
        value = 0;

    if (coercedNumber !== 0 && isFinite(coercedNumber)) ***REMOVED***
        value = absFloor(coercedNumber);
    ***REMOVED***

    return value;
***REMOVED***

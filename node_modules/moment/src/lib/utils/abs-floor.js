export default function absFloor (number) ***REMOVED***
    if (number < 0) ***REMOVED***
        // -0 -> 0
        return Math.ceil(number) || 0;
    ***REMOVED*** else ***REMOVED***
        return Math.floor(number);
    ***REMOVED***
***REMOVED***

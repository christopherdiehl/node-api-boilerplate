export default function map(arr, fn) ***REMOVED***
    var res = [], i;
    for (i = 0; i < arr.length; ++i) ***REMOVED***
        res.push(fn(arr[i], i));
    ***REMOVED***
    return res;
***REMOVED***

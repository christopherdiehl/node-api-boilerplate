import ***REMOVED*** normalizeUnits ***REMOVED*** from '../units/aliases';

export function startOf (units) ***REMOVED***
    units = normalizeUnits(units);
    // the following switch intentionally omits break keywords
    // to utilize falling through the cases.
    switch (units) ***REMOVED***
        case 'year':
            this.month(0);
            /* falls through */
        case 'quarter':
        case 'month':
            this.date(1);
            /* falls through */
        case 'week':
        case 'isoWeek':
        case 'day':
        case 'date':
            this.hours(0);
            /* falls through */
        case 'hour':
            this.minutes(0);
            /* falls through */
        case 'minute':
            this.seconds(0);
            /* falls through */
        case 'second':
            this.milliseconds(0);
    ***REMOVED***

    // weeks are a special case
    if (units === 'week') ***REMOVED***
        this.weekday(0);
    ***REMOVED***
    if (units === 'isoWeek') ***REMOVED***
        this.isoWeekday(1);
    ***REMOVED***

    // quarters are also special
    if (units === 'quarter') ***REMOVED***
        this.month(Math.floor(this.month() / 3) * 3);
    ***REMOVED***

    return this;
***REMOVED***

export function endOf (units) ***REMOVED***
    units = normalizeUnits(units);
    if (units === undefined || units === 'millisecond') ***REMOVED***
        return this;
    ***REMOVED***

    // 'date' is an alias for 'day', so it should be considered as such.
    if (units === 'date') ***REMOVED***
        units = 'day';
    ***REMOVED***

    return this.startOf(units).add(1, (units === 'isoWeek' ? 'week' : units)).subtract(1, 'ms');
***REMOVED***

export function valueOf () ***REMOVED***
    return this._d.valueOf() - ((this._offset || 0) * 60000);
***REMOVED***

export function unix () ***REMOVED***
    return Math.floor(this.valueOf() / 1000);
***REMOVED***

export function toDate () ***REMOVED***
    return new Date(this.valueOf());
***REMOVED***

export function toArray () ***REMOVED***
    var m = this;
    return [m.year(), m.month(), m.date(), m.hour(), m.minute(), m.second(), m.millisecond()];
***REMOVED***

export function toObject () ***REMOVED***
    var m = this;
    return ***REMOVED***
        years: m.year(),
        months: m.month(),
        date: m.date(),
        hours: m.hours(),
        minutes: m.minutes(),
        seconds: m.seconds(),
        milliseconds: m.milliseconds()
    ***REMOVED***;
***REMOVED***

export function toJSON () ***REMOVED***
    // new Date(NaN).toJSON() === null
    return this.isValid() ? this.toISOString() : null;
***REMOVED***

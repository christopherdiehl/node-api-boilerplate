import ***REMOVED*** Moment ***REMOVED*** from './constructor';

var proto = Moment.prototype;

import ***REMOVED*** add, subtract ***REMOVED*** from './add-subtract';
import ***REMOVED*** calendar, getCalendarFormat ***REMOVED*** from './calendar';
import ***REMOVED*** clone ***REMOVED*** from './clone';
import ***REMOVED*** isBefore, isBetween, isSame, isAfter, isSameOrAfter, isSameOrBefore ***REMOVED*** from './compare';
import ***REMOVED*** diff ***REMOVED*** from './diff';
import ***REMOVED*** format, toString, toISOString, inspect ***REMOVED*** from './format';
import ***REMOVED*** from, fromNow ***REMOVED*** from './from';
import ***REMOVED*** to, toNow ***REMOVED*** from './to';
import ***REMOVED*** stringGet, stringSet ***REMOVED*** from './get-set';
import ***REMOVED*** locale, localeData, lang ***REMOVED*** from './locale';
import ***REMOVED*** prototypeMin, prototypeMax ***REMOVED*** from './min-max';
import ***REMOVED*** startOf, endOf ***REMOVED*** from './start-end-of';
import ***REMOVED*** valueOf, toDate, toArray, toObject, toJSON, unix ***REMOVED*** from './to-type';
import ***REMOVED*** isValid, parsingFlags, invalidAt ***REMOVED*** from './valid';
import ***REMOVED*** creationData ***REMOVED*** from './creation-data';

proto.add               = add;
proto.calendar          = calendar;
proto.clone             = clone;
proto.diff              = diff;
proto.endOf             = endOf;
proto.format            = format;
proto.from              = from;
proto.fromNow           = fromNow;
proto.to                = to;
proto.toNow             = toNow;
proto.get               = stringGet;
proto.invalidAt         = invalidAt;
proto.isAfter           = isAfter;
proto.isBefore          = isBefore;
proto.isBetween         = isBetween;
proto.isSame            = isSame;
proto.isSameOrAfter     = isSameOrAfter;
proto.isSameOrBefore    = isSameOrBefore;
proto.isValid           = isValid;
proto.lang              = lang;
proto.locale            = locale;
proto.localeData        = localeData;
proto.max               = prototypeMax;
proto.min               = prototypeMin;
proto.parsingFlags      = parsingFlags;
proto.set               = stringSet;
proto.startOf           = startOf;
proto.subtract          = subtract;
proto.toArray           = toArray;
proto.toObject          = toObject;
proto.toDate            = toDate;
proto.toISOString       = toISOString;
proto.inspect           = inspect;
proto.toJSON            = toJSON;
proto.toString          = toString;
proto.unix              = unix;
proto.valueOf           = valueOf;
proto.creationData      = creationData;

// Year
import ***REMOVED*** getSetYear, getIsLeapYear ***REMOVED*** from '../units/year';
proto.year       = getSetYear;
proto.isLeapYear = getIsLeapYear;

// Week Year
import ***REMOVED*** getSetWeekYear, getSetISOWeekYear, getWeeksInYear, getISOWeeksInYear ***REMOVED*** from '../units/week-year';
proto.weekYear    = getSetWeekYear;
proto.isoWeekYear = getSetISOWeekYear;

// Quarter
import ***REMOVED*** getSetQuarter ***REMOVED*** from '../units/quarter';
proto.quarter = proto.quarters = getSetQuarter;

// Month
import ***REMOVED*** getSetMonth, getDaysInMonth ***REMOVED*** from '../units/month';
proto.month       = getSetMonth;
proto.daysInMonth = getDaysInMonth;

// Week
import ***REMOVED*** getSetWeek, getSetISOWeek ***REMOVED*** from '../units/week';
proto.week           = proto.weeks        = getSetWeek;
proto.isoWeek        = proto.isoWeeks     = getSetISOWeek;
proto.weeksInYear    = getWeeksInYear;
proto.isoWeeksInYear = getISOWeeksInYear;

// Day
import ***REMOVED*** getSetDayOfMonth ***REMOVED*** from '../units/day-of-month';
import ***REMOVED*** getSetDayOfWeek, getSetISODayOfWeek, getSetLocaleDayOfWeek ***REMOVED*** from '../units/day-of-week';
import ***REMOVED*** getSetDayOfYear ***REMOVED*** from '../units/day-of-year';
proto.date       = getSetDayOfMonth;
proto.day        = proto.days             = getSetDayOfWeek;
proto.weekday    = getSetLocaleDayOfWeek;
proto.isoWeekday = getSetISODayOfWeek;
proto.dayOfYear  = getSetDayOfYear;

// Hour
import ***REMOVED*** getSetHour ***REMOVED*** from '../units/hour';
proto.hour = proto.hours = getSetHour;

// Minute
import ***REMOVED*** getSetMinute ***REMOVED*** from '../units/minute';
proto.minute = proto.minutes = getSetMinute;

// Second
import ***REMOVED*** getSetSecond ***REMOVED*** from '../units/second';
proto.second = proto.seconds = getSetSecond;

// Millisecond
import ***REMOVED*** getSetMillisecond ***REMOVED*** from '../units/millisecond';
proto.millisecond = proto.milliseconds = getSetMillisecond;

// Offset
import ***REMOVED***
    getSetOffset,
    setOffsetToUTC,
    setOffsetToLocal,
    setOffsetToParsedOffset,
    hasAlignedHourOffset,
    isDaylightSavingTime,
    isDaylightSavingTimeShifted,
    getSetZone,
    isLocal,
    isUtcOffset,
    isUtc
***REMOVED*** from '../units/offset';
proto.utcOffset            = getSetOffset;
proto.utc                  = setOffsetToUTC;
proto.local                = setOffsetToLocal;
proto.parseZone            = setOffsetToParsedOffset;
proto.hasAlignedHourOffset = hasAlignedHourOffset;
proto.isDST                = isDaylightSavingTime;
proto.isLocal              = isLocal;
proto.isUtcOffset          = isUtcOffset;
proto.isUtc                = isUtc;
proto.isUTC                = isUtc;

// Timezone
import ***REMOVED*** getZoneAbbr, getZoneName ***REMOVED*** from '../units/timezone';
proto.zoneAbbr = getZoneAbbr;
proto.zoneName = getZoneName;

// Deprecations
import ***REMOVED*** deprecate ***REMOVED*** from '../utils/deprecate';
proto.dates  = deprecate('dates accessor is deprecated. Use date instead.', getSetDayOfMonth);
proto.months = deprecate('months accessor is deprecated. Use month instead', getSetMonth);
proto.years  = deprecate('years accessor is deprecated. Use year instead', getSetYear);
proto.zone   = deprecate('moment().zone is deprecated, use moment().utcOffset instead. http://momentjs.com/guides/#/warnings/zone/', getSetZone);
proto.isDSTShifted = deprecate('isDSTShifted is deprecated. See http://momentjs.com/guides/#/warnings/dst-shifted/ for more information', isDaylightSavingTimeShifted);

export default proto;

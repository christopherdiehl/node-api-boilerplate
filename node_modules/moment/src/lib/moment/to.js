import ***REMOVED*** createDuration ***REMOVED*** from '../duration/create';
import ***REMOVED*** createLocal ***REMOVED*** from '../create/local';
import ***REMOVED*** isMoment ***REMOVED*** from '../moment/constructor';

export function to (time, withoutSuffix) ***REMOVED***
    if (this.isValid() &&
            ((isMoment(time) && time.isValid()) ||
             createLocal(time).isValid())) ***REMOVED***
        return createDuration(***REMOVED***from: this, to: time***REMOVED***).locale(this.locale()).humanize(!withoutSuffix);
    ***REMOVED*** else ***REMOVED***
        return this.localeData().invalidDate();
    ***REMOVED***
***REMOVED***

export function toNow (withoutSuffix) ***REMOVED***
    return this.to(createLocal(), withoutSuffix);
***REMOVED***

import ***REMOVED*** createDuration ***REMOVED*** from '../duration/create';
import ***REMOVED*** createLocal ***REMOVED*** from '../create/local';
import ***REMOVED*** isMoment ***REMOVED*** from '../moment/constructor';

export function from (time, withoutSuffix) ***REMOVED***
    if (this.isValid() &&
            ((isMoment(time) && time.isValid()) ||
             createLocal(time).isValid())) ***REMOVED***
        return createDuration(***REMOVED***to: this, from: time***REMOVED***).locale(this.locale()).humanize(!withoutSuffix);
    ***REMOVED*** else ***REMOVED***
        return this.localeData().invalidDate();
    ***REMOVED***
***REMOVED***

export function fromNow (withoutSuffix) ***REMOVED***
    return this.from(createLocal(), withoutSuffix);
***REMOVED***

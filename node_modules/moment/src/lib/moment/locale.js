import ***REMOVED*** getLocale ***REMOVED*** from '../locale/locales';
import ***REMOVED*** deprecate ***REMOVED*** from '../utils/deprecate';

// If passed a locale key, it will set the locale for this
// instance.  Otherwise, it will return the locale configuration
// variables for this instance.
export function locale (key) ***REMOVED***
    var newLocaleData;

    if (key === undefined) ***REMOVED***
        return this._locale._abbr;
    ***REMOVED*** else ***REMOVED***
        newLocaleData = getLocale(key);
        if (newLocaleData != null) ***REMOVED***
            this._locale = newLocaleData;
        ***REMOVED***
        return this;
    ***REMOVED***
***REMOVED***

export var lang = deprecate(
    'moment().lang() is deprecated. Instead, use moment().localeData() to get the language configuration. Use moment().locale() to change languages.',
    function (key) ***REMOVED***
        if (key === undefined) ***REMOVED***
            return this.localeData();
        ***REMOVED*** else ***REMOVED***
            return this.locale(key);
        ***REMOVED***
    ***REMOVED***
);

export function localeData () ***REMOVED***
    return this._locale;
***REMOVED***

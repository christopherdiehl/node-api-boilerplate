import ***REMOVED*** addFormatToken ***REMOVED*** from '../format/format';
import ***REMOVED*** addRegexToken, matchTimestamp, matchSigned ***REMOVED*** from '../parse/regex';
import ***REMOVED*** addParseToken ***REMOVED*** from '../parse/token';
import toInt from '../utils/to-int';

// FORMATTING

addFormatToken('X', 0, 0, 'unix');
addFormatToken('x', 0, 0, 'valueOf');

// PARSING

addRegexToken('x', matchSigned);
addRegexToken('X', matchTimestamp);
addParseToken('X', function (input, array, config) ***REMOVED***
    config._d = new Date(parseFloat(input, 10) * 1000);
***REMOVED***);
addParseToken('x', function (input, array, config) ***REMOVED***
    config._d = new Date(toInt(input));
***REMOVED***);

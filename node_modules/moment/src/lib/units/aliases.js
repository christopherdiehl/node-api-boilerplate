import hasOwnProp from '../utils/has-own-prop';

var aliases = ***REMOVED******REMOVED***;

export function addUnitAlias (unit, shorthand) ***REMOVED***
    var lowerCase = unit.toLowerCase();
    aliases[lowerCase] = aliases[lowerCase + 's'] = aliases[shorthand] = unit;
***REMOVED***

export function normalizeUnits(units) ***REMOVED***
    return typeof units === 'string' ? aliases[units] || aliases[units.toLowerCase()] : undefined;
***REMOVED***

export function normalizeObjectUnits(inputObject) ***REMOVED***
    var normalizedInput = ***REMOVED******REMOVED***,
        normalizedProp,
        prop;

    for (prop in inputObject) ***REMOVED***
        if (hasOwnProp(inputObject, prop)) ***REMOVED***
            normalizedProp = normalizeUnits(prop);
            if (normalizedProp) ***REMOVED***
                normalizedInput[normalizedProp] = inputObject[prop];
            ***REMOVED***
        ***REMOVED***
    ***REMOVED***

    return normalizedInput;
***REMOVED***


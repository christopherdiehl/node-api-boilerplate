import ***REMOVED*** addFormatToken ***REMOVED*** from '../format/format';
import ***REMOVED*** addUnitAlias ***REMOVED*** from './aliases';
import ***REMOVED*** addUnitPriority ***REMOVED*** from './priorities';
import ***REMOVED*** addRegexToken, match3, match1to3 ***REMOVED*** from '../parse/regex';
import ***REMOVED*** daysInYear ***REMOVED*** from './year';
import ***REMOVED*** createUTCDate ***REMOVED*** from '../create/date-from-array';
import ***REMOVED*** addParseToken ***REMOVED*** from '../parse/token';
import toInt from '../utils/to-int';

// FORMATTING

addFormatToken('DDD', ['DDDD', 3], 'DDDo', 'dayOfYear');

// ALIASES

addUnitAlias('dayOfYear', 'DDD');

// PRIORITY
addUnitPriority('dayOfYear', 4);

// PARSING

addRegexToken('DDD',  match1to3);
addRegexToken('DDDD', match3);
addParseToken(['DDD', 'DDDD'], function (input, array, config) ***REMOVED***
    config._dayOfYear = toInt(input);
***REMOVED***);

// HELPERS

// MOMENTS

export function getSetDayOfYear (input) ***REMOVED***
    var dayOfYear = Math.round((this.clone().startOf('day') - this.clone().startOf('year')) / 864e5) + 1;
    return input == null ? dayOfYear : this.add((input - dayOfYear), 'd');
***REMOVED***

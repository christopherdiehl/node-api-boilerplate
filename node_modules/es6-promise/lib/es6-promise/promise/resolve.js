import ***REMOVED***
  noop,
  resolve as _resolve
***REMOVED*** from '../-internal';

/**
  `Promise.resolve` returns a promise that will become resolved with the
  passed `value`. It is shorthand for the following:

  ```javascript
  var promise = new Promise(function(resolve, reject)***REMOVED***
    resolve(1);
  ***REMOVED***);

  promise.then(function(value)***REMOVED***
    // value === 1
  ***REMOVED***);
  ```

  Instead of writing the above, your code now simply becomes the following:

  ```javascript
  var promise = Promise.resolve(1);

  promise.then(function(value)***REMOVED***
    // value === 1
  ***REMOVED***);
  ```

  @method resolve
  @static
  @param ***REMOVED***Any***REMOVED*** value value that the returned promise will be resolved with
  Useful for tooling.
  @return ***REMOVED***Promise***REMOVED*** a promise that will become fulfilled with the given
  `value`
*/
export default function resolve(object) ***REMOVED***
  /*jshint validthis:true */
  var Constructor = this;

  if (object && typeof object === 'object' && object.constructor === Constructor) ***REMOVED***
    return object;
  ***REMOVED***

  var promise = new Constructor(noop);
  _resolve(promise, object);
  return promise;
***REMOVED***

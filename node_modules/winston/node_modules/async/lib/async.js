/*!
 * async
 * https://github.com/caolan/async
 *
 * Copyright 2010-2014 Caolan McMahon
 * Released under the MIT license
 */
(function () ***REMOVED***

    var async = ***REMOVED******REMOVED***;
    var noop = function () ***REMOVED******REMOVED***;

    // global on the server, window in the browser
    var root, previous_async;

    if (typeof window == 'object' && this === window) ***REMOVED***
        root = window;
    ***REMOVED***
    else if (typeof global == 'object' && this === global) ***REMOVED***
        root = global;
    ***REMOVED***
    else ***REMOVED***
        root = this;
    ***REMOVED***

    if (root != null) ***REMOVED***
      previous_async = root.async;
    ***REMOVED***

    async.noConflict = function () ***REMOVED***
        root.async = previous_async;
        return async;
    ***REMOVED***;

    function only_once(fn) ***REMOVED***
        var called = false;
        return function() ***REMOVED***
            if (called) throw new Error("Callback was already called.");
            called = true;
            fn.apply(root, arguments);
        ***REMOVED***;
    ***REMOVED***

    //// cross-browser compatiblity functions ////

    var _toString = Object.prototype.toString;

    var _isArray = Array.isArray || function (obj) ***REMOVED***
        return _toString.call(obj) === '[object Array]';
    ***REMOVED***;

    var _each = function (arr, iterator) ***REMOVED***
      var index = -1,
          length = arr.length;

      while (++index < length) ***REMOVED***
        iterator(arr[index], index, arr);
      ***REMOVED***
    ***REMOVED***;

    var _map = function (arr, iterator) ***REMOVED***
      var index = -1,
          length = arr.length,
          result = Array(length);

      while (++index < length) ***REMOVED***
        result[index] = iterator(arr[index], index, arr);
      ***REMOVED***
      return result;
    ***REMOVED***;

    var _reduce = function (arr, iterator, memo) ***REMOVED***
        _each(arr, function (x, i, a) ***REMOVED***
            memo = iterator(memo, x, i, a);
        ***REMOVED***);
        return memo;
    ***REMOVED***;

    var _forEachOf = function (object, iterator) ***REMOVED***
        _each(_keys(object), function (key) ***REMOVED***
            iterator(object[key], key);
        ***REMOVED***);
    ***REMOVED***;

    var _keys = Object.keys || function (obj) ***REMOVED***
        var keys = [];
        for (var k in obj) ***REMOVED***
            if (obj.hasOwnProperty(k)) ***REMOVED***
                keys.push(k);
            ***REMOVED***
        ***REMOVED***
        return keys;
    ***REMOVED***;

    var _baseSlice = function (arr, start) ***REMOVED***
        start = start || 0;
        var index = -1;
        var length = arr.length;

        if (start) ***REMOVED***
          length -= start;
          length = length < 0 ? 0 : length;
        ***REMOVED***
        var result = Array(length);

        while (++index < length) ***REMOVED***
          result[index] = arr[index + start];
        ***REMOVED***
        return result;
    ***REMOVED***;

    //// exported async module functions ////

    //// nextTick implementation with browser-compatible fallback ////

    // capture the global reference to guard against fakeTimer mocks
    var _setImmediate;
    if (typeof setImmediate === 'function') ***REMOVED***
        _setImmediate = setImmediate;
    ***REMOVED***

    if (typeof process === 'undefined' || !(process.nextTick)) ***REMOVED***
        if (_setImmediate) ***REMOVED***
            async.nextTick = function (fn) ***REMOVED***
                // not a direct alias for IE10 compatibility
                _setImmediate(fn);
            ***REMOVED***;
            async.setImmediate = async.nextTick;
        ***REMOVED***
        else ***REMOVED***
            async.nextTick = function (fn) ***REMOVED***
                setTimeout(fn, 0);
            ***REMOVED***;
            async.setImmediate = async.nextTick;
        ***REMOVED***
    ***REMOVED***
    else ***REMOVED***
        async.nextTick = process.nextTick;
        if (_setImmediate) ***REMOVED***
            async.setImmediate = function (fn) ***REMOVED***
              // not a direct alias for IE10 compatibility
              _setImmediate(fn);
            ***REMOVED***;
        ***REMOVED***
        else ***REMOVED***
            async.setImmediate = async.nextTick;
        ***REMOVED***
    ***REMOVED***

    async.each = function (arr, iterator, callback) ***REMOVED***
        callback = callback || noop;
        if (!arr.length) ***REMOVED***
            return callback();
        ***REMOVED***
        var completed = 0;
        _each(arr, function (x) ***REMOVED***
            iterator(x, only_once(done) );
        ***REMOVED***);
        function done(err) ***REMOVED***
          if (err) ***REMOVED***
              callback(err);
              callback = noop;
          ***REMOVED***
          else ***REMOVED***
              completed += 1;
              if (completed >= arr.length) ***REMOVED***
                  callback();
              ***REMOVED***
          ***REMOVED***
        ***REMOVED***
    ***REMOVED***;
    async.forEach = async.each;

    async.eachSeries = function (arr, iterator, callback) ***REMOVED***
        callback = callback || noop;
        if (!arr.length) ***REMOVED***
            return callback();
        ***REMOVED***
        var completed = 0;
        var iterate = function () ***REMOVED***
            iterator(arr[completed], function (err) ***REMOVED***
                if (err) ***REMOVED***
                    callback(err);
                    callback = noop;
                ***REMOVED***
                else ***REMOVED***
                    completed += 1;
                    if (completed >= arr.length) ***REMOVED***
                        callback();
                    ***REMOVED***
                    else ***REMOVED***
                        iterate();
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED***);
        ***REMOVED***;
        iterate();
    ***REMOVED***;
    async.forEachSeries = async.eachSeries;


    async.eachLimit = function (arr, limit, iterator, callback) ***REMOVED***
        var fn = _eachLimit(limit);
        fn.apply(null, [arr, iterator, callback]);
    ***REMOVED***;
    async.forEachLimit = async.eachLimit;

    var _eachLimit = function (limit) ***REMOVED***

        return function (arr, iterator, callback) ***REMOVED***
            callback = callback || noop;
            if (!arr.length || limit <= 0) ***REMOVED***
                return callback();
            ***REMOVED***
            var completed = 0;
            var started = 0;
            var running = 0;

            (function replenish () ***REMOVED***
                if (completed >= arr.length) ***REMOVED***
                    return callback();
                ***REMOVED***

                while (running < limit && started < arr.length) ***REMOVED***
                    started += 1;
                    running += 1;
                    iterator(arr[started - 1], function (err) ***REMOVED***
                        if (err) ***REMOVED***
                            callback(err);
                            callback = noop;
                        ***REMOVED***
                        else ***REMOVED***
                            completed += 1;
                            running -= 1;
                            if (completed >= arr.length) ***REMOVED***
                                callback();
                            ***REMOVED***
                            else ***REMOVED***
                                replenish();
                            ***REMOVED***
                        ***REMOVED***
                    ***REMOVED***);
                ***REMOVED***
            ***REMOVED***)();
        ***REMOVED***;
    ***REMOVED***;



    async.forEachOf = async.eachOf = function (object, iterator, callback) ***REMOVED***
        callback = callback || function () ***REMOVED******REMOVED***;
        var size = object.length || _keys(object).length;
        var completed = 0;
        if (!size) ***REMOVED***
            return callback();
        ***REMOVED***
        _forEachOf(object, function (value, key) ***REMOVED***
            iterator(object[key], key, function (err) ***REMOVED***
                if (err) ***REMOVED***
                    callback(err);
                    callback = function () ***REMOVED******REMOVED***;
                ***REMOVED*** else ***REMOVED***
                    completed += 1;
                    if (completed === size) ***REMOVED***
                        callback(null);
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED***);
        ***REMOVED***);
    ***REMOVED***;

    async.forEachOfSeries = async.eachOfSeries = function (obj, iterator, callback) ***REMOVED***
        callback = callback || function () ***REMOVED******REMOVED***;
        var keys = _keys(obj);
        var size = keys.length;
        if (!size) ***REMOVED***
            return callback();
        ***REMOVED***
        var completed = 0;
        var iterate = function () ***REMOVED***
            var sync = true;
            var key = keys[completed];
            iterator(obj[key], key, function (err) ***REMOVED***
                if (err) ***REMOVED***
                    callback(err);
                    callback = function () ***REMOVED******REMOVED***;
                ***REMOVED***
                else ***REMOVED***
                    completed += 1;
                    if (completed >= size) ***REMOVED***
                        callback(null);
                    ***REMOVED***
                    else ***REMOVED***
                        if (sync) ***REMOVED***
                            async.nextTick(iterate);
                        ***REMOVED***
                        else ***REMOVED***
                            iterate();
                        ***REMOVED***
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED***);
            sync = false;
        ***REMOVED***;
        iterate();
    ***REMOVED***;



    async.forEachOfLimit = async.eachOfLimit = function (obj, limit, iterator, callback) ***REMOVED***
        _forEachOfLimit(limit)(obj, iterator, callback);
    ***REMOVED***;

    var _forEachOfLimit = function (limit) ***REMOVED***

        return function (obj, iterator, callback) ***REMOVED***
            callback = callback || function () ***REMOVED******REMOVED***;
            var keys = _keys(obj);
            var size = keys.length;
            if (!size || limit <= 0) ***REMOVED***
                return callback();
            ***REMOVED***
            var completed = 0;
            var started = 0;
            var running = 0;

            (function replenish () ***REMOVED***
                if (completed >= size) ***REMOVED***
                    return callback();
                ***REMOVED***

                while (running < limit && started < size) ***REMOVED***
                    started += 1;
                    running += 1;
                    var key = keys[started - 1];
                    iterator(obj[key], key, function (err) ***REMOVED***
                        if (err) ***REMOVED***
                            callback(err);
                            callback = function () ***REMOVED******REMOVED***;
                        ***REMOVED***
                        else ***REMOVED***
                            completed += 1;
                            running -= 1;
                            if (completed >= size) ***REMOVED***
                                callback();
                            ***REMOVED***
                            else ***REMOVED***
                                replenish();
                            ***REMOVED***
                        ***REMOVED***
                    ***REMOVED***);
                ***REMOVED***
            ***REMOVED***)();
        ***REMOVED***;
    ***REMOVED***;


    var doParallel = function (fn) ***REMOVED***
        return function () ***REMOVED***
            var args = _baseSlice(arguments);
            return fn.apply(null, [async.each].concat(args));
        ***REMOVED***;
    ***REMOVED***;
    var doParallelLimit = function(limit, fn) ***REMOVED***
        return function () ***REMOVED***
            var args = _baseSlice(arguments);
            return fn.apply(null, [_eachLimit(limit)].concat(args));
        ***REMOVED***;
    ***REMOVED***;
    var doSeries = function (fn) ***REMOVED***
        return function () ***REMOVED***
            var args = _baseSlice(arguments);
            return fn.apply(null, [async.eachSeries].concat(args));
        ***REMOVED***;
    ***REMOVED***;


    var _asyncMap = function (eachfn, arr, iterator, callback) ***REMOVED***
        arr = _map(arr, function (x, i) ***REMOVED***
            return ***REMOVED***index: i, value: x***REMOVED***;
        ***REMOVED***);
        if (!callback) ***REMOVED***
            eachfn(arr, function (x, callback) ***REMOVED***
                iterator(x.value, function (err) ***REMOVED***
                    callback(err);
                ***REMOVED***);
            ***REMOVED***);
        ***REMOVED*** else ***REMOVED***
            var results = [];
            eachfn(arr, function (x, callback) ***REMOVED***
                iterator(x.value, function (err, v) ***REMOVED***
                    results[x.index] = v;
                    callback(err);
                ***REMOVED***);
            ***REMOVED***, function (err) ***REMOVED***
                callback(err, results);
            ***REMOVED***);
        ***REMOVED***
    ***REMOVED***;
    async.map = doParallel(_asyncMap);
    async.mapSeries = doSeries(_asyncMap);
    async.mapLimit = function (arr, limit, iterator, callback) ***REMOVED***
        return _mapLimit(limit)(arr, iterator, callback);
    ***REMOVED***;

    var _mapLimit = function(limit) ***REMOVED***
        return doParallelLimit(limit, _asyncMap);
    ***REMOVED***;

    // reduce only has a series version, as doing reduce in parallel won't
    // work in many situations.
    async.reduce = function (arr, memo, iterator, callback) ***REMOVED***
        async.eachSeries(arr, function (x, callback) ***REMOVED***
            iterator(memo, x, function (err, v) ***REMOVED***
                memo = v;
                callback(err);
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            callback(err, memo);
        ***REMOVED***);
    ***REMOVED***;
    // inject alias
    async.inject = async.reduce;
    // foldl alias
    async.foldl = async.reduce;

    async.reduceRight = function (arr, memo, iterator, callback) ***REMOVED***
        var reversed = _map(arr, function (x) ***REMOVED***
            return x;
        ***REMOVED***).reverse();
        async.reduce(reversed, memo, iterator, callback);
    ***REMOVED***;
    // foldr alias
    async.foldr = async.reduceRight;

    var _filter = function (eachfn, arr, iterator, callback) ***REMOVED***
        var results = [];
        arr = _map(arr, function (x, i) ***REMOVED***
            return ***REMOVED***index: i, value: x***REMOVED***;
        ***REMOVED***);
        eachfn(arr, function (x, callback) ***REMOVED***
            iterator(x.value, function (v) ***REMOVED***
                if (v) ***REMOVED***
                    results.push(x);
                ***REMOVED***
                callback();
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            callback(_map(results.sort(function (a, b) ***REMOVED***
                return a.index - b.index;
            ***REMOVED***), function (x) ***REMOVED***
                return x.value;
            ***REMOVED***));
        ***REMOVED***);
    ***REMOVED***;
    async.filter = doParallel(_filter);
    async.filterSeries = doSeries(_filter);
    // select alias
    async.select = async.filter;
    async.selectSeries = async.filterSeries;

    var _reject = function (eachfn, arr, iterator, callback) ***REMOVED***
        var results = [];
        arr = _map(arr, function (x, i) ***REMOVED***
            return ***REMOVED***index: i, value: x***REMOVED***;
        ***REMOVED***);
        eachfn(arr, function (x, callback) ***REMOVED***
            iterator(x.value, function (v) ***REMOVED***
                if (!v) ***REMOVED***
                    results.push(x);
                ***REMOVED***
                callback();
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            callback(_map(results.sort(function (a, b) ***REMOVED***
                return a.index - b.index;
            ***REMOVED***), function (x) ***REMOVED***
                return x.value;
            ***REMOVED***));
        ***REMOVED***);
    ***REMOVED***;
    async.reject = doParallel(_reject);
    async.rejectSeries = doSeries(_reject);

    var _detect = function (eachfn, arr, iterator, main_callback) ***REMOVED***
        eachfn(arr, function (x, callback) ***REMOVED***
            iterator(x, function (result) ***REMOVED***
                if (result) ***REMOVED***
                    main_callback(x);
                    main_callback = noop;
                ***REMOVED***
                else ***REMOVED***
                    callback();
                ***REMOVED***
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            main_callback();
        ***REMOVED***);
    ***REMOVED***;
    async.detect = doParallel(_detect);
    async.detectSeries = doSeries(_detect);

    async.some = function (arr, iterator, main_callback) ***REMOVED***
        async.each(arr, function (x, callback) ***REMOVED***
            iterator(x, function (v) ***REMOVED***
                if (v) ***REMOVED***
                    main_callback(true);
                    main_callback = noop;
                ***REMOVED***
                callback();
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            main_callback(false);
        ***REMOVED***);
    ***REMOVED***;
    // any alias
    async.any = async.some;

    async.every = function (arr, iterator, main_callback) ***REMOVED***
        async.each(arr, function (x, callback) ***REMOVED***
            iterator(x, function (v) ***REMOVED***
                if (!v) ***REMOVED***
                    main_callback(false);
                    main_callback = noop;
                ***REMOVED***
                callback();
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            main_callback(true);
        ***REMOVED***);
    ***REMOVED***;
    // all alias
    async.all = async.every;

    async.sortBy = function (arr, iterator, callback) ***REMOVED***
        async.map(arr, function (x, callback) ***REMOVED***
            iterator(x, function (err, criteria) ***REMOVED***
                if (err) ***REMOVED***
                    callback(err);
                ***REMOVED***
                else ***REMOVED***
                    callback(null, ***REMOVED***value: x, criteria: criteria***REMOVED***);
                ***REMOVED***
            ***REMOVED***);
        ***REMOVED***, function (err, results) ***REMOVED***
            if (err) ***REMOVED***
                return callback(err);
            ***REMOVED***
            else ***REMOVED***
                var fn = function (left, right) ***REMOVED***
                    var a = left.criteria, b = right.criteria;
                    return a < b ? -1 : a > b ? 1 : 0;
                ***REMOVED***;
                callback(null, _map(results.sort(fn), function (x) ***REMOVED***
                    return x.value;
                ***REMOVED***));
            ***REMOVED***
        ***REMOVED***);
    ***REMOVED***;

    async.auto = function (tasks, callback) ***REMOVED***
        callback = callback || noop;
        var keys = _keys(tasks);
        var remainingTasks = keys.length;
        if (!remainingTasks) ***REMOVED***
            return callback();
        ***REMOVED***

        var results = ***REMOVED******REMOVED***;

        var listeners = [];
        var addListener = function (fn) ***REMOVED***
            listeners.unshift(fn);
        ***REMOVED***;
        var removeListener = function (fn) ***REMOVED***
            for (var i = 0; i < listeners.length; i += 1) ***REMOVED***
                if (listeners[i] === fn) ***REMOVED***
                    listeners.splice(i, 1);
                    return;
                ***REMOVED***
            ***REMOVED***
        ***REMOVED***;
        var taskComplete = function () ***REMOVED***
            remainingTasks--;
            _each(listeners.slice(0), function (fn) ***REMOVED***
                fn();
            ***REMOVED***);
        ***REMOVED***;

        addListener(function () ***REMOVED***
            if (!remainingTasks) ***REMOVED***
                var theCallback = callback;
                // prevent final callback from calling itself if it errors
                callback = noop;

                theCallback(null, results);
            ***REMOVED***
        ***REMOVED***);

        _each(keys, function (k) ***REMOVED***
            var task = _isArray(tasks[k]) ? tasks[k]: [tasks[k]];
            var taskCallback = function (err) ***REMOVED***
                var args = _baseSlice(arguments, 1);
                if (args.length <= 1) ***REMOVED***
                    args = args[0];
                ***REMOVED***
                if (err) ***REMOVED***
                    var safeResults = ***REMOVED******REMOVED***;
                    _each(_keys(results), function(rkey) ***REMOVED***
                        safeResults[rkey] = results[rkey];
                    ***REMOVED***);
                    safeResults[k] = args;
                    callback(err, safeResults);
                    // stop subsequent errors hitting callback multiple times
                    callback = noop;
                ***REMOVED***
                else ***REMOVED***
                    results[k] = args;
                    async.setImmediate(taskComplete);
                ***REMOVED***
            ***REMOVED***;
            var requires = task.slice(0, Math.abs(task.length - 1)) || [];
            // prevent dead-locks
            var len = requires.length;
            var dep;
            while (len--) ***REMOVED***
                if (!(dep = tasks[requires[len]])) ***REMOVED***
                    throw new Error('Has inexistant dependency');
                ***REMOVED***
                if (_isArray(dep) && !!~dep.indexOf(k)) ***REMOVED***
                    throw new Error('Has cyclic dependencies');
                ***REMOVED***
            ***REMOVED***
            var ready = function () ***REMOVED***
                return _reduce(requires, function (a, x) ***REMOVED***
                    return (a && results.hasOwnProperty(x));
                ***REMOVED***, true) && !results.hasOwnProperty(k);
            ***REMOVED***;
            if (ready()) ***REMOVED***
                task[task.length - 1](taskCallback, results);
            ***REMOVED***
            else ***REMOVED***
                var listener = function () ***REMOVED***
                    if (ready()) ***REMOVED***
                        removeListener(listener);
                        task[task.length - 1](taskCallback, results);
                    ***REMOVED***
                ***REMOVED***;
                addListener(listener);
            ***REMOVED***
        ***REMOVED***);
    ***REMOVED***;

    async.retry = function(times, task, callback) ***REMOVED***
        var DEFAULT_TIMES = 5;
        var attempts = [];
        // Use defaults if times not passed
        if (typeof times === 'function') ***REMOVED***
            callback = task;
            task = times;
            times = DEFAULT_TIMES;
        ***REMOVED***
        // Make sure times is a number
        times = parseInt(times, 10) || DEFAULT_TIMES;
        var wrappedTask = function(wrappedCallback, wrappedResults) ***REMOVED***
            var retryAttempt = function(task, finalAttempt) ***REMOVED***
                return function(seriesCallback) ***REMOVED***
                    task(function(err, result)***REMOVED***
                        seriesCallback(!err || finalAttempt, ***REMOVED***err: err, result: result***REMOVED***);
                    ***REMOVED***, wrappedResults);
                ***REMOVED***;
            ***REMOVED***;
            while (times) ***REMOVED***
                attempts.push(retryAttempt(task, !(times-=1)));
            ***REMOVED***
            async.series(attempts, function(done, data)***REMOVED***
                data = data[data.length - 1];
                (wrappedCallback || callback)(data.err, data.result);
            ***REMOVED***);
        ***REMOVED***;
        // If a callback is passed, run this as a controll flow
        return callback ? wrappedTask() : wrappedTask;
    ***REMOVED***;

    async.waterfall = function (tasks, callback) ***REMOVED***
        callback = callback || noop;
        if (!_isArray(tasks)) ***REMOVED***
          var err = new Error('First argument to waterfall must be an array of functions');
          return callback(err);
        ***REMOVED***
        if (!tasks.length) ***REMOVED***
            return callback();
        ***REMOVED***
        var wrapIterator = function (iterator) ***REMOVED***
            return function (err) ***REMOVED***
                if (err) ***REMOVED***
                    callback.apply(null, arguments);
                    callback = noop;
                ***REMOVED***
                else ***REMOVED***
                    var args = _baseSlice(arguments, 1);
                    var next = iterator.next();
                    if (next) ***REMOVED***
                        args.push(wrapIterator(next));
                    ***REMOVED***
                    else ***REMOVED***
                        args.push(callback);
                    ***REMOVED***
                    async.setImmediate(function () ***REMOVED***
                        iterator.apply(null, args);
                    ***REMOVED***);
                ***REMOVED***
            ***REMOVED***;
        ***REMOVED***;
        wrapIterator(async.iterator(tasks))();
    ***REMOVED***;

    var _parallel = function(eachfn, tasks, callback) ***REMOVED***
        callback = callback || noop;
        if (_isArray(tasks)) ***REMOVED***
            eachfn.map(tasks, function (fn, callback) ***REMOVED***
                if (fn) ***REMOVED***
                    fn(function (err) ***REMOVED***
                        var args = _baseSlice(arguments, 1);
                        if (args.length <= 1) ***REMOVED***
                            args = args[0];
                        ***REMOVED***
                        callback.call(null, err, args);
                    ***REMOVED***);
                ***REMOVED***
            ***REMOVED***, callback);
        ***REMOVED***
        else ***REMOVED***
            var results = ***REMOVED******REMOVED***;
            eachfn.each(_keys(tasks), function (k, callback) ***REMOVED***
                tasks[k](function (err) ***REMOVED***
                    var args = _baseSlice(arguments, 1);
                    if (args.length <= 1) ***REMOVED***
                        args = args[0];
                    ***REMOVED***
                    results[k] = args;
                    callback(err);
                ***REMOVED***);
            ***REMOVED***, function (err) ***REMOVED***
                callback(err, results);
            ***REMOVED***);
        ***REMOVED***
    ***REMOVED***;

    async.parallel = function (tasks, callback) ***REMOVED***
        _parallel(***REMOVED*** map: async.map, each: async.each ***REMOVED***, tasks, callback);
    ***REMOVED***;

    async.parallelLimit = function(tasks, limit, callback) ***REMOVED***
        _parallel(***REMOVED*** map: _mapLimit(limit), each: _eachLimit(limit) ***REMOVED***, tasks, callback);
    ***REMOVED***;

    async.series = function (tasks, callback) ***REMOVED***
        callback = callback || noop;
        if (_isArray(tasks)) ***REMOVED***
            async.mapSeries(tasks, function (fn, callback) ***REMOVED***
                if (fn) ***REMOVED***
                    fn(function (err) ***REMOVED***
                        var args = _baseSlice(arguments, 1);
                        if (args.length <= 1) ***REMOVED***
                            args = args[0];
                        ***REMOVED***
                        callback.call(null, err, args);
                    ***REMOVED***);
                ***REMOVED***
            ***REMOVED***, callback);
        ***REMOVED***
        else ***REMOVED***
            var results = ***REMOVED******REMOVED***;
            async.eachSeries(_keys(tasks), function (k, callback) ***REMOVED***
                tasks[k](function (err) ***REMOVED***
                    var args = _baseSlice(arguments, 1);
                    if (args.length <= 1) ***REMOVED***
                        args = args[0];
                    ***REMOVED***
                    results[k] = args;
                    callback(err);
                ***REMOVED***);
            ***REMOVED***, function (err) ***REMOVED***
                callback(err, results);
            ***REMOVED***);
        ***REMOVED***
    ***REMOVED***;

    async.iterator = function (tasks) ***REMOVED***
        var makeCallback = function (index) ***REMOVED***
            var fn = function () ***REMOVED***
                if (tasks.length) ***REMOVED***
                    tasks[index].apply(null, arguments);
                ***REMOVED***
                return fn.next();
            ***REMOVED***;
            fn.next = function () ***REMOVED***
                return (index < tasks.length - 1) ? makeCallback(index + 1): null;
            ***REMOVED***;
            return fn;
        ***REMOVED***;
        return makeCallback(0);
    ***REMOVED***;

    async.apply = function (fn) ***REMOVED***
        var args = _baseSlice(arguments, 1);
        return function () ***REMOVED***
            return fn.apply(
                null, args.concat(_baseSlice(arguments))
            );
        ***REMOVED***;
    ***REMOVED***;

    var _concat = function (eachfn, arr, fn, callback) ***REMOVED***
        var r = [];
        eachfn(arr, function (x, cb) ***REMOVED***
            fn(x, function (err, y) ***REMOVED***
                r = r.concat(y || []);
                cb(err);
            ***REMOVED***);
        ***REMOVED***, function (err) ***REMOVED***
            callback(err, r);
        ***REMOVED***);
    ***REMOVED***;
    async.concat = doParallel(_concat);
    async.concatSeries = doSeries(_concat);

    async.whilst = function (test, iterator, callback) ***REMOVED***
        if (test()) ***REMOVED***
            iterator(function (err) ***REMOVED***
                if (err) ***REMOVED***
                    return callback(err);
                ***REMOVED***
                async.whilst(test, iterator, callback);
            ***REMOVED***);
        ***REMOVED***
        else ***REMOVED***
            callback();
        ***REMOVED***
    ***REMOVED***;

    async.doWhilst = function (iterator, test, callback) ***REMOVED***
        iterator(function (err) ***REMOVED***
            if (err) ***REMOVED***
                return callback(err);
            ***REMOVED***
            var args = _baseSlice(arguments, 1);
            if (test.apply(null, args)) ***REMOVED***
                async.doWhilst(iterator, test, callback);
            ***REMOVED***
            else ***REMOVED***
                callback();
            ***REMOVED***
        ***REMOVED***);
    ***REMOVED***;

    async.until = function (test, iterator, callback) ***REMOVED***
        if (!test()) ***REMOVED***
            iterator(function (err) ***REMOVED***
                if (err) ***REMOVED***
                    return callback(err);
                ***REMOVED***
                async.until(test, iterator, callback);
            ***REMOVED***);
        ***REMOVED***
        else ***REMOVED***
            callback();
        ***REMOVED***
    ***REMOVED***;

    async.doUntil = function (iterator, test, callback) ***REMOVED***
        iterator(function (err) ***REMOVED***
            if (err) ***REMOVED***
                return callback(err);
            ***REMOVED***
            var args = _baseSlice(arguments, 1);
            if (!test.apply(null, args)) ***REMOVED***
                async.doUntil(iterator, test, callback);
            ***REMOVED***
            else ***REMOVED***
                callback();
            ***REMOVED***
        ***REMOVED***);
    ***REMOVED***;

    async.queue = function (worker, concurrency) ***REMOVED***
        if (concurrency === undefined) ***REMOVED***
            concurrency = 1;
        ***REMOVED***
        else if(concurrency === 0) ***REMOVED***
            throw new Error('Concurrency must not be zero');
        ***REMOVED***
        function _insert(q, data, pos, callback) ***REMOVED***
          if (!q.started)***REMOVED***
            q.started = true;
          ***REMOVED***
          if (!_isArray(data)) ***REMOVED***
              data = [data];
          ***REMOVED***
          if(data.length === 0) ***REMOVED***
             // call drain immediately if there are no tasks
             return async.setImmediate(function() ***REMOVED***
                 if (q.drain) ***REMOVED***
                     q.drain();
                 ***REMOVED***
             ***REMOVED***);
          ***REMOVED***
          _each(data, function(task) ***REMOVED***
              var item = ***REMOVED***
                  data: task,
                  callback: typeof callback === 'function' ? callback : null
              ***REMOVED***;

              if (pos) ***REMOVED***
                q.tasks.unshift(item);
              ***REMOVED*** else ***REMOVED***
                q.tasks.push(item);
              ***REMOVED***

              if (q.saturated && q.tasks.length === q.concurrency) ***REMOVED***
                  q.saturated();
              ***REMOVED***
              async.setImmediate(q.process);
          ***REMOVED***);
        ***REMOVED***

        var workers = 0;
        var q = ***REMOVED***
            tasks: [],
            concurrency: concurrency,
            saturated: null,
            empty: null,
            drain: null,
            started: false,
            paused: false,
            push: function (data, callback) ***REMOVED***
              _insert(q, data, false, callback);
            ***REMOVED***,
            kill: function () ***REMOVED***
              q.drain = null;
              q.tasks = [];
            ***REMOVED***,
            unshift: function (data, callback) ***REMOVED***
              _insert(q, data, true, callback);
            ***REMOVED***,
            process: function () ***REMOVED***
                if (!q.paused && workers < q.concurrency && q.tasks.length) ***REMOVED***
                    var task = q.tasks.shift();
                    if (q.empty && q.tasks.length === 0) ***REMOVED***
                        q.empty();
                    ***REMOVED***
                    workers += 1;
                    var next = function () ***REMOVED***
                        workers -= 1;
                        if (task.callback) ***REMOVED***
                            task.callback.apply(task, arguments);
                        ***REMOVED***
                        if (q.drain && q.tasks.length + workers === 0) ***REMOVED***
                            q.drain();
                        ***REMOVED***
                        q.process();
                    ***REMOVED***;
                    var cb = only_once(next);
                    worker(task.data, cb);
                ***REMOVED***
            ***REMOVED***,
            length: function () ***REMOVED***
                return q.tasks.length;
            ***REMOVED***,
            running: function () ***REMOVED***
                return workers;
            ***REMOVED***,
            idle: function() ***REMOVED***
                return q.tasks.length + workers === 0;
            ***REMOVED***,
            pause: function () ***REMOVED***
                if (q.paused === true) ***REMOVED*** return; ***REMOVED***
                q.paused = true;
            ***REMOVED***,
            resume: function () ***REMOVED***
                if (q.paused === false) ***REMOVED*** return; ***REMOVED***
                q.paused = false;
                var resumeCount = Math.min(q.concurrency, q.tasks.length);
                // Need to call q.process once per concurrent
                // worker to preserve full concurrency after pause
                for (var w = 1; w <= resumeCount; w++) ***REMOVED***
                    async.setImmediate(q.process);
                ***REMOVED***
            ***REMOVED***
        ***REMOVED***;
        return q;
    ***REMOVED***;

    async.priorityQueue = function (worker, concurrency) ***REMOVED***

        function _compareTasks(a, b)***REMOVED***
          return a.priority - b.priority;
        ***REMOVED***

        function _binarySearch(sequence, item, compare) ***REMOVED***
          var beg = -1,
              end = sequence.length - 1;
          while (beg < end) ***REMOVED***
            var mid = beg + ((end - beg + 1) >>> 1);
            if (compare(item, sequence[mid]) >= 0) ***REMOVED***
              beg = mid;
            ***REMOVED*** else ***REMOVED***
              end = mid - 1;
            ***REMOVED***
          ***REMOVED***
          return beg;
        ***REMOVED***

        function _insert(q, data, priority, callback) ***REMOVED***
          if (!q.started)***REMOVED***
            q.started = true;
          ***REMOVED***
          if (!_isArray(data)) ***REMOVED***
              data = [data];
          ***REMOVED***
          if(data.length === 0) ***REMOVED***
             // call drain immediately if there are no tasks
             return async.setImmediate(function() ***REMOVED***
                 if (q.drain) ***REMOVED***
                     q.drain();
                 ***REMOVED***
             ***REMOVED***);
          ***REMOVED***
          _each(data, function(task) ***REMOVED***
              var item = ***REMOVED***
                  data: task,
                  priority: priority,
                  callback: typeof callback === 'function' ? callback : null
              ***REMOVED***;

              q.tasks.splice(_binarySearch(q.tasks, item, _compareTasks) + 1, 0, item);

              if (q.saturated && q.tasks.length === q.concurrency) ***REMOVED***
                  q.saturated();
              ***REMOVED***
              async.setImmediate(q.process);
          ***REMOVED***);
        ***REMOVED***

        // Start with a normal queue
        var q = async.queue(worker, concurrency);

        // Override push to accept second parameter representing priority
        q.push = function (data, priority, callback) ***REMOVED***
          _insert(q, data, priority, callback);
        ***REMOVED***;

        // Remove unshift function
        delete q.unshift;

        return q;
    ***REMOVED***;

    async.cargo = function (worker, payload) ***REMOVED***
        var working     = false,
            tasks       = [];

        var cargo = ***REMOVED***
            tasks: tasks,
            payload: payload,
            saturated: null,
            empty: null,
            drain: null,
            drained: true,
            push: function (data, callback) ***REMOVED***
                if (!_isArray(data)) ***REMOVED***
                    data = [data];
                ***REMOVED***
                _each(data, function(task) ***REMOVED***
                    tasks.push(***REMOVED***
                        data: task,
                        callback: typeof callback === 'function' ? callback : null
                    ***REMOVED***);
                    cargo.drained = false;
                    if (cargo.saturated && tasks.length === payload) ***REMOVED***
                        cargo.saturated();
                    ***REMOVED***
                ***REMOVED***);
                async.setImmediate(cargo.process);
            ***REMOVED***,
            process: function process() ***REMOVED***
                if (working) return;
                if (tasks.length === 0) ***REMOVED***
                    if(cargo.drain && !cargo.drained) cargo.drain();
                    cargo.drained = true;
                    return;
                ***REMOVED***

                var ts = typeof payload === 'number' ?
                    tasks.splice(0, payload) :
                    tasks.splice(0, tasks.length);

                var ds = _map(ts, function (task) ***REMOVED***
                    return task.data;
                ***REMOVED***);

                if(cargo.empty) cargo.empty();
                working = true;
                worker(ds, function () ***REMOVED***
                    working = false;

                    var args = arguments;
                    _each(ts, function (data) ***REMOVED***
                        if (data.callback) ***REMOVED***
                            data.callback.apply(null, args);
                        ***REMOVED***
                    ***REMOVED***);

                    process();
                ***REMOVED***);
            ***REMOVED***,
            length: function () ***REMOVED***
                return tasks.length;
            ***REMOVED***,
            running: function () ***REMOVED***
                return working;
            ***REMOVED***
        ***REMOVED***;
        return cargo;
    ***REMOVED***;

    var _console_fn = function (name) ***REMOVED***
        return function (fn) ***REMOVED***
            var args = _baseSlice(arguments, 1);
            fn.apply(null, args.concat([function (err) ***REMOVED***
                var args = _baseSlice(arguments, 1);
                if (typeof console !== 'undefined') ***REMOVED***
                    if (err) ***REMOVED***
                        if (console.error) ***REMOVED***
                            console.error(err);
                        ***REMOVED***
                    ***REMOVED***
                    else if (console[name]) ***REMOVED***
                        _each(args, function (x) ***REMOVED***
                            console[name](x);
                        ***REMOVED***);
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED***]));
        ***REMOVED***;
    ***REMOVED***;
    async.log = _console_fn('log');
    async.dir = _console_fn('dir');
    /*async.info = _console_fn('info');
    async.warn = _console_fn('warn');
    async.error = _console_fn('error');*/

    async.memoize = function (fn, hasher) ***REMOVED***
        var memo = ***REMOVED******REMOVED***;
        var queues = ***REMOVED******REMOVED***;
        hasher = hasher || function (x) ***REMOVED***
            return x;
        ***REMOVED***;
        var memoized = function () ***REMOVED***
            var args = _baseSlice(arguments);
            var callback = args.pop();
            var key = hasher.apply(null, args);
            if (key in memo) ***REMOVED***
                async.nextTick(function () ***REMOVED***
                    callback.apply(null, memo[key]);
                ***REMOVED***);
            ***REMOVED***
            else if (key in queues) ***REMOVED***
                queues[key].push(callback);
            ***REMOVED***
            else ***REMOVED***
                queues[key] = [callback];
                fn.apply(null, args.concat([function () ***REMOVED***
                    memo[key] = _baseSlice(arguments);
                    var q = queues[key];
                    delete queues[key];
                    for (var i = 0, l = q.length; i < l; i++) ***REMOVED***
                      q[i].apply(null, arguments);
                    ***REMOVED***
                ***REMOVED***]));
            ***REMOVED***
        ***REMOVED***;
        memoized.memo = memo;
        memoized.unmemoized = fn;
        return memoized;
    ***REMOVED***;

    async.unmemoize = function (fn) ***REMOVED***
      return function () ***REMOVED***
        return (fn.unmemoized || fn).apply(null, arguments);
      ***REMOVED***;
    ***REMOVED***;

    async.times = function (count, iterator, callback) ***REMOVED***
        var counter = [];
        for (var i = 0; i < count; i++) ***REMOVED***
            counter.push(i);
        ***REMOVED***
        return async.map(counter, iterator, callback);
    ***REMOVED***;

    async.timesSeries = function (count, iterator, callback) ***REMOVED***
        var counter = [];
        for (var i = 0; i < count; i++) ***REMOVED***
            counter.push(i);
        ***REMOVED***
        return async.mapSeries(counter, iterator, callback);
    ***REMOVED***;

    async.seq = function (/* functions... */) ***REMOVED***
        var fns = arguments;
        return function () ***REMOVED***
            var that = this;
            var args = _baseSlice(arguments);
            var callback = args.pop();
            async.reduce(fns, args, function (newargs, fn, cb) ***REMOVED***
                fn.apply(that, newargs.concat([function () ***REMOVED***
                    var err = arguments[0];
                    var nextargs = _baseSlice(arguments, 1);
                    cb(err, nextargs);
                ***REMOVED***]));
            ***REMOVED***,
            function (err, results) ***REMOVED***
                callback.apply(that, [err].concat(results));
            ***REMOVED***);
        ***REMOVED***;
    ***REMOVED***;

    async.compose = function (/* functions... */) ***REMOVED***
      return async.seq.apply(null, Array.prototype.reverse.call(arguments));
    ***REMOVED***;

    var _applyEach = function (eachfn, fns /*args...*/) ***REMOVED***
        var go = function () ***REMOVED***
            var that = this;
            var args = _baseSlice(arguments);
            var callback = args.pop();
            return eachfn(fns, function (fn, cb) ***REMOVED***
                fn.apply(that, args.concat([cb]));
            ***REMOVED***,
            callback);
        ***REMOVED***;
        if (arguments.length > 2) ***REMOVED***
            var args = _baseSlice(arguments, 2);
            return go.apply(this, args);
        ***REMOVED***
        else ***REMOVED***
            return go;
        ***REMOVED***
    ***REMOVED***;
    async.applyEach = doParallel(_applyEach);
    async.applyEachSeries = doSeries(_applyEach);

    async.forever = function (fn, callback) ***REMOVED***
        function next(err) ***REMOVED***
            if (err) ***REMOVED***
                if (callback) ***REMOVED***
                    return callback(err);
                ***REMOVED***
                throw err;
            ***REMOVED***
            fn(next);
        ***REMOVED***
        next();
    ***REMOVED***;

    // Node.js
    if (typeof module !== 'undefined' && module.exports) ***REMOVED***
        module.exports = async;
    ***REMOVED***
    // AMD / RequireJS
    else if (typeof define !== 'undefined' && define.amd) ***REMOVED***
        define([], function () ***REMOVED***
            return async;
        ***REMOVED***);
    ***REMOVED***
    // included directly via <script> tag
    else ***REMOVED***
        root.async = async;
    ***REMOVED***

***REMOVED***());

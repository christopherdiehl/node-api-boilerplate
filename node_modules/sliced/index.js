
/**
 * An Array.prototype.slice.call(arguments) alternative
 *
 * @param ***REMOVED***Object***REMOVED*** args something with a length
 * @param ***REMOVED***Number***REMOVED*** slice
 * @param ***REMOVED***Number***REMOVED*** sliceEnd
 * @api public
 */

module.exports = function (args, slice, sliceEnd) ***REMOVED***
  var ret = [];
  var len = args.length;

  if (0 === len) return ret;

  var start = slice < 0
    ? Math.max(0, slice + len)
    : slice || 0;

  if (sliceEnd !== undefined) ***REMOVED***
    len = sliceEnd < 0
      ? sliceEnd + len
      : sliceEnd
  ***REMOVED***

  while (len-- > start) ***REMOVED***
    ret[len - start] = args[len];
  ***REMOVED***

  return ret;
***REMOVED***


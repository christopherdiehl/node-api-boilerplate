/*!
 * type-is
 * Copyright(c) 2014 Jonathan Ong
 * Copyright(c) 2014-2015 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module dependencies.
 * @private
 */

var typer = require('media-typer')
var mime = require('mime-types')

/**
 * Module exports.
 * @public
 */

module.exports = typeofrequest
module.exports.is = typeis
module.exports.hasBody = hasbody
module.exports.normalize = normalize
module.exports.match = mimeMatch

/**
 * Compare a `value` content-type with `types`.
 * Each `type` can be an extension like `html`,
 * a special shortcut like `multipart` or `urlencoded`,
 * or a mime type.
 *
 * If no types match, `false` is returned.
 * Otherwise, the first `type` that matches is returned.
 *
 * @param ***REMOVED***String***REMOVED*** value
 * @param ***REMOVED***Array***REMOVED*** types
 * @public
 */

function typeis (value, types_) ***REMOVED***
  var i
  var types = types_

  // remove parameters and normalize
  var val = tryNormalizeType(value)

  // no type or invalid
  if (!val) ***REMOVED***
    return false
  ***REMOVED***

  // support flattened arguments
  if (types && !Array.isArray(types)) ***REMOVED***
    types = new Array(arguments.length - 1)
    for (i = 0; i < types.length; i++) ***REMOVED***
      types[i] = arguments[i + 1]
    ***REMOVED***
  ***REMOVED***

  // no types, return the content type
  if (!types || !types.length) ***REMOVED***
    return val
  ***REMOVED***

  var type
  for (i = 0; i < types.length; i++) ***REMOVED***
    if (mimeMatch(normalize(type = types[i]), val)) ***REMOVED***
      return type[0] === '+' || type.indexOf('*') !== -1
        ? val
        : type
    ***REMOVED***
  ***REMOVED***

  // no matches
  return false
***REMOVED***

/**
 * Check if a request has a request body.
 * A request with a body __must__ either have `transfer-encoding`
 * or `content-length` headers set.
 * http://www.w3.org/Protocols/rfc2616/rfc2616-sec4.html#sec4.3
 *
 * @param ***REMOVED***Object***REMOVED*** request
 * @return ***REMOVED***Boolean***REMOVED***
 * @public
 */

function hasbody (req) ***REMOVED***
  return req.headers['transfer-encoding'] !== undefined ||
    !isNaN(req.headers['content-length'])
***REMOVED***

/**
 * Check if the incoming request contains the "Content-Type"
 * header field, and it contains any of the give mime `type`s.
 * If there is no request body, `null` is returned.
 * If there is no content type, `false` is returned.
 * Otherwise, it returns the first `type` that matches.
 *
 * Examples:
 *
 *     // With Content-Type: text/html; charset=utf-8
 *     this.is('html'); // => 'html'
 *     this.is('text/html'); // => 'text/html'
 *     this.is('text/*', 'application/json'); // => 'text/html'
 *
 *     // When Content-Type is application/json
 *     this.is('json', 'urlencoded'); // => 'json'
 *     this.is('application/json'); // => 'application/json'
 *     this.is('html', 'application/*'); // => 'application/json'
 *
 *     this.is('html'); // => false
 *
 * @param ***REMOVED***String|Array***REMOVED*** types...
 * @return ***REMOVED***String|false|null***REMOVED***
 * @public
 */

function typeofrequest (req, types_) ***REMOVED***
  var types = types_

  // no body
  if (!hasbody(req)) ***REMOVED***
    return null
  ***REMOVED***

  // support flattened arguments
  if (arguments.length > 2) ***REMOVED***
    types = new Array(arguments.length - 1)
    for (var i = 0; i < types.length; i++) ***REMOVED***
      types[i] = arguments[i + 1]
    ***REMOVED***
  ***REMOVED***

  // request content type
  var value = req.headers['content-type']

  return typeis(value, types)
***REMOVED***

/**
 * Normalize a mime type.
 * If it's a shorthand, expand it to a valid mime type.
 *
 * In general, you probably want:
 *
 *   var type = is(req, ['urlencoded', 'json', 'multipart']);
 *
 * Then use the appropriate body parsers.
 * These three are the most common request body types
 * and are thus ensured to work.
 *
 * @param ***REMOVED***String***REMOVED*** type
 * @private
 */

function normalize (type) ***REMOVED***
  if (typeof type !== 'string') ***REMOVED***
    // invalid type
    return false
  ***REMOVED***

  switch (type) ***REMOVED***
    case 'urlencoded':
      return 'application/x-www-form-urlencoded'
    case 'multipart':
      return 'multipart/*'
  ***REMOVED***

  if (type[0] === '+') ***REMOVED***
    // "+json" -> "*/*+json" expando
    return '*/*' + type
  ***REMOVED***

  return type.indexOf('/') === -1
    ? mime.lookup(type)
    : type
***REMOVED***

/**
 * Check if `expected` mime type
 * matches `actual` mime type with
 * wildcard and +suffix support.
 *
 * @param ***REMOVED***String***REMOVED*** expected
 * @param ***REMOVED***String***REMOVED*** actual
 * @return ***REMOVED***Boolean***REMOVED***
 * @private
 */

function mimeMatch (expected, actual) ***REMOVED***
  // invalid type
  if (expected === false) ***REMOVED***
    return false
  ***REMOVED***

  // split types
  var actualParts = actual.split('/')
  var expectedParts = expected.split('/')

  // invalid format
  if (actualParts.length !== 2 || expectedParts.length !== 2) ***REMOVED***
    return false
  ***REMOVED***

  // validate type
  if (expectedParts[0] !== '*' && expectedParts[0] !== actualParts[0]) ***REMOVED***
    return false
  ***REMOVED***

  // validate suffix wildcard
  if (expectedParts[1].substr(0, 2) === '*+') ***REMOVED***
    return expectedParts[1].length <= actualParts[1].length + 1 &&
      expectedParts[1].substr(1) === actualParts[1].substr(1 - expectedParts[1].length)
  ***REMOVED***

  // validate subtype
  if (expectedParts[1] !== '*' && expectedParts[1] !== actualParts[1]) ***REMOVED***
    return false
  ***REMOVED***

  return true
***REMOVED***

/**
 * Normalize a type and remove parameters.
 *
 * @param ***REMOVED***string***REMOVED*** value
 * @return ***REMOVED***string***REMOVED***
 * @private
 */

function normalizeType (value) ***REMOVED***
  // parse the type
  var type = typer.parse(value)

  // remove the parameters
  type.parameters = undefined

  // reformat it
  return typer.format(type)
***REMOVED***

/**
 * Try to normalize a type and remove parameters.
 *
 * @param ***REMOVED***string***REMOVED*** value
 * @return ***REMOVED***string***REMOVED***
 * @private
 */

function tryNormalizeType (value) ***REMOVED***
  try ***REMOVED***
    return normalizeType(value)
  ***REMOVED*** catch (err) ***REMOVED***
    return null
  ***REMOVED***
***REMOVED***
